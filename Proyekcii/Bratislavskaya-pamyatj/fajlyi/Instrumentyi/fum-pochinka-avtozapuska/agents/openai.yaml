interface:
  display_name: "Починка автозапуска"
  short_description: "Запускает отдельную ограждённую задачу починки"
  default_prompt: "Используй $fum-pochinka-avtozapuska только по явному пользовательскому запросу в прикреплённой задаче диспетчера: огради один внеполосный запуск отдельной задачи в том же локальном проекте, немедленно освободи FIFO и не выполняй ремонт в родительском ходе."
