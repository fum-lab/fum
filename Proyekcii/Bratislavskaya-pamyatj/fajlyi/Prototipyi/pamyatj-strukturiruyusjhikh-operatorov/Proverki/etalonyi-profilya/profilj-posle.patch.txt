--- a/Sources/FUMStructuringOperatorMemory/AutomationAndSynchronization.swift
+++ b/Sources/FUMStructuringOperatorMemory/AutomationAndSynchronization.swift
@@ -17,41 +17,37 @@
     let определение = ОпределениеОператора(
       идентификатор: fixture.operatorId, версия: 1,
       шаги: try fixture.steps.enumerated().map { номер, шаг in
-        let действие: ДействиеОператора
-        switch шаг.kind {
-        case .trim:
-          действие = .обрезать
-        case .lowercase:
-          действие = .нижнийРегистр
-        case .collapseWhitespace:
-          действие = .сжатьПробелы
-        case .replace:
-          guard let аргумент = шаг.argument,
-            let разделитель = аргумент.range(of: "=>")
-          else {
-            throw OperatorMemoryError.automationFailed("replace requires old=>new")
-          }
-          let старое = String(аргумент[..<разделитель.lowerBound])
-          действие =
-            старое.isEmpty
-            ? .префикс("")
-            : .заменить(
-              старое, String(аргумент[разделитель.upperBound...]))
-        case .prefix:
-          guard let аргумент = шаг.argument else {
-            throw OperatorMemoryError.automationFailed("prefix requires argument")
-          }
-          действие = .префикс(аргумент)
-        }
-        return ШагОператора(идентификатор: "шаг-\(номер)", действие: действие)
-      })
+      let действие: ДействиеОператора
+      switch шаг.kind {
+      case .trim:
+        действие = .обрезать
+      case .lowercase:
+        действие = .нижнийРегистр
+      case .collapseWhitespace:
+        действие = .сжатьПробелы
+      case .replace:
+        guard let аргумент = шаг.argument,
+          let разделитель = аргумент.range(of: "=>")
+        else {
+          throw OperatorMemoryError.automationFailed("replace requires old=>new")
+        }
+        let старое = String(аргумент[..<разделитель.lowerBound])
+        действие = старое.isEmpty ? .префикс("") : .заменить(
+          старое, String(аргумент[разделитель.upperBound...]))
+      case .prefix:
+        guard let аргумент = шаг.argument else {
+          throw OperatorMemoryError.automationFailed("prefix requires argument")
+        }
+        действие = .префикс(аргумент)
+      }
+      return ШагОператора(идентификатор: "шаг-\(номер)", действие: действие)
+    })
     let наблюдение = try выполнить(определение, вход: .текст(fixture.input))
     guard case .текст(let current) = наблюдение.результат else {
       throw OperatorMemoryError.automationFailed("Ожидался текст адаптера")
     }
     let traces = zip(fixture.steps, наблюдение.шаги).map { шаг, след in
-      AutomationStepTrace(
-        stepID: шаг.id, kind: шаг.kind,
+      AutomationStepTrace(stepID: шаг.id, kind: шаг.kind,
         inputHash: след.хэшВхода, outputHash: след.хэшВыхода)
     }
 
--- a/Sources/FUMStructuringOperatorMemory/ЗакрытыйРазбор.swift
+++ b/Sources/FUMStructuringOperatorMemory/ЗакрытыйРазбор.swift
@@ -130,9 +130,8 @@
       let байт = байты[позиция]
       позиция += 1
       if байт == 34 {
-        do {
-          return try JSONDecoder().decode(String.self, from: Data(байты[начало..<позиция]))
-        } catch { throw отказ() }
+        do { return try JSONDecoder().decode(String.self, from: Data(байты[начало..<позиция])) }
+        catch { throw отказ() }
       }
       guard байт >= 32 else { throw отказ() }
       if байт == 92 {
--- a/Sources/FUMStructuringOperatorMemory/ИсполнениеОператоров.swift
+++ b/Sources/FUMStructuringOperatorMemory/ИсполнениеОператоров.swift
@@ -6,10 +6,9 @@
   public let позицияБайта: Int?
   public let началоПоследовательности: Int?
 
-  init(
-    _ код: String, _ пояснение: String, позицияБайта: Int? = nil,
-    началоПоследовательности: Int? = nil
-  ) {
+  init(_ код: String, _ пояснение: String, позицияБайта: Int? = nil,
+    началоПоследовательности: Int? = nil)
+  {
     self.код = код
     self.пояснение = пояснение
     self.позицияБайта = позицияБайта
@@ -74,10 +73,9 @@
   public let операций: Int
   public let записейТрассы: Int
 
-  public init(
-    байтовВхода: Int = 262_144, байтовРезультата: Int = 1_048_576,
-    операций: Int = 16_777_216, записейТрассы: Int = 256
-  ) {
+  public init(байтовВхода: Int = 262_144, байтовРезультата: Int = 1_048_576,
+    операций: Int = 16_777_216, записейТрассы: Int = 256)
+  {
     self.байтовВхода = байтовВхода
     self.байтовРезультата = байтовРезультата
     self.операций = операций
@@ -124,12 +122,6 @@
   public let стадия: String
   public let наносекунды: UInt64
   public let исход: String
-
-  public init(стадия: String, наносекунды: UInt64, исход: String) {
-    self.стадия = стадия
-    self.наносекунды = наносекунды
-    self.исход = исход
-  }
 }
 
 private struct СчётчикИсполнения {
@@ -148,7 +140,8 @@
 
   mutating func записать(_ запись: СледПравилаОператора) {
     let начало = DispatchTime.now().uptimeNanoseconds
-    if трасса.count < пределы.записейТрассы { трасса.append(запись) } else { пропущено += 1 }
+    if трасса.count < пределы.записейТрассы { трасса.append(запись) }
+    else { пропущено += 1 }
     времяТрассы += DispatchTime.now().uptimeNanoseconds - начало
   }
 }
@@ -169,17 +162,11 @@
       if case .скаляры(let скаляры) = вход, !скаляры.allSatisfy(допустимыйСкаляр) {
         throw ОшибкаИсполнения("скаляр", "Вход содержит значение вне множества Unicode-скаляров")
       }
-      профиль?(
-        ЗамерИсполнения(
-          стадия: "проверка",
-          наносекунды:
-            DispatchTime.now().uptimeNanoseconds - началоПроверки, исход: "успешно"))
+      профиль?(ЗамерИсполнения(стадия: "проверка", наносекунды:
+        DispatchTime.now().uptimeNanoseconds - началоПроверки, исход: "успешно"))
     } catch {
-      профиль?(
-        ЗамерИсполнения(
-          стадия: "проверка",
-          наносекунды:
-            DispatchTime.now().uptimeNanoseconds - началоПроверки, исход: "отказ"))
+      профиль?(ЗамерИсполнения(стадия: "проверка", наносекунды:
+        DispatchTime.now().uptimeNanoseconds - началоПроверки, исход: "отказ"))
       throw error
     }
     var счётчик = СчётчикИсполнения(пределы: пределы)
@@ -199,8 +186,7 @@
             throw ОшибкаИсполнения("тип-входа", "Повтор правил принимает исходные байты")
           }
           let правила = имена.map { имя in определение.правила.first { $0.идентификатор == имя }! }
-          let значения = try повторить(
-            правила, байты: байты, шаг: шаг.идентификатор,
+          let значения = try повторить(правила, байты: байты, шаг: шаг.идентификатор,
             счётчик: &счётчик)
           скаляры = значения
           текущий = .скаляры(значения)
@@ -235,53 +221,37 @@
           throw ОшибкаИсполнения("предел-результата", "Превышен бюджет результата")
         }
         let началоСледа = DispatchTime.now().uptimeNanoseconds
-        шаги.append(
-          СледШагаОператора(
-            идентификатор: шаг.идентификатор,
-            хэшВхода: хэшВхода, хэшВыхода: sha256Digest(текущий.исходныеБайты)))
+        шаги.append(СледШагаОператора(идентификатор: шаг.идентификатор,
+          хэшВхода: хэшВхода, хэшВыхода: sha256Digest(текущий.исходныеБайты)))
         счётчик.времяТрассы += DispatchTime.now().uptimeNanoseconds - началоСледа
       }
       guard текущий.размер <= пределы.байтовРезультата else {
         throw ОшибкаИсполнения("предел-результата", "Превышен бюджет результата")
       }
     } catch {
-      профиль?(
-        ЗамерИсполнения(
-          стадия: "исполнение",
-          наносекунды:
-            DispatchTime.now().uptimeNanoseconds - началоИсполнения - счётчик.времяТрассы,
-          исход: "отказ"))
+      профиль?(ЗамерИсполнения(стадия: "исполнение", наносекунды:
+        DispatchTime.now().uptimeNanoseconds - началоИсполнения - счётчик.времяТрассы, исход: "отказ"))
       профиль?(ЗамерИсполнения(стадия: "трасса", наносекунды: счётчик.времяТрассы, исход: "отказ"))
       throw error
     }
-    профиль?(
-      ЗамерИсполнения(
-        стадия: "исполнение",
-        наносекунды:
-          DispatchTime.now().uptimeNanoseconds - началоИсполнения - счётчик.времяТрассы,
-        исход: "успешно"))
+    профиль?(ЗамерИсполнения(стадия: "исполнение", наносекунды:
+      DispatchTime.now().uptimeNanoseconds - началоИсполнения - счётчик.времяТрассы, исход: "успешно"))
     let началоТрассы = DispatchTime.now().uptimeNanoseconds
     let хэшОпределения = sha256Digest(try каноническиеДанные(определение.представление))
     let хэшВхода = sha256Digest(вход.исходныеБайты)
     // Хэш содержит тип входа, определение, результат и точную ограниченную трассу.
-    let безХэша = НаблюдениеОператора(
-      версияОпределения: определение.версия,
+    let безХэша = НаблюдениеОператора(версияОпределения: определение.версия,
       хэшОпределения: хэшОпределения, хэшИсходныхБайтов: хэшВхода, типВхода: вход.вид,
       результат: текущий, скаляры: скаляры, шаги: шаги, трасса: счётчик.трасса,
       пропущеноЗаписей: счётчик.пропущено, выполненоОпераций: счётчик.выполнено,
       хэшНаблюдения: "")
-    let результат = НаблюдениеОператора(
-      версияОпределения: определение.версия,
+    let результат = НаблюдениеОператора(версияОпределения: определение.версия,
       хэшОпределения: хэшОпределения, хэшИсходныхБайтов: хэшВхода, типВхода: вход.вид,
       результат: текущий, скаляры: скаляры, шаги: шаги, трасса: счётчик.трасса,
       пропущеноЗаписей: счётчик.пропущено, выполненоОпераций: счётчик.выполнено,
       хэшНаблюдения: sha256Digest(try каноническиеДанные(безХэша)))
-    профиль?(
-      ЗамерИсполнения(
-        стадия: "трасса",
-        наносекунды:
-          счётчик.времяТрассы + DispatchTime.now().uptimeNanoseconds - началоТрассы,
-        исход: "успешно"))
+    профиль?(ЗамерИсполнения(стадия: "трасса", наносекунды:
+      счётчик.времяТрассы + DispatchTime.now().uptimeNanoseconds - началоТрассы, исход: "успешно"))
     return результат
   }
 
@@ -289,10 +259,9 @@
     скаляр <= 0x10FFFF && !(0xD800...0xDFFF).contains(скаляр)
   }
 
-  private static func повторить(
-    _ правила: [ПравилоБайтов], байты: [UInt8], шаг: String,
-    счётчик: inout СчётчикИсполнения
-  ) throws -> [UInt32] {
+  private static func повторить(_ правила: [ПравилоБайтов], байты: [UInt8], шаг: String,
+    счётчик: inout СчётчикИсполнения) throws -> [UInt32]
+  {
     var позиция = 0
     var скаляры: [UInt32] = []
     while позиция < байты.count {
@@ -309,14 +278,10 @@
             break
           }
         }
-        if совпало {
-          выбранное = правило
-          break
-        }
+        if совпало { выбранное = правило; break }
       }
       guard let правило = выбранное else {
-        throw ОшибкаИсполнения(
-          достигнуто == байты.count ? "незавершённый-вход" : "нет-правила",
+        throw ОшибкаИсполнения(достигнуто == байты.count ? "незавершённый-вход" : "нет-правила",
           "Исходные байты не соответствуют определению", позицияБайта: достигнуто,
           началоПоследовательности: позиция)
       }
@@ -326,27 +291,23 @@
         скаляр |= (UInt32(байты[позиция + поле.байт]) & UInt32(поле.маска)) << поле.сдвиг
       }
       guard допустимыйСкаляр(скаляр) else {
-        throw ОшибкаИсполнения(
-          "скаляр", "Получено значение вне множества Unicode-скаляров",
+        throw ОшибкаИсполнения("скаляр", "Получено значение вне множества Unicode-скаляров",
           позицияБайта: позиция, началоПоследовательности: позиция)
       }
       guard скаляры.count < счётчик.пределы.байтовРезультата / 4 else {
         throw ОшибкаИсполнения("предел-результата", "Скаляры превышают бюджет")
       }
       скаляры.append(скаляр)
-      счётчик.записать(
-        СледПравилаОператора(
-          шаг: шаг, правило: правило.идентификатор,
-          позицияБайта: позиция, длина: правило.байты.count, скаляр: скаляр))
+      счётчик.записать(СледПравилаОператора(шаг: шаг, правило: правило.идентификатор,
+        позицияБайта: позиция, длина: правило.байты.count, скаляр: скаляр))
       позиция += правило.байты.count
     }
     return скаляры
   }
 
-  private static func изменитьТекст(
-    _ текст: String, действие: ДействиеОператора,
-    счётчик: inout СчётчикИсполнения
-  ) throws -> String {
+  private static func изменитьТекст(_ текст: String, действие: ДействиеОператора,
+    счётчик: inout СчётчикИсполнения) throws -> String
+  {
     let предел = счётчик.пределы.байтовРезультата
     var результат = ""
     func добавить(_ часть: String) throws {
@@ -367,13 +328,9 @@
       var нуженПробел = false
       for символ in текст {
         try счётчик.операция()
-        if символ.isWhitespace {
-          нуженПробел = !результат.isEmpty
-        } else {
-          if нуженПробел {
-            try добавить(" ")
-            нуженПробел = false
-          }
+        if символ.isWhitespace { нуженПробел = !результат.isEmpty }
+        else {
+          if нуженПробел { try добавить(" "); нуженПробел = false }
           try добавить(String(символ))
         }
       }
--- a/Sources/FUMStructuringOperatorMemory/КонечныйВвод.swift
+++ b/Sources/FUMStructuringOperatorMemory/КонечныйВвод.swift
@@ -33,10 +33,7 @@
     while позиция < аргументы.count {
       let ключ = аргументы[позиция]
       позиция += 1
-      if ключ == "--профиль", !профиль {
-        профиль = true
-        continue
-      }
+      if ключ == "--профиль", !профиль { профиль = true; continue }
       guard ["--определение", "--вход"].contains(ключ), значения[ключ] == nil,
         позиция < аргументы.count, !аргументы[позиция].isEmpty
       else { throw ОшибкаИсполнения("параметры", "Неизвестный, повторный или неполный параметр") }
@@ -50,9 +47,7 @@
   }
 
   public func значение(_ данные: Data) throws -> ЗначениеОператора {
-    guard данные.count <= 262_144 else {
-      throw ОшибкаИсполнения("предел-входа", "Вход превышает предел")
-    }
+    guard данные.count <= 262_144 else { throw ОшибкаИсполнения("предел-входа", "Вход превышает предел") }
     switch типВхода {
     case "байты": return .байты(Array(данные))
     case "текст":
--- a/Sources/FUMStructuringOperatorMemory/ОпределениеОператора.swift
+++ b/Sources/FUMStructuringOperatorMemory/ОпределениеОператора.swift
@@ -41,10 +41,8 @@
 
   var представление: УзелОпределения {
     let (оператор, аргументы) = действие.представление
-    return .объект([
-      "идентификатор": .строка(идентификатор),
-      "оператор": .строка(оператор), "аргументы": .объект(аргументы),
-    ])
+    return .объект(["идентификатор": .строка(идентификатор),
+      "оператор": .строка(оператор), "аргументы": .объект(аргументы)])
   }
 
   static func разобрать(_ узел: УзелОпределения) throws -> ШагОператора {
@@ -55,8 +53,7 @@
     switch имя {
     case "обрезать", "нижний-регистр", "сжать-пробелы":
       _ = try аргументы.объект([])
-      действие =
-        имя == "обрезать" ? .обрезать : (имя == "нижний-регистр" ? .нижнийРегистр : .сжатьПробелы)
+      действие = имя == "обрезать" ? .обрезать : (имя == "нижний-регистр" ? .нижнийРегистр : .сжатьПробелы)
     case "заменить":
       let значения = try аргументы.объект(["старое", "новое"])
       действие = .заменить(try значения["старое"]!.строка(), try значения["новое"]!.строка())
@@ -82,10 +79,7 @@
   public let от: Int
   public let до: Int
 
-  public init(от: Int, до: Int) {
-    self.от = от
-    self.до = до
-  }
+  public init(от: Int, до: Int) { self.от = от; self.до = до }
   func содержит(_ значение: Int) -> Bool { от <= значение && значение <= до }
   var представление: УзелОпределения { .объект(["от": .целое(от), "до": .целое(до)]) }
 }
@@ -96,9 +90,7 @@
   public let сдвиг: Int
 
   public init(байт: Int, маска: Int, сдвиг: Int) {
-    self.байт = байт
-    self.маска = маска
-    self.сдвиг = сдвиг
+    self.байт = байт; self.маска = маска; self.сдвиг = сдвиг
   }
   var представление: УзелОпределения {
     .объект(["байт": .целое(байт), "маска": .целое(маска), "сдвиг": .целое(сдвиг)])
@@ -111,15 +103,11 @@
   public let поля: [БитовоеПоле]
 
   public init(идентификатор: String, байты: [ДиапазонБайта], поля: [БитовоеПоле]) {
-    self.идентификатор = идентификатор
-    self.байты = байты
-    self.поля = поля
-  }
-  var представление: УзелОпределения {
-    .объект([
-      "идентификатор": .строка(идентификатор),
-      "байты": .массив(байты.map(\.представление)), "поля": .массив(поля.map(\.представление)),
-    ])
+    self.идентификатор = идентификатор; self.байты = байты; self.поля = поля
+  }
+  var представление: УзелОпределения {
+    .объект(["идентификатор": .строка(идентификатор),
+      "байты": .массив(байты.map(\.представление)), "поля": .массив(поля.map(\.представление))])
   }
   static func разобрать(_ узел: УзелОпределения) throws -> ПравилоБайтов {
     let значения = try узел.объект(["идентификатор", "байты", "поля"])
@@ -129,12 +117,10 @@
     }
     let поля = try значения["поля"]!.массив().map { узел in
       let поле = try узел.объект(["байт", "маска", "сдвиг"])
-      return БитовоеПоле(
-        байт: try поле["байт"]!.целое(), маска: try поле["маска"]!.целое(),
+      return БитовоеПоле(байт: try поле["байт"]!.целое(), маска: try поле["маска"]!.целое(),
         сдвиг: try поле["сдвиг"]!.целое())
     }
-    return ПравилоБайтов(
-      идентификатор: try значения["идентификатор"]!.строка(), байты: байты, поля: поля)
+    return ПравилоБайтов(идентификатор: try значения["идентификатор"]!.строка(), байты: байты, поля: поля)
   }
 }
 
@@ -144,21 +130,15 @@
   public let правила: [ПравилоБайтов]
   public let шаги: [ШагОператора]
 
-  public init(
-    идентификатор: String, версия: Int, правила: [ПравилоБайтов] = [], шаги: [ШагОператора]
-  ) {
-    self.идентификатор = идентификатор
-    self.версия = версия
-    self.правила = правила
-    self.шаги = шаги
-  }
-
-  var представление: УзелОпределения {
-    .объект([
-      "схема": .строка("fum.определение-оператора.1"),
+  public init(идентификатор: String, версия: Int, правила: [ПравилоБайтов] = [], шаги: [ШагОператора]) {
+    self.идентификатор = идентификатор; self.версия = версия
+    self.правила = правила; self.шаги = шаги
+  }
+
+  var представление: УзелОпределения {
+    .объект(["схема": .строка("fum.определение-оператора.1"),
       "идентификатор": .строка(идентификатор), "версия": .целое(версия),
-      "правила": .массив(правила.map(\.представление)), "шаги": .массив(шаги.map(\.представление)),
-    ])
+      "правила": .массив(правила.map(\.представление)), "шаги": .массив(шаги.map(\.представление))])
   }
 
   public func данные() throws -> Data {
@@ -168,14 +148,11 @@
 
   public static func разобрать(_ данные: Data) throws -> ОпределениеОператора {
     var разбор = try РазборОпределения(данные)
-    let поля = try разбор.разобрать().объект([
-      "схема", "идентификатор", "версия", "правила", "шаги",
-    ])
+    let поля = try разбор.разобрать().объект(["схема", "идентификатор", "версия", "правила", "шаги"])
     guard try поля["схема"]!.строка() == "fum.определение-оператора.1" else {
       throw ОшибкаИсполнения("схема", "Неподдержанная версия схемы определения")
     }
-    let определение = ОпределениеОператора(
-      идентификатор: try поля["идентификатор"]!.строка(),
+    let определение = ОпределениеОператора(идентификатор: try поля["идентификатор"]!.строка(),
       версия: try поля["версия"]!.целое(),
       правила: try поля["правила"]!.массив().map(ПравилоБайтов.разобрать),
       шаги: try поля["шаги"]!.массив().map(ШагОператора.разобрать))
@@ -193,27 +170,21 @@
     }
     try потребовать(имя(идентификатор) && (1...1_000_000).contains(версия), "Неверная идентичность")
     try потребовать(шаги.count <= 32 && правила.count <= 32, "Превышен предел шагов или правил")
-    try потребовать(
-      Set(шаги.map(\.идентификатор)).count == шаги.count
-        && Set(правила.map(\.идентификатор)).count == правила.count, "Повтор идентификатора")
+    try потребовать(Set(шаги.map(\.идентификатор)).count == шаги.count
+      && Set(правила.map(\.идентификатор)).count == правила.count, "Повтор идентификатора")
     for правило in правила {
-      try потребовать(
-        имя(правило.идентификатор) && (1...8).contains(правило.байты.count)
-          && (1...8).contains(правило.поля.count), "Неверное правило или отсутствие продвижения")
+      try потребовать(имя(правило.идентификатор) && (1...8).contains(правило.байты.count)
+        && (1...8).contains(правило.поля.count), "Неверное правило или отсутствие продвижения")
       for диапазон in правило.байты {
-        try потребовать(
-          (0...255).contains(диапазон.от) && диапазон.до >= диапазон.от
-            && диапазон.до <= 255, "Неверный диапазон байта")
+        try потребовать((0...255).contains(диапазон.от) && диапазон.до >= диапазон.от
+          && диапазон.до <= 255, "Неверный диапазон байта")
       }
       var занятыеБиты: UInt64 = 0
       for поле in правило.поля {
-        try потребовать(
-          (0..<правило.байты.count).contains(поле.байт)
-            && (1...255).contains(поле.маска) && (0...31).contains(поле.сдвиг),
-          "Неверное битовое поле")
+        try потребовать((0..<правило.байты.count).contains(поле.байт)
+          && (1...255).contains(поле.маска) && (0...31).contains(поле.сдвиг), "Неверное битовое поле")
         let биты = UInt64(поле.маска) << поле.сдвиг
-        try потребовать(
-          биты <= UInt32.max && биты & занятыеБиты == 0, "Переполнение или перекрытие полей")
+        try потребовать(биты <= UInt32.max && биты & занятыеБиты == 0, "Переполнение или перекрытие полей")
         занятыеБиты |= биты
       }
     }
@@ -221,16 +192,12 @@
       try потребовать(имя(шаг.идентификатор), "Неверное имя шага")
       switch шаг.действие {
       case .заменить(let старое, let новое):
-        try потребовать(
-          !старое.isEmpty && старое.utf8.count <= 4096 && новое.utf8.count <= 4096,
+        try потребовать(!старое.isEmpty && старое.utf8.count <= 4096 && новое.utf8.count <= 4096,
           "Неверный аргумент замены")
-      case .префикс(let текст):
-        try потребовать(текст.utf8.count <= 4096, "Превышен размер префикса")
-      case .упаковатьСлова(let ширина, _):
-        try потребовать([1, 2, 4].contains(ширина), "Неверная ширина слова")
+      case .префикс(let текст): try потребовать(текст.utf8.count <= 4096, "Превышен размер префикса")
+      case .упаковатьСлова(let ширина, _): try потребовать([1, 2, 4].contains(ширина), "Неверная ширина слова")
       case .повторитьПравила(let имена):
-        try потребовать(
-          !имена.isEmpty && имена.count <= 32 && Set(имена).count == имена.count,
+        try потребовать(!имена.isEmpty && имена.count <= 32 && Set(имена).count == имена.count,
           "Неверная композиция правил")
         let выбранные = имена.compactMap { имя in правила.first { $0.идентификатор == имя } }
         try потребовать(выбранные.count == имена.count, "Неизвестное имя правила")
@@ -245,16 +212,14 @@
       case .обрезать, .нижнийРегистр, .сжатьПробелы: break
       }
     }
-    try потребовать(
-      каноническиеДанные(представление).count <= 65_536, "Превышен размер определения")
+    try потребовать(каноническиеДанные(представление).count <= 65_536, "Превышен размер определения")
   }
 }
 
 public enum РесурсыОпределений {
   public static func прочитать(_ имя: String) throws -> Data {
     guard ["UTF-8-в-UTF-32LE", "нормализация"].contains(имя),
-      let адрес = Bundle.module.url(
-        forResource: имя, withExtension: "json", subdirectory: "Определения")
+      let адрес = Bundle.module.url(forResource: имя, withExtension: "json", subdirectory: "Определения")
     else { throw ОшибкаИсполнения("ресурс", "Нет сохранённого определения") }
     return try КонечныйВвод.определение(адрес.path)
   }
--- a/Sources/FUMStructuringOperatorMemoryProbe/main.swift
+++ b/Sources/FUMStructuringOperatorMemoryProbe/main.swift
@@ -67,24 +67,20 @@
     // Отдельный замер загрузки не включает проверку определения и исполнение.
     let длительностьЗагрузки = DispatchTime.now().uptimeNanoseconds - начало
     if параметры.профиль {
-      let запись = ЗамерИсполнения(
-        стадия: "загрузка", наносекунды: длительностьЗагрузки, исход: "успешно")
-      FileHandle.standardError.write(try каноническиеДанные(запись) + Data("\n".utf8))
+      let запись: [String: String] = ["стадия": "загрузка", "наносекунды": String(длительностьЗагрузки), "исход": "успешно"]
+      FileHandle.standardError.write(try canonicalJSONData(запись) + Data("\n".utf8))
     }
     let началоРазбора = DispatchTime.now().uptimeNanoseconds
     let определение = try ОпределениеОператора.разобрать(данные)
     let вход = try параметры.значение(входныеДанные)
     if параметры.профиль {
-      let запись = ЗамерИсполнения(
-        стадия: "разбор",
-        наносекунды: DispatchTime.now().uptimeNanoseconds - началоРазбора,
-        исход: "успешно")
-      FileHandle.standardError.write(try каноническиеДанные(запись) + Data("\n".utf8))
+      let запись: [String: String] = ["стадия": "разбор", "наносекунды": String(DispatchTime.now().uptimeNanoseconds - началоРазбора), "исход": "успешно"]
+      FileHandle.standardError.write(try canonicalJSONData(запись) + Data("\n".utf8))
     }
     defer {
       if параметры.профиль {
         for замер in замеры {
-          if let данные = try? каноническиеДанные(замер) {
+          if let данные = try? canonicalJSONData(замер) {
             FileHandle.standardError.write(данные + Data("\n".utf8))
           }
         }
--- a/Проверки/проверить-и-измерить.py
+++ b/Проверки/проверить-и-измерить.py
@@ -89,7 +89,7 @@
                 эталон = текущийХэш
                 стадии = [json.loads(строка) for строка in процесс.stderr.splitlines()]
                 assert [стадия['стадия'] for стадия in стадии] == ['загрузка', 'разбор', 'проверка', 'исполнение', 'трасса']
-                assert all(стадия['исход'] == 'успешно' and type(стадия['наносекунды']) is int and стадия['наносекунды'] >= 0 for стадия in стадии)
+                assert all(стадия['исход'] == 'успешно' and int(стадия['наносекунды']) >= 0 for стадия in стадии)
                 повторы.append({'номер': номер + 1, 'процесс_наносекунды': длительность, 'стадии': стадии})
             замеры.append({'сценарий': имя, 'байтов_входа': len(вход), 'вход_sha256': хэш(вход),
                 'определение_sha256': хэш(определение.read_bytes()), 'наблюдение_sha256': эталон,
