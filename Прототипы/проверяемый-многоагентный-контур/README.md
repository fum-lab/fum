# Проверяемый многоагентный контур

Этот самостоятельный безоконный SwiftPM-прототип задаёт набор версионированных границ проверяемого многоагентного контура FUM, отдельный профиль сохранения живого прогона, узкого исполнителя одного пишущего рабочего пакета, сериализованного CAS-интегратора с ограниченным реестром resolver-правил, локальный маршрут долговечного fork-подузла, автономный маршрут самостоятельного проекта и единую автономную сквозную приёмку всей репозиторной композиции. Предпусковой анализатор рабочего пакета возвращает `ready` либо `split_required`, анализаторы символического паспорта эпизода и паспорта репозиторной композиции — `valid` либо `invalid`, а восстанавливаемая общая память публикует каждое принятое событие как одно каноническое поколение от точного подтверждённого родителя. Отдельные закрытые схемы назначения и состояния конечной линейной цепочки связывают роль, границы, маршрут результата, карточки, рабочую ветку, частные паспорта, бюджеты, проверки, очередь и обязательные продолжения без запуска живого распределённого исполнения. Схема журнала и reducer версии 4 объединяют происхождение, корреляции, проверки и разногласия с доказательным выбором, шестимерным конечным бюджетом, точным ожидающим переходом и обязательной остановкой. Отдельный локальный режим автономной приёмки сводит эти части в четыре детерминированных сценария и печатает один канонический JSON-отчёт. Профиль `fum.live-distributed-run-generation.v1` дополнительно встраивает хэшированные артефакты фактической внешней оркестрации Codex и связывает две корневые сессии адресуемым преемником с типизированным результатом передачи.

Прототип не является готовым многоагентным runtime FUM. Он не создаёт реальные роли и эпизоды, не вызывает модель, не читает сеть и не координирует удалённых исполнителей. Интегратор разрешает только два заранее зарегистрированных класса и не претендует на устранение любого конфликта. Все пишущие, fork-, проектные и родительские репозитории сквозного стенда являются временными локальными bare-репозиториями. Стенд не выполняет сетевой либо иной внешний push и не создаёт внешний fork, реальный проект, публикуемый submodule или сетевой remote; локальные push-переходы к временным bare-репозиториям входят в проверяемую механику, локальная блокировка не выдаётся за распределённый консенсус, а воспроизводимость фикстур — за независимость моделей. Прототип детерминированно проверяет и воспроизводит уже переданные события, включая основания выбора и учёт расхода, но не превращает семантическую оценку проверяющего в доказанную истину. Память — локальный стенд для сотрудничающих процессов, а не готовая долговременная память FUM. Она сохраняет модель согласованности после аварии процесса, а не после потери питания; структурная проверка исполнителя и происхождения не является криптографической аутентификацией или доказательством семантической независимости.

## Контракт рабочего пакета версии 1

Верхнеуровневый JSON-объект закрыт для неизвестных полей и требует:

- `schema_version` со значением `1` и технический `package_id`;
- непустую `goal` и ровно одну поставку `deliverables` с ролью `primary` без зависимых поставок;
- конечный непустой манифест `inputs` с уникальными идентификаторами, ограниченными относительными путями, хэшами вида `sha256:<64 lowercase hex>` и явным признаком `required`;
- `change_scope` с политикой `listed_paths_only`, непустыми допустимыми путями и непустыми явными исключениями без пересечений; каждый путь обозначает сам объект и его поддерево;
- конечный список `dependencies`, каждая из которых имеет статус `resolved` и наблюдаемое обоснование;
- непустой конечный список `checks` и `handoff` с форматом и обязательными артефактами передачи;
- `budget` в декларативных `planning_units` с общим пределом и отдельными положительными лимитами `reading`, `work`, `verification`, `response` и `reserve`;
- `preflight`, фиксирующий запуск до модельного вызова и до изменения пользовательских данных.

Каждый вложенный объект также закрыт для неизвестных полей. Коллекции, строки, глубина и число узлов JSON имеют конечные пределы, повторные ключи отклоняются до обычного разбора объекта, а нарушения дедуплицируются и сортируются по коду, JSON Pointer и сообщению. Обязательные артефакты передачи должны находиться в допустимой области и не пересекать исключения. Отчёт содержит хэш точных входных байтов контракта и кодируется как канонический JSON с отсортированными ключами.

Анализатор получает явный корень рабочей области и читает только перечисленные входы. Корень удерживается открытым дескриптором, каждый компонент пути открывается относительно него через `openat` с запретом символических ссылок, а обязательный вход должен быть обычным читаемым файлом. Фактический SHA-256 обязан совпасть с манифестом; повторная проверка метаданных обнаруживает изменение во время чтения. Предел одного файла равен 16 МиБ, общий предел прочитанных входов — 64 МиБ, причём счётчики соблюдаются и для растущего файла. Положительная фикстура использует включённый в пакет реальный ресурс с настоящим хэшем; отрицательная фикстура отсутствующего входа ссылается на реально отсутствующий путь.

`ready` означает только то, что наблюдаемый пакет не нарушает статические инварианты версии 1. Оно не разрешает читать другие пути и не подтверждает, что будущий исполнитель сохранит входы неизменными после preflight; исполняющий контур должен удержать проверенный снимок или повторно проверить хэши перед работой.

## Паспорт мыслительного эпизода версии 1

Паспорт связывает идентификаторы общей цели и критериев, ролей и гипотез, рабочих пакетов и локальных манифестов входов, общей памяти, вкладов, инструментальных наблюдений, отдельной проверки, решения выбора и условия остановки. Содержимое этих сущностей не копируется в паспорт: центральный реестр `artifacts` объявляет только уникальный `artifact_id`, тип, значение `persistence = persisted` и SHA-256, а остальные разделы образуют проверяемый граф типизированных ссылок.

Допустимый паспорт содержит не менее двух вкладов с разными `package_id` и `input_manifest_id`. Вклады должны различаться хотя бы ролью или набором гипотез; одинаковые хэши входов и результатов при этом разрешены и не считаются свидетельством независимости. Каждый вклад согласован со своим рабочим пакетом по роли, гипотезам и манифесту, а все обязательные ссылки замкнуты на объект требуемого типа.

Проверка, политика выбора и условие остановки представлены отдельными сущностями. Выбор допускает только основание `verified_evidence`, а `evidence_policy` обязано явно запрещать считать согласие доказательством и выводить независимость из числа вкладов. Паспорт отклоняет голосование утверждениями, отсутствующую роль, общий рабочий пакет и несохранённую память. В передачу `handoff` допускаются только объявленные сохранённые артефакты с хэшами; сообщения чата и скрытое рассуждение не являются допустимыми видами артефактов. Закреплённые паспортом политика выбора и условие остановки не являются заранее вычисленными результатами: динамические решение и терминальный исход возникают только как события журнала версии 4.

`valid` доказывает только синтаксическую и ссылочную замкнутость декларации версии 1. Анализатор паспорта не открывает сами артефакты, не сверяет их байты с хэшами, не запускает предпусковой анализ рабочих пакетов, не устанавливает истинность вкладов, не доказывает их семантическую независимость и не подтверждает фактическое исполнение проверки, выбора или остановки.

## Паспорт репозиторной композиции версии 1

Закрытая схема отделяет родительский репозиторий композиции от трёх видов дочерней линии. `step_branch` задаёт эфемерную ветку внутри уже известного репозитория и поэтому не имеет пути submodule или gitlink. `specialized_subnode` задаёт долговечный fork общего upstream, а `project` — самостоятельный репозиторий; оба вида требуют устойчивую идентичность дочернего узла, полный `refs/heads/...`, путь submodule, точный `gitlink_oid`, режим чистого detached-снимка и отдельный пишущий клон. Для каждой линии паспорт также закрепляет точный базовый commit, уровень доступа, публикационную границу, обязательные проверки и маршрут передачи вверх. Репозитории представлены устойчивыми URI-идентичностями; машинно-локальная схема `file` запрещена. Временные пути доказательной среды остаются во внешнем runtime-контексте и не попадают в паспорт.

Автономный preflight использует только Git plumbing над локальными bare-репозиториями. Он проверяет существование каждого объявленного commit, точное значение полного live-ref и цепочку `base_oid ≤ gitlink_oid ≤ live-ref`, а для submodule отдельно читает режим `160000`, OID и `.gitmodules` из дерева родительского снимка. Gitlink сравнивается именно с сохранённым снимком: более новая вершина дочерней ветки не изменяет его неявно. Фикстура создаёт свежий clone родителя и восстанавливает два объявленных submodule без `--recursive`; рабочее дерево каждого снимка должно быть чистым, находиться в detached HEAD на gitlink и иметь родительский superproject. Отдельность писателя доказывается разными Git common-dir, совпадением origin, символического ref и HEAD с bare live-ref, а не только разными строками путей.

Проверка закрывается машиночитаемым `invalid` при неизвестных полях, имени ветки вместо OID, коротком ref, отсутствующей ревизии, несовместимой границе доступа, повторе идентичности репозитория или пути submodule, цикле идентичностей, ссылке дочернего submodule на предка и саморекурсивной инициализации. Отрицательные bare-репозитории содержат соответствующие настоящие gitlink и `.gitmodules`; валидатор выводит наблюдаемый граф из точных дочерних деревьев и обнаруживает цикл, даже если паспорт скрыл декларативное ребро. Он никогда не вызывает рекурсивную инициализацию submodule и не обращается к сети.

Композиционный валидатор не выполняет запись сам. Соседний исполнитель пишущего пакета использует его топологическую границу, но не интегрирует кандидат вверх и не обновляет gitlink. Создание временного проектного репозитория, исполнение дочернего шага и отдельное CAS-обновление родителя принадлежат автономному контуру ниже; внешний fork, настоящий проектный submodule или GitHub-ресурс он не создаёт.

## Паспорт универсального fork-подузла

Отдельный версионный слой сохраняет схему версии `1` без переосмысления `specialized_subnode`: этот вид остаётся только историческим контрактом завершённых фикстур. Целевой дочерний fork описывается как универсальный исполнительный профиль с назначаемой контекстной ролью и отдельной границей полномочий. Паспорт связывает устойчивые идентичности ядра, композиции, трёх репозиториев и ребёнка, разные `origin` и `upstream`, зеркальный `master`, ролевые refs, путь submodule, точный принятый gitlink, полный живой ref, дочерние branch-scoped FIFO, обязательное продолжение и branch-specific прямой selector. Полномочия задают конечный список разрешённых целей pull request, а конкретное назначение цепочки выбирает один класс, маршрут, целевые репозиторий и ref. Смена роли создаёт новое поколение предпочтения назначения, но не меняет идентичность ребёнка и не расширяет доступ.

Родительская регистрация хранит точную паспортную связь и gitlink, не копируя FIFO, квитанции продолжения, рабочий набор selector или живое состояние ветки. Материализованный submodule восстанавливается как чистый detached-снимок, тогда как пишущий клон имеет отдельный Git common-dir и собственный полный ref. Fork наследует instance-free ядро, а не assembly. Валидатор закрывается отказом при совпавших `origin` и `upstream`, неверном upstream, локальном commit либо divergence зеркального `master`, устаревшем поколении роли, отсутствующем или несовпавшем паспорте ребёнка, gitlink не от потомка закреплённой синхронизированной основы, использовании submodule как рабочего клона, self-gitlink и прямом либо транзитивном цикле.

Фикстуры полностью локальны: они материализуют только временный assembly-submodule и не создают внешний fork, сетевой remote, постоянный submodule, Codex-задачу или pull request. Они не заявляют живую оркестрацию, исполнение цепочки либо передачу результата на host. Host-сессии цепочки, её живое исполнение, ревью и интеграция остаются отдельными последующими слоями.

## Назначение и паспорт состояния конечной линейной цепочки

Две закрытые JSON-схемы поколения `1` добавляют самостоятельную границу поверх паспорта универсального исполнителя. Назначение ссылается на точный паспорт ребёнка, карточку цепочки, рабочий набор и конечный упорядоченный список карточек по устойчивым идентификаторам, безопасным относительным путям и SHA-256, не копируя содержимое документов. В то же хэшируемое значение входят одна контекстная роль, точное поколение корневого источника, исходный коммит, непрозрачная идентичность одной физической рабочей копии, отличный от цели полный рабочий ref, конечные области и исключения, инструменты, доступ, эффекты, бюджеты, параллелизм, глубина рекурсии, проверки, остановка, возобновление, терминальные исходы, класс и маршрут результата, целевые репозиторий и ref. Для общего вклада в ядро дополнительно обязательны точная цель pull request и явный признак кандидата переносимого навыка; для собственного и проектного маршрутов эти поля запрещены.

`хэш_назначения` вычисляется по каноническому JSON после исключения самого поля хэша и повторяется в паспорте состояния. Паспорт состояния связывает начальную задачу среды, ту же физическую рабочую копию и рабочий ref, упорядоченные шаги, точные частные паспорта, расход и результаты проверок каждого шага, совокупный бюджет, однородительские коммиты, подтверждённые входные вершины, строгие номера веточной очереди и ровно одно заранее созданное продолжение каждого осмысленного коммита. Назначенный, активный, остановленный и готовый префиксы различаются явно; активный владелец и завершение без коммита получают отдельные допуски и доверенные свидетельства. Продолжение обязано сохранить рабочую копию и ref; проверяющая и интеграционная задачи представлены отдельно и допускаются только после готовности диапазона, не становясь его промежуточными владельцами. Политика принятия разрешает всю цепочку либо явно названный существующий точный префикс.

Автономная каноническая фикстура содержит два последовательных шага. Локальный конечный профиль действительно исполняет обе закрытые схемы JSON Schema Draft 2020-12, после чего семантический валидатор сопоставляет документы с отдельно предоставленным неизменяемым доверенным контекстом точных карточек и рабочего набора, эпохи и снимка FIFO, допусков, связанных commit-квитанций и `finish-clean`. Сорок три адресных сценария воспроизводят назначенный, активный, остановленный и готовый префиксы и закрывают структурные, хэшевые, маршрутные, бюджетные, Git- и FIFO-подмены, а также подмены частных паспортов и свидетельств. Это автономная проверка без сети, модели, Git-записи или host-вызова; она не строит доверенный контекст из живых источников и не доказывает фактическое создание задач, допуск FIFO, выполнение коммитов или интеграцию диапазона.

## Исторический долговечный специализированный fork-подузел и передача вверх

`DurableForkSubnodeRuntime` сохраняет завершённый исторический срез версии `1` и создаёт временные локальные bare-репозитории общего ядра, специализированного fork-подузла и родительской assembly. Регистрация строит fork от точного OID ядра, закрепляет устойчивые идентичности и закрытый паспорт специализации, настраивает в живом клоне раздельные `origin` и `upstream`, публикует полный живой ref и сохраняет собственные правила, каноническую карточку и рабочий набор следующего шага схемы `5`. Вместе с ними ветка наследует точные standalone-сценарии выбора шага и очереди; штатные `validate`, `show` и `status` проверяют файлы и отдельную checkout-local идентичность очереди до публикации fork. Этот runtime остаётся воспроизводимой основой нового универсального паспорта, но сам не задаёт целевую онтологию FUM-агента.

Assembly добавляет fork как submodule без рекурсивной инициализации и фиксирует точный достижимый gitlink. Восстановленный снимок обязан быть чистым detached-checkout на этом OID. Пишущий маршрут продолжает полный ref только в отдельном живом клоне, публикует кандидат в bare-репозиторий fork-подузла и доказывает неизменность родительского ref и снимка.

После независимого движения общего ядра синхронизация fork-подузла запускается отдельно, требует точный ожидаемый upstream OID, проверяет commit, diff и итоговое дерево, запрещает instance-gitlink и не двигает ref вслед за remote неявно. Затем паспорт передачи вверх связывает опубликованный исходный commit, его родителя, хэш паспорта специализации, область общей пользы, фактические хэшированные проверки, доступ, публикационную границу и новую точную базу ядра. Принятие сохраняет кандидат в родословной ядра, после чего assembly создаёт отдельный родительский commit, меняющий только объявленный gitlink на синхронизированную вершину fork.

Свежий клон родителя восстанавливает точный detached-снимок без выбора ветки, а новый живой клон повторно получает из паспорта `origin`, `upstream`, полный attached-ref, правила и следующий шаг. В нём штатные `validate` и `show` заново подтверждают selector и карточку, а сценарий очереди вычисляет новый служебный ref из Git-каталога этого checkout и выполняет собственный `join` с немедленным `finish-clean`. Service ref и активные билеты исходного клона не хранятся в bare fork и не переносятся. Отказы охватывают несовпавшие remote и OID, устаревшую базу, непубликуемый commit или diff, gitlink в общем upstream, конфликт синхронизации, недостижимый кандидат, ссылку submodule на репозиторий-предок и запрос recursive-init. Все отрицательные пути проверяют отсутствие неожиданного движения refs; наблюдаемые циклы и саморекурсивные gitlink-топологии дополнительно остаются общей границей `RepositoryCompositionPreflight`.

## Самостоятельный проектный репозиторий

Автономный local-bare контур создаёт отдельные репозитории проекта и родительской композиции. Первый проектный commit хранит читаемый `README.md`, связанный с закрытым машинным `Паспорт-проекта.json`, собственные `AGENTS.md`, штатные сценарии очереди и выбора следующего шага, карточку и schema-5 рабочий набор. Паспорт закрепляет цель, устойчивые идентичности проекта и репозитория, полный live-ref, доступ и публикационную границу, источники, проверки, условие завершения, хэш рабочего набора и пути дочерней плоскости управления.

Штатные `validate`, `show`, `status` и `claim` выполняются в физическом checkout проекта. Отдельные Git common-dir и вычисленные служебные refs доказывают, что очередь и claim ребёнка не используют состояние родительского checkout как признак допуска или простоя. В родительском commit сохраняются только узкая машинная регистрация `Проекты/регистрации/самостоятельный.json`, `.gitmodules` и точный gitlink; цель, карточка, очередь, claim и следующий шаг проекта туда не копируются.

Пишущий исполнитель создаёт кандидат только в отдельном живом клоне проекта, после чего проектный live-ref получает новый commit. Родитель остаётся неподвижным до отдельной интеграции: изолированный родительский клон меняет регистрацию и gitlink вместе и публикует новый родительский commit точным CAS. После этого live-ref проекта намеренно продвигается ещё одним непринятым дочерним commit. Свежий parent-only clone сначала сверяет прежний сохранённый gitlink без materialized submodule и не подменяет его новой вершиной live-ref, а затем явная нерекурсивная материализация восстанавливает чистый detached-снимок именно принятой ревизии.

Реализация и её автономная проверка находятся в точных файлах:

- [`РепозиторийПроекта.swift`](Sources/FUMVerifiableMultiAgentContour/РепозиторийПроекта.swift) — паспорт, закрытые preflight-проверки и локальный запуск дочерних инструментов;
- [`ФикстурыРепозиторияПроекта.swift`](Sources/FUMVerifiableMultiAgentContour/ФикстурыРепозиторияПроекта.swift) — построение временной Git-топологии и сценарии;
- [`ТестыРепозиторияПроекта.swift`](Tests/FUMVerifiableMultiAgentContourTests/ТестыРепозиторияПроекта.swift) — стабильный инвентарь, положительная приёмка, закрытые отказы и каноничность отчёта.

Инвентарь состоит из положительной фикстуры `сквозной-сценарий` и семи отрицательных: `обычный-каталог-вместо-gitlink`, `нет-паспорта`, `нет-следующего-шага`, `общий-checkout`, `неверный-gitlink`, `цикл`, `недоступная-публикация`. Отрицательные маршруты обязаны закрыться до непредусмотренной публикации и оставить родительский ref неизменным. Отчёт не содержит временных путей или `file`-URL. Сам этот узкий маршрут не создаёт реальный проект или submodule, не обращается к сети и не настраивает внешний remote; его совместное исполнение с fork-контуром проверяет единая сквозная приёмка ниже.

## Изолированный исполнитель пишущего пакета

`WritingSubnodeExecutor` принимает только уже проверенный WorkPackage v1 и отдельный типизированный запрос исполнения. Запрос связывает устойчивые идентификаторы эпизода, поколения шага, карточки, запуска, подузла и репозитория с точными `base_oid`, целевым ref, сообщением commit и конечным набором детерминированных записей. Машинные URL исходного checkout и каталога исполнения остаются runtime-параметрами и не кодируются в паспорте.

Непосредственно перед записью в кандидатный checkout исполнитель повторяет полный `WorkPackagePreflight`: хэши обязательных входов, `listed_paths_only`, исключения, разрешённые зависимости, конечный набор проверок, handoff и бюджеты должны совпасть с проверенным отчётом. Идентификаторы проверок разрешаются только через переданный доверенный реестр; текст `description` рабочего пакета не исполняется как shell-команда. Отсутствующая реализация проверки закрывает запуск до записи кандидатного содержимого.

Для нового `run_id` исполнитель создаёт собственный клон через `git clone --no-local --no-hardlinks --no-checkout`, удаляет runtime-remote, проверяет отдельные Git directory, common-dir и object store без alternates и создаёт уникальный полный `refs/heads/fum-step/...` от точного `base_oid`. Записи ограничены обычными файлами внутри разрешённой области; символические ссылки, выход за scope и фиксированные признаки секретов закрывают попытку. После проверки staged diff и доверенных проверок один непустой tree получает ровно один непосредственный commit от `base_oid`. Кандидат закрепляется отдельными прямыми branch ref и `refs/fum/results/...` только в клоне исполнителя.

До первой Git-команды над источником или восстанавливаемым клоном проверяются обычные компоненты каталогов и отсутствие специальных объектов и символических ссылок внутри `.git`; конфигурация читается без перехода по ссылке и разбирается через закрытый список безопасных ключей. Поэтому hooks, filters, внешние diff- и merge-команды, `alternateRefsCommand`, перенаправляющие worktree-настройки и сетевые помощники закрывают запуск до клонирования или восстановление до `git status`. Подача крупной безопасной конфигурации в Git и одновременное чтение его вывода выполняются разными системными потоками, не образуя взаимной блокировки pipe.

Внешний канонический паспорт связывает точные байты пакета и его предпускового отчёта, устойчивые идентификаторы, входы, зависимости, бюджеты, scope, фактические пути и хэш diff, проверки, commit, tree, родителя, оба refs и запланированный маршрут передачи. Паспорт хранится вне кандидатного commit: иначе включение собственного OID создало бы циклическую зависимость хэша. Он не содержит абсолютных путей, временных каталогов, host-значений или credentials, а состояние передачи остаётся `prepared` с отдельными ложными признаками принятия и публикации.

Исходы `no_op`, `blocked_before_write`, `forbidden_path`, `dirty_source`, `input_changed`, `secret_detected`, `publication_rejected`, `check_failed`, `run_already_exists` и `base_changed` не создают искусственный commit или result ref. Точный повтор успешно завершённого запроса с тем же `run_id` восстанавливает прежний паспорт и OID; повтор с иным устойчивым содержимым закрывается конфликтом. До и после каждого положительного и отрицательного сценария исполнитель сравнивает HEAD, символический HEAD, статус, refs, достижимые объекты и байты индекса исходного checkout.

Это узкий однопакетный слой. Сам по себе он не выдаёт доверенный реестр проверок за песочницу произвольного кода, выполняет ровно один пакет и не интегрирует его. Сквозной стенд оркестрирует два экземпляра этого слоя, считает покрытие запусков и передаёт два commit CAS-интегратору. Ни слой, ни стенд не выполняют push или сетевую передачу и не доказывают независимость либо истинность результата.

## CAS-интегратор и ограниченный resolver кандидатных commit

`CandidateCommitIntegrator` принимает точные идентификатор попытки и владельца, устойчивую идентичность репозитория, полный прямой `refs/heads/...`, ожидаемый OID цели, локальные runtime-пути, непустой набор кандидатных ссылок, непустой набор обязательных проверок и конечный список resolver-правил. Каждая кандидатная ссылка закрепляет `run_id`, точный commit OID и SHA-256 внешнего паспорта. Runtime-пути не входят в канонический хэш запроса или паспорт интеграции.

До записи интегратор требует раздельные целевой bare-репозиторий, интеграционный корень и корни исполнителей. Git-каталоги проходят закрытый аудит обычных файлов, конфигурации, alternates и локальных attributes; системные attributes отключены. Каждый кандидат независимо восстанавливается через `WritingSubnodeCandidateRecovery`, его паспорт, refs, commit, tree, родитель, diff, scope и публикационная чистота проверяются заново. Все кандидаты должны принадлежать тому же репозиторию и целевому ref, иметь общий непосредственный родительский commit, а этот родитель должен быть предком точной ожидаемой цели. Канонический порядок задаётся полным commit OID; разные пути, совпавшие после Unicode-нормализации и приведения регистра, закрывают попытку до слияния.

Один локальный владелец удерживает advisory-блокировку внутри физического целевого bare-репозитория по полному ref. Корректность публикации от неё не зависит: итог может стать видимым только через точный CAS. Интеграционный клон создаётся без local hardlinks и alternates, runtime-remote удаляется. Кандидаты сливаются последовательно в каноническом порядке; итоговый commit получает прежнюю цель и все исходные кандидаты прямыми родителями. Доверенный закрытый реестр после разрешения исполняет весь объявленный непустой набор проверок над итоговым tree дважды. Чистое Git-слияние без успешных проверок не принимается.

Реестр `fum.candidate-conflict-resolver-registry` версии `1` содержит ровно два алгоритма. `rebuild_derived_manifest_v1` полностью пересобирает точный выход из объявленных канонических regular-file-источников; построчный выбор `ours` или `theirs` не используется. `merge_stable_records_v1` base-aware-объединяет канонические JSON-записи только при уникальных нормализованных ID, точной схеме, одинаковых пересекающихся нормативных полях и отсутствии смыслового дублирования. Паспорт версии `2` фиксирует версию, путь, алгоритм, хэш спецификации, входные и выходной SHA-256, инварианты и обязательные проверки каждого правила.

Неизвестный путь, неоднозначное правило, нормализованная коллизия любого компонента итогового дерева, нарушение предусловия, расхождение схемы, повтор ID, противоречивое нормативное поле, смысловая несовместимость и провал проверки дают `resolution_required`. Каноническая `resolution-required.json` сохраняет commit, tree и blob OID всех входов, причины и проваленные проверки; прямые refs в клоне попытки оставляют исходные commit достижимыми, а целевой ref не меняется.

Интеграционный commit создаётся с прежней вершиной первым прямым родителем и всеми кандидатными commit следующими родителями в каноническом порядке. Он не переписывает и не удаляет исходные refs исполнителей. До передачи commit удерживается отдельным `refs/fum/integrations/...`, а паспорт версии `2` связывает запрос, tree, commit, всех родителей, паспорта кандидатов, resolver-правила и два прогона проверок. Возобновление не доверяет изменяемым runtime-артефактам: в отдельном временном клоне из закреплённых входных commit полностью повторяются merge и resolver, после чего сверяются весь tree, resolver-записи и точный канонический commit object. Сохранённая диагностика так же повторно вычисляется и должна совпасть по каноническим байтам.

Объекты передаются по прямому интеграционному ref без изменения целевого ref. После последней проверки ожидаемой вершины единственная публикационная транзакция `git update-ref --stdin` использует `option no-deref` и пару `новый OID, точный старый OID`. Движение цели до CAS даёт `target_changed`, проигрыш самой операции — `cas_lost`; оба исхода оставляют целевой ref интегратору неподвластным и не делают кандидатов недостижимыми. Квитанция успеха сохраняется после CAS. Если процесс остановился после публикации, точный повтор узнаёт уже достижимый интеграционный commit, повторяет проверки и завершает ту же попытку как `already_integrated`; изменённый запрос с прежним `attempt_id` отклоняется.

Тридцать автономных тестов используют настоящие локальные Git-репозитории. Помимо бесконфликтного CAS-контура они покрывают оба разрешённых класса, неизвестный и неоднозначный путь, схему, повтор ID, нормативное и смысловое противоречия, отсутствующую и проваленную обязательную проверку, regular-file-предусловие, нормализованные коллизии компонентов пути и подмену канонической диагностики, resolver-происхождения, commit payload и безопасного blob в prepared-tree. Отдельный sentinel подтверждает неподвижность постороннего FIFO-ref и checkout.

## Сквозная приёмка репозиторной композиции

`СквознаяПриёмкаРепозиторнойКомпозиции.выполнить()` собирает все узкие слои в один автономный сценарий на временных локальных bare-репозиториях. Два `WritingSubnodeExecutor` стартуют параллельно от закреплённых пакетов в разных клонах и уникальных ветках, публикуют два осмысленных кандидатных commit и не меняют исходный checkout. Отчёт покрытия сворачивает пять фактических событий допущенных пишущих запусков и три события интеграции: он отдельно фиксирует `commit`, `no-op`, блокировку, публикационный отказ и конфликт, а пустой commit признаёт только по равенству дерева реально созданного commit базовому дереву.

Бесконфликтный кандидат интегрируется точным CAS с сохранением исходного commit в родословной. Зарегистрированный класс конфликта проходит ограниченный resolver и повторные проверки, после чего публикуется отдельный интеграционный commit с исходными родителями. Неизвестный или смысловой конфликт возвращает `resolution_required`: целевой ref остаётся на прежнем OID, кандидатные refs — достижимыми, а каноническая диагностика сохраняется в отчёте.

В том же родительском дереве закрепляются точные gitlink долговечного fork-подузла и самостоятельного проекта. Fork продолжает свою ветку и передаёт общий результат вверх, а проект исполняет собственный следующий шаг и передаёт новую вершину родителю. Каждый дочерний commit хранит канонический `Планирование/состояние-очереди.json` со следующей последовательностью, хэшем следующего шага и преемственностью состояния. Свежий клон родителя восстанавливает оба чистых detached-снимка на закреплённых OID. Отдельные свежие живые клоны читают состояние очереди только из собственного `HEAD`, проверяют канонические байты и следующий шаг, создают новую checkout-local служебную ссылку, проводят очередной билет и подтверждают, что `refs/fum/` не переносились через bare-репозитории.

Повтор полного сценария даёт совпадающую карту шести паспортов и диагностик — двух кандидатов, двух интеграций, неизвестного конфликта и композиции — и совпадающие итоговые деревья. Версионированный профиль эквивалентности версии `1` сохраняет все канонические поля, кроме конечного явного перечня runtime-зависимых хэшей: `candidate.execution_request_sha256`, `candidate.source_repository_sha256`, `integration.candidates[].passport_sha256` и `integration.request_sha256`. Диагностика неизвестного конфликта и композиционный паспорт сравниваются без такой проекции.

Контролируемые прерывания охватывают восемь настоящих границ: публикацию обоих кандидатов, обе интеграции, продвижение fork, передачу в общее ядро, проект и родительский gitlink. В каждой точке объект уже передан и проверен, а ошибка вводится непосредственно перед атомарной заменой ref; целевой ref и квитанция успеха остаются неизменными, подготовленный commit достижим в базе объектов, после чего точный повтор завершает переход. Итог содержит девять типизированных проверок, журналы запусков и интеграций, две карты паспортов, две карты tree, свидетельства восстановления очередей и честную границу доказательства. `каноническиеДанные()` возвращает его канонический JSON.

## Общая память распределённого эпизода

Общая память переиспользует `CanonicalMemoryJSON` и `ContentAddressedGenerationStore` из уже проверенного одноагентного хранилища. Она не маскирует вклады под операции `remember` и `compose`, а добавляет доменные схему, reducer, журнал и валидатор преемственности над теми же каноническими байтами, `CURRENT` и межпроцессным CAS.

Пустое начальное поколение встраивает валидный паспорт и все нужные для replay UTF-8-артефакты, включая заранее объявленные критерии, политику выбора, конфигурацию бюджета и условие остановки. Каждый преемник добавляет ровно одно событие вклада, проверки, решения, бюджетной резервации или сверки, изменения ожидающего перехода либо терминального исхода. Происхождение версии 1 внутри вклада версии 2 закрепляет идентификаторы исполнителя, роли и рабочего пакета, наблюдаемые модель и поставщика, SHA-256 задачи, канонически упорядоченных локальных входов, родительского поколения и результата. Вклады с одинаковыми байтами и разным происхождением остаются различимыми и не превращаются в фиктивное согласие.

Исполнитель точно связан с автором канонического события, тогда как роль проверяется отдельно по паспорту. Расхождение этих деклараций закрывается отказом; реальная личность исполнителя криптографически не аттестуется.

Общие модель, поставщик, исходный материал и системный шаблон представлены группами корреляции. Использование родительского результата, прямая копия и производный ответ представлены направленными рёбрами к известному вкладу. Один вклад может входить в несколько групп, а их пересечения транзитивно образуют связные компоненты. Валидатор выводит `independent_by_observed_features`, `correlated`, `copy` или `unconfirmed_provenance` и считает не вклады, а компоненты с хотя бы одним полностью наблюдаемым вкладом, не являющимся копией. Поэтому коррелированный вклад и копия не увеличивают `independent_confirmation_count`, а `semantic_independence_proven` всегда остаётся ложным.

Инструментальное наблюдение хранит отдельные идентификатор и вид полномочия источника, идентичность вызова, SHA-256 входа и результата и время наблюдения. Паспорт связывает полный канонический объект наблюдения с точным вкладом и SHA-256. Повтор одного вызова связывает вклады, а противоречивый повтор закрывается отказом. Ссылка `derived_from_observation_ids` отмечает модельное утверждение как производное; его результат не подменяет результат инструмента и не наследует полномочие источника. Совпадение локального входа с хэшем такого результата требует эту ссылку.

Событие проверки отдельно называет проверяющего и его роль, заранее объявленные критерии, проверяемые утверждения с точными идентификатором вклада и SHA-256 результата, а также ссылки на сохранённые инструментальные наблюдения. Его исход ограничен значениями `passed`, `failed` и `inconclusive`. Отдельный выводимый статус равен `external_by_observed_features`, `self_verification`, `correlated_verification` либо `unconfirmed_provenance`: недопустимая роль закрывается отказом, совпадение исполнителя сохраняется как самопроверка, а запрещённая группа корреляции — как коррелированная проверка. Только первый статус получает ограниченный внешний вес, но и он не доказывает абсолютную независимость. Ссылки проверки на предмет и свидетельства сами по себе не создают запрещённую корреляцию с производителем.

Несколько одинаковых непроверенных ответов не образуют консенсус, а недостаточное или не связанное с утверждением свидетельство даёт `inconclusive`, не `passed`. Возражения, конфликты утверждений, отрицательные результаты и причины отклонения являются типизированными дописываемыми (`append-only`) записями. Состояние хранит их кумулятивно вместе со всеми проверками; последующее поколение, решение выбора и восстановление из `CURRENT` не заменяют их последним исходом.

Проверка формы подтверждает только полноту канонических полей, допустимость значений и замкнутость типизированных ссылок. Инструментально подтверждённым фактом остаётся лишь сохранённое наблюдение в границах его вида полномочия, идентичности вызова и SHA-256 входа и результата. `passed`, `failed` и `inconclusive` являются семантическими оценками объявленного проверяющего по объявленным критериям и свидетельствам; ссылка на наблюдение не передаёт оценке полномочие инструмента и не превращает её в доказанную истину.

### Доказательный выбор

Решение версии 4 является отдельным событием от точного родителя. Оно перечисляет заранее объявленные критерии, рассмотренные вклады с SHA-256 результатов, записи проверки, инструментальные доказательства, относящиеся к ним разногласия и disposition каждого кандидата. Снимок разногласия связывает допустимое доказательство с точными проверкой и утверждением; позднее разрешение принимает доказательство только из завершённой связанной через журнал заранее объявленной различающей проверки при совпадении утверждения, вклада и SHA-256 результата. SHA-256 полного фронтира выбора и ссылка на заменяемую редакцию запрещают `goal_met` по решению, устаревшему после нового вклада или проверки. Все ссылки разрешаются только в уже принятом префиксе журнала. При выборе существует ровно один выбранный результат; при отклонении либо неустранённом конфликте выбранного результата нет.

Число совпадающих ответов или `passed`-оценок не является основанием. Коррелированные вклады, копии, один проверяющий и одно наблюдение не умножают вес. Добавление коррелированных копий не должно менять результат. Внутренний выбор остаётся модельным кандидатом или рекомендацией и не создаёт пользовательского подтверждения, авторизации или допуска внешнего действия.

### Шестимерный бюджет и двухфазное списание

Исполняемый бюджет отдельно от статических `planning_units` хранит конечные целочисленные пределы `executors`, `rounds`, `model_calls`, `tool_calls`, `input_units` и `output_units`. Та же политика закрепляет версионный `metering_policy_id` и защищённые шестимерные резервы завершающей проверки и передачи. Для вкладов и проверок действующая версия детерминированно считает distinct-идентичности, наблюдаемый model-вызов, инструментальные наблюдения, закреплённые входные SHA-256 и байты канонической нагрузки. Обычное действие допускается только тогда, когда списанное потребление, незавершённые резервации, максимальная стоимость действия и защищённые остатки покомпонентно укладываются в предел без арифметического переполнения.

Перед модельным или инструментальным вызовом событие `reservation` сохраняет точные действие, родителя и максимальную стоимость отдельным поколением. Событие `settlement` ссылается на ту же резервацию и закрывает её следующим поколением; convenience-интерфейс явно возвращает оба CAS-предшественника. Неуспешная попытка не стирает расход, незавершённая после аварии резервация остаётся занятой, а канонический повтор одного события не списывает бюджет второй раз. Проверка и передача расходуют только предназначенные им резервы. Недостаток любой компоненты останавливает действие до вызова и сохраняет машиночитаемые требование и остаток.

### Ожидающий переход и остановка

Ожидающий внешний переход сохраняет точные идентификатор, версию, объект, ожидаемый эффект и политику подтверждения. Состояние `parked` закрывает только этот переход: пока существует безопасная продуктивная model-only-ветвь в бюджете, весь эпизод не получает `needs_input`. Модельный выбор и успешная внутренняя проверка не создают подтверждение или авторизацию. Поздний ответ должен совпасть с точным переходом и его версией; отказ, отзыв и устаревший ответ не становятся согласием.

Терминальный исход ровно один: `goal_met`, `budget_exhausted`, `needs_input`, `unresolved_conflict` или `failed`. Он содержит типизированную причину, окончательный бюджет, решение, ожидающие переходы и неустранённые разногласия. `goal_met` требует актуального выбора и пустого набора неустранённых разногласий; `needs_input` запрещён, пока укладывается хотя бы одно безопасное продуктивное продолжение; `unresolved_conflict` — пока доступна различающая проверка. После терминального события текущее семантическое поколение не принимает новые события. Возобновление создаёт новое семантическое поколение с новым контекстно посильным рабочим пакетом и точной ссылкой на SHA-256 терминального поколения предшественника; обычный следующий адресуемый blob того же журнала возобновлением не считается.

Новый процесс начинает с `CURRENT`, повторно проигрывает весь кумулятивный журнал и сравнивает точные канонические байты состояния. Происхождение, корреляции, инструментальные наблюдения, проверки, решения, остатки и резервации бюджета, ожидающий переход, терминальный исход и разногласия сохраняются без потерь. Прежний чат, внешняя фикстура и новый вызов модели для этого не нужны. Повреждение, несовместимая прежняя схема, неполная публикация, settlement без reservation, превышение бюджета, ложное повышение перехода, запись после остановки и устаревший родитель закрываются отказом; точный повтор идемпотентен, а неизвестные файлы не удаляются. Полный контракт закреплён в [документе 49](../../Документация/49-контракт-восстанавливаемой-общей-памяти-распределённого-эпизода.md).

## Автономная сквозная приёмка

Режим `acceptance all` выполняет четыре сценария в фиксированном порядке и выдаёт один детерминированный канонический JSON-отчёт. Отчёт отдельно сохраняет исход каждого сценария и общую границу проверки: используются только записанные фикстуры, живая модель и живой инструмент не вызываются, сеть не используется, а готовность живого многомодельного FUM остаётся ложной.

- `positive_goal_met` проводит валидный паспорт и два разных прошедших preflight рабочих пакета через вклады двух различимых производителей. Ровно одно утверждение связано с записанным инструментальным наблюдением, отдельный проверяющий получает внешний по наблюдаемым признакам статус, селектор сохраняет выбор по доказательству, а эпизод останавливается с `goal_met`. Между вкладами продолжение выполняется новым процессом от подтверждённого `CURRENT`; полный канонический итог непрерывного и возобновлённого прогонов совпадает побайтово.
- `false_consensus` сохраняет два одинаковых коррелированных ответа как один ограниченный компонент подтверждения, не публикует выбор без отдельного положительного доказательства и завершает эпизод с `unresolved_conflict`, сохранив отрицательную проверку и разногласия.
- `budget_exhaustion` отклоняет иначе допустимое действие до его публикации, сохраняет точные остаток, размерность, требуемое и доступное значения бюджета и останавливается с `budget_exhausted` без неподтверждённого результата.
- `pending_confirmation` запускает полностью локальный валидатор канонической трассы версии `3`: точный внешний переход остаётся закрытым, две ограниченные model-only-ветви продолжаются от общего модельного предка, их проверки и внутренний кандидатный отбор сохраняются, но пользовательское подтверждение и внешнее действие не фабрикуются.

Этот режим проверяет совместимость записанных исполнителей, инструментальных ответов и переходов с контрактами, а не фактическую работу живых провайдеров. Он не доказывает криптографическую подлинность ролей, семантическую независимость производителей, истинность их результатов или готовность распределённого многомодельного runtime.

## Живой прогон внешними исполнителями Codex

Один узкий прогон в общей корневой сессии поставил реальный вопрос к локальной памяти FUM двум внешним исполнителям Codex. Нормативный аналитик и исполняемый аудитор получили разные роли, непересекающиеся первичные входы и прошедшие предпусковой анализ рабочие пакеты; результаты друг друга им не передавались до публикации обоих вкладов. Отдельный проверяющий затем сопоставил все 11 утверждений с точными файлами и двумя свежими автономными командами. Все утверждения прошли проверку, расхождений не обнаружено, а корневое решение `accepted` основано на доказательствах, не на числе совпавших ответов.

Прогон сохранён в [каталоге живого прогона](Живые-прогоны/2026-08-02_15-36-30_MSK/). Там лежат паспорт, четыре рабочих пакета и их предпусковые отчёты, два вклада, наблюдаемое происхождение и группы корреляции, отдельная проверка, решение, отрицательные результаты и терминальный исход. Закрытый профиль первого поколения допускает ровно 15 артефактов известных видов; неизвестный вид, неизвестное поле на любом описанном уровне, повторный JSON-ключ или дробное число в целочисленном поле закрывают проверку. Архив требует точные SHA-256 исходных байтов, безопасные относительные пути, ровно две разные производящие роли, отдельного проверяющего с отличимыми исполнителем и ролью, четыре однозначные пары `work_package` ↔ `preflight`, точное равенство входов каждого исполнителя своему пакету по парам `(path, sha256)`, замкнутые ссылки решения и передачи, согласованную проекцию происхождения и явные группы корреляции. Сохранённый preflight каждого пакета обязан иметь `ready`, пустой список нарушений и SHA-256 точных байтов пакета; пакет следующего шага дополнительно проходит свежий анализ по текущим обязательным входам перед публикацией. Исторические preflight производителей и проверяющего не пересчитываются по изменившемуся после их работы checkout и фиксируют заявленную и наблюдавшуюся корнем последовательность, а не криптографическое время запуска. `CURRENT` указывает на подтверждённое 16-артефактное поколение-преемник, а первое 15-артефактное поколение остаётся его точным адресуемым родителем.

FUM-STEP-0083 завершил эту передачу в новой корневой сессии. Проверенный `CURRENT` закреплял точного родителя `sha256:c9c721deb92d3a2552af273a7304c66de99abc6f9637c7a6564733a0b0c8c089`; после сверки семи обязательных входов опубликовано ровно одно поколение-преемник `sha256:e759453e8f7cf12b3ddf735f20ffba7b374fe413c5046943ee40799e04661b9a`. Его закрытый 16-артефактный граф содержит единственный [`handoff_result`](Живые-прогоны/2026-08-02_15-36-30_MSK/результат-возобновления-FUM-STEP-0083.json) по заранее объявленному пути, точную связь с родителем и исполненным пакетом, новую корневую сессию, семь уникальных `input_checks` со статусом `passed`, совпадающий терминальный исход и `outcome = completed`. Повторные восстановления из `CURRENT` заново проверили всю цепочку и воспроизвели канонически совпадающие байты поколения. Это подтверждает узкое межсессионное восстановление из сохранённой памяти как машинно проверяемую структурную аттестацию, но не доказывает отсутствие скрытого чтения.

Первое поколение подтверждает работу Codex как внешних исполнителей локального стенда в одной корневой сессии, а преемник отдельно подтверждает структурную передачу сохранённого состояния новой корневой сессии. Оба производителя использовали одну поверхность оркестрации и общую конфигурационную границу модели; точная идентичность активной модели средой не раскрыта. Цепочка не доказывает семантическую независимость моделей, криптографическую подлинность ролей, истинность вкладов, отсутствие скрытого чтения, готовность внутреннего многоагентного runtime FUM, распределённый консенсус, сохранность после потери питания или готовую долговременную память.

## Граница оценки помещаемости

Статический бюджет рабочего пакета позволяет до запуска обнаружить отсутствующий резерв, неположительный или чрезмерный компонент, превышение общего объявленного предела и компонент ниже структурного минимума по числу входов, допустимых изменений, проверок или артефактов передачи. Его `planning_units` — ограниченная структурная шкала версии 1, а не токены и не компоненты исполняемого бюджета. Шестимерный бюджет общей памяти ограничивает сохранённые действия эпизода, но тоже не измеряет размер будущего контекстного окна.

Фактическое доказательство контекстной помещаемости потребует телеметрии чтения, работы, проверок, ответа и резерва от живого исполняющего контура с сопоставлением объявленного и наблюдаемого расхода. Версия 4 проверяет согласованность переданных reservation и settlement, но не доказывает достоверность телеметрии недоверенного адаптера и не вычисляет вероятность успеха.

## Воспроизводимый запуск

Положительная фикстура рабочего пакета запускается без аргументов из любого текущего каталога:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh
```

Пробник печатает один канонический JSON-отчёт и завершает работу с кодом `0` для `ready`. Отрицательная фикстура печатает `split_required` и использует код `3`, а ошибка команды — код `2`. Команда `stdin` считает явным корнем рабочей области текущий каталог процесса и читает из него только пути манифеста:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh fixture split-no-reserve
./Прототипы/проверяемый-многоагентный-контур/запустить.sh --list
printf '%s\n' '{"schema_version":1}' | ./Прототипы/проверяемый-многоагентный-контур/запустить.sh stdin
```

Фикстуры паспорта доступны через отдельную группу `episode`. Положительный отчёт `valid` завершается с кодом `0`, отрицательный `invalid` — с кодом `3`:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh episode fixture valid
./Прототипы/проверяемый-многоагентный-контур/запустить.sh episode fixture invalid-missing-role
./Прототипы/проверяемый-многоагентный-контур/запустить.sh episode --list
printf '%s\n' '{"schema_version":1}' | ./Прототипы/проверяемый-многоагентный-контур/запустить.sh episode stdin
```

Репозиторная композиция доступна через группу `composition`. Каждый запуск создаёт изолированную временную топологию из локальных bare-репозиториев, точных commit и двух разных checkout для снимка и писателя, а после отчёта удаляет её:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh composition fixture valid
./Прототипы/проверяемый-многоагентный-контур/запустить.sh composition fixture invalid-repository-cycle
./Прототипы/проверяемый-многоагентный-контур/запустить.sh composition --list
```

Положительный отчёт возвращает `valid` и код `0`; любое проверяемое нарушение возвращает `invalid` и код `3`, а ошибка команды или построения стенда — код `2`.

Единая сквозная приёмка репозиторной композиции запускается одним локальным пробником:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh composition acceptance
```

Команда сама создаёт и удаляет временную топологию, не требует сети, секретов или модели и печатает канонический JSON-отчёт.

Маршрут долговечного fork-подузла доступен через группу `fork`. Каждый сценарий создаёт и удаляет собственную локальную топологию без сети:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh fork fixture roundtrip
./Прототипы/проверяемый-многоагентный-контур/запустить.sh fork fixture invalid-sync-conflict
./Прототипы/проверяемый-многоагентный-контур/запустить.sh fork --list
```

Положительный `roundtrip` проверяет регистрацию, кандидат, передачу в ядро, явную синхронизацию, отдельное обновление gitlink и два свежих клона. Отрицательные сценарии считаются успешно пройденными только при ожидаемом закрытом отказе и отсутствии неожиданного движения refs.

Общая память проверяется последовательными безоконными процессами. Каталог задаётся явно и не должен находиться внутри репозитория, если его файлы не прошли отдельную публикационную проверку:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh memory bootstrap <каталог-поколений>
./Прототипы/проверяемый-многоагентный-контур/запустить.sh memory continue <каталог-поколений> primary
./Прототипы/проверяемый-многоагентный-контур/запустить.sh memory show <каталог-поколений>
```

Архив фактического прогона принимает канонический JSON-запрос, повторно хэширует только перечисленные файлы внутри явного корня репозитория и публикует встроенные байты через тот же адресуемый механизм:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh live canonicalize <запрос.json> > <канонический-запрос.json>
./Прототипы/проверяемый-многоагентный-контур/запустить.sh live archive <запрос.json> <каталог-поколений> --repo-root <корень-репозитория>
./Прототипы/проверяемый-многоагентный-контур/запустить.sh live show <каталог-поколений>
./Прототипы/проверяемый-многоагентный-контур/запустить.sh live --list
```

`live canonicalize` закрыто проверяет схему и печатает точные байты без завершающего перевода строки; `live archive` принимает именно такой запрос. `live show` заново проверяет `CURRENT`, адрес поколения, канонические байты, встроенный запрос, манифест, закрытый граф ролей и точные входные пары, не обращаясь к исходным файлам. Для преемника он заново загружает всю цепочку предков по контентным адресам и на каждом шаге требует новый корень, результат по единственному объявленному пути прежнего пакета, точную типизированную связь `handoff_result` с родительским поколением и исполненным пакетом, полный набор успешных `input_checks` и совпадение `terminal_outcome`. Отсутствующий или повреждённый предок закрывает восстановление отказом. Указатель маркирует общий байтовый профиль `fum.memory.canonical-json.v1`, а само поколение — доменный профиль `fum.live-distributed-run-generation.v1`. Состояние `replayed` в отчёте отличается от `archived`, но вложенное поколение и его SHA-256 должны совпадать.

Все четыре автономных приёмочных сценария запускаются одной командой без пути к рабочему каталогу:

```bash
./Прототипы/проверяемый-многоагентный-контур/запустить.sh acceptance all
```

Пробник сам создаёт и удаляет временные каталоги, перезапускает положительный сценарий отдельными процессами и проверяет локальную трассу ожидания подтверждения. Успех печатает единственный канонический JSON-отчёт и возвращает код `0`; нарушение любого сценария возвращает код `3`. Список фиксированных идентификаторов доступен через `acceptance --list`.

Пакет имеет только одну прямую локальную SwiftPM-зависимость от соседнего пакета `воспроизводимое-пополнение-памяти`; сеть и внешние сервисы не требуются. Автономные проверки выполняются так:

```bash
swift test --package-path Прототипы/проверяемый-многоагентный-контур
swift build --package-path Прототипы/проверяемый-многоагентный-контур --product FUMWorkPackageProbe
swift build --package-path Прототипы/проверяемый-многоагентный-контур --product FUMWorkPackageProbe -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
swift build --package-path Прототипы/проверяемый-многоагентный-контур --product FUMWritingSubnodePassportProbe -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
swift format lint --configuration Инструменты/fum-kompleksnaya-proverka-repozitoriya/swift-format.json --strict --recursive Прототипы/проверяемый-многоагентный-контур/Package.swift Прототипы/проверяемый-многоагентный-контур/Sources Прототипы/проверяемый-многоагентный-контур/Tests
```

Фикстуры рабочего пакета включают положительный случай и отрицательные варианты зависимых поставок, отсутствующего обязательного входа, неограниченной области изменений, неразрешённой зависимости и нулевого резерва. Автономный набор также проверяет неполный и повреждённый JSON, неизвестные поля, повторные ключи, фактическое несовпадение хэша, запрет символической ссылки, экстремальные числа и глубину, управляющие символы в путях, конфликт передачи с областью и структурно заниженный бюджет.

Паспорт эпизода имеет положительную фикстуру полного минимального графа и отрицательные варианты отсутствующей роли, общего пакета, голосования утверждениями и несохранённой памяти. Дополнительные сценарии проверяют различимость вкладов по роли или гипотезе, допустимость одинаковых хэшей, закрытые объекты, повторные ключи, висячие и неверно типизированные ссылки и устойчивую сортировку нарушений.

Фикстуры репозиторной композиции воспроизводят без сети валидные `step_branch`, специализированный fork и независимый проект, фиксированный gitlink при ушедшем вперёд live-ref, чистый detached-снимок и отдельный пишущий клон. Отрицательные варианты отдельно проверяют несовместимый доступ, ссылку submodule на предка, повтор идентичности, повтор пути, отсутствующую ревизию, цикл репозиториев и саморекурсивную топологию.

Фикстуры долговечного fork-подузла выполняют полный локальный roundtrip и отрицательные маршруты регистрации, передачи, синхронизации и восстановления. Они проверяют точные remotes и refs, достижимость кандидата, хэши правил и следующего шага, настоящий schema-5 selector, отдельные вычисленные refs штатной очереди, публикационную границу по фактическим байтам Git, конфликт без движения ref, запрет gitlink в общем upstream, точный detached-снимок и отказ от recursive-init. Успешная локальная передача одного тестового билета не означает перенос активных FIFO-билетов между клонами или запуск отдельного диспетчера.

Фикстуры самостоятельного проекта выполняют один полный local-bare roundtrip и семь закрытых отказов. Положительный маршрут проверяет дочерние паспорт, правила, очередь, claim, диспетчер и следующий шаг, узкую родительскую регистрацию, отдельные проектный и родительский CAS-коммиты, parent-only восстановление точного gitlink и последующую явную материализацию detached-снимка. Это проверка временной автономной топологии, а не создание настоящего проекта или сетевого remote.

Автономные сценарии пишущего исполнителя создают настоящий локальный Git-репозиторий и проверяют успешный кандидатный commit, `no-op`, запрещённый и Git-метаданный путь, изменившийся обязательный вход, закрытую декларативную проверку, повтор и конфликт одного `run_id`, восстановление паспорта отдельным безоконным процессом, повтор оборванной попытки в новом клоне, повреждение result ref, символическую ссылку вместо паспорта, грязный исходный checkout, секрет, машинно-локальное значение, бинарный diff и блокировку сырого пакета. Каждый сценарий сравнивает исходный checkout до и после исполнения; отрицательные исходы получают устойчивую квитанцию, но не искусственный commit или result ref.

Сценарии общей памяти должны сохранять прежние проверки продолжения новым экземпляром хранилища, самодостаточного replay, stale-конфликта и межпроцессной гонки, точной связи паспортных SHA с рабочими артефактами, повреждения и неполной публикации, точного повтора, staging-указателя, адресного orphan и неизвестных файлов.

Сценарии происхождения различают наблюдаемые источники, общую модель и системный шаблон, транзитивное пересечение групп, прямую копию, инструментальный результат и его модельный пересказ, неполную идентичность модели с поставщиком, общего исполнителя, повтор и противоречие одного инструментального вызова и скрытое потребление результата другого вклада.

Для версии 4 обязательны автономные сценарии выбора по доказательствам вопреки коррелированному большинству, отклонения голосования и пропущенного разногласия, точного и превышенного на единицу расхода каждой компоненты, защиты обоих резервов, незавершённой резервации, неблокирующего `parked`-перехода, преждевременных `needs_input` и `unresolved_conflict`, каждого терминального исхода, запрета записи после остановки и побайтового восстановления решения, бюджета, исхода и разногласий. Этот перечень задаёт приёмочную границу и сам по себе не утверждает результат конкретного запуска тестов.

## Статус

Статус — проверочный прототип контракта рабочего пакета, символического паспорта эпизода, исторического паспорта репозиторной композиции версии `1`, отдельного локального паспорта универсального fork-подузла, закрытых назначения и паспорта состояния конечной линейной цепочки, однопакетного пишущего исполнителя, CAS-интегратора с двумя ограниченными resolver-классами, локального долговечного fork-подузла, автономного самостоятельного проекта, единой сквозной приёмки этой композиции и восстанавливаемой общей памяти. Сквозной стенд параллельно создаёт два кандидата, сериализует их CAS-интеграцию, типизирует отрицательные исходы, сохраняет неизвестный конфликт как `resolution_required`, восстанавливает fork и проект из свежих клонов и закрывает сбои перед CAS без частичной публикации. Паспорт универсального подузла локально доказывает профиль ребёнка, отдельность роли и полномочий и точную топологию; валидатор исполняет две закрытые схемы паспортов цепочки и сопоставляет документы с неизменяемым доверенным контекстом двухшагового диапазона. Он не собирает этот контекст из Git и host, не создаёт именованного агента, реальные host-задачи и FIFO-билеты, не выполняет многокоммитную передачу и не интегрирует диапазон. Схема и reducer версии 4 задают реализуемую границу доказательного выбора, конечного бюджета, ожидающего перехода, остановки и возобновления. Эти слои не являются универсальным планировщиком, resolver или живым host-диспетчером, не создают реальный проект, внешний remote или удалённую публикацию и не доказывают отсутствие скрытого чтения, независимость моделей, готовность всей долговременной памяти FUM или внутреннего живого многоагентного runtime.

## Источники требований

- [текущий запрос 2026-08-12 09:11:46 MSK — Закрепить паспорт делегирования конечной цепочки карточек](../../Журнал/2026-08-12_09-11-46_MSK_закрепить-паспорт-делегирования-конечной-цепочки-карточек/запрос.md)
- [исходный запрос 2026-08-05 00:37:53 MSK — Провести автономную сквозную приёмку репозиторной композиции](../../Журнал/2026-08-05_00-37-53_MSK_провести-автономную-сквозную-приёмку-репозиторной-композиции/запрос.md)
- [исходный запрос 2026-08-04 17:51:27 MSK — Перевести проекты на репозитории submodule с собственными очередями](../../Журнал/2026-08-04_17-51-27_MSK_перевести-проекты-на-репозитории-submodule-с-собственными-очередями/запрос.md)
- [исходный запрос 2026-08-04 09:38:47 MSK — Подключить долговечный fork-подузел и передачу вверх](../../Журнал/2026-08-04_09-38-47_MSK_подключить-долговечный-fork-подузел-и-передачу-вверх/запрос.md)
- [исходный запрос 2026-08-04 02:55:45 MSK — Добавить ограниченное автоматическое разрешение Git-конфликтов](../../Журнал/2026-08-04_02-55-45_MSK_добавить-ограниченное-автоматическое-разрешение-Git-конфликтов/запрос.md)
- [исходный запрос 2026-08-03 21:37:49 MSK — Добавить CAS-интеграцию бесконфликтных коммитов](../../Журнал/2026-08-03_21-37-49_MSK_добавить-CAS-интеграцию-бесконфликтных-коммитов/запрос.md)
- [исходный запрос 2026-08-03 18:46:53 MSK — Добавить изолированный пишущий подузел и кандидатный commit](../../Журнал/2026-08-03_18-46-53_MSK_добавить-изолированный-пишущий-подузел-и-кандидатный-commit/запрос.md)
- [исходный запрос 2026-08-03 08:48:44 MSK — Закрепить топологию и паспорт репозиторной композиции FUM](../../Журнал/2026-08-03_08-48-44_MSK_закрепить-топологию-и-паспорт-репозиторной-композиции-FUM/запрос.md)
- [исходный запрос 2026-08-02 21:01:15 MSK — Возобновить распределённый прогон из памяти без скрытого контекста](../../Журнал/2026-08-02_21-01-15_MSK_возобновить-распределённый-прогон-из-памяти-без-скрытого-контекста/запрос.md)
- [исходный запрос 2026-08-02 15:36:30 MSK — Провести живой распределённый прогон Codex и сохранить передачу](../../Журнал/2026-08-02_15-36-30_MSK_провести-живой-распределённый-прогон-Codex-и-сохранить-передачу/запрос.md)
- [исходный запрос 2026-08-02 13:26:18 MSK — Провести автономную приёмку распределённого мыслительного эпизода](../../Журнал/2026-08-02_13-26-18_MSK_провести-автономную-приёмку-распределённого-мыслительного-эпизода/запрос.md)
- [исходный запрос 2026-08-02 09:36:50 MSK — Добавить выбор, бюджеты и условие остановки эпизода](../../Журнал/2026-08-02_09-36-50_MSK_добавить-выбор-бюджеты-и-условие-остановки-эпизода/запрос.md)
- [исходный запрос 2026-08-01 23:00:38 MSK — Добавить восстанавливаемую общую память распределённого эпизода](../../Журнал/2026-08-01_23-00-38_MSK_добавить-восстанавливаемую-общую-память-распределённого-эпизода/запрос.md)
- [исходный запрос 2026-07-26 18:56:09 MSK — Закрепить контракт контекстно посильного рабочего пакета FUM](../../Журнал/2026-07-26_18-56-09_MSK_закрепить-контракт-контекстно-посильного-рабочего-пакета-FUM/запрос.md)
- [исходный запрос 2026-07-27 17:15:27 MSK — Закрепить паспорт распределённого мыслительного эпизода FUM](../../Журнал/2026-07-27_17-15-27_MSK_закрепить-паспорт-распределённого-мыслительного-эпизода-FUM/запрос.md)
- [исходный запрос 2026-07-25 11:56:07 MSK — Закрепить контекстно ограниченную многоагентную реализацию FUM](../../Журнал/2026-07-25_11-56-07_MSK_закрепить-контекстно-ограниченную-многоагентную-реализацию-FUM/запрос.md)
- [FUM-STEP-0080 — выбор, бюджеты и условие остановки](../../Планирование/карточки-шагов/✅-FUM-STEP-0080-добавить-выбор-бюджеты-и-условие-остановки-эпизода.md)
- [FUM-STEP-0081 — автономная приёмка распределённого мыслительного эпизода](../../Планирование/карточки-шагов/✅-FUM-STEP-0081-провести-автономную-приёмку-распределённого-мыслительного-эпизода.md)
- [FUM-STEP-0082 — живой распределённый прогон Codex](../../Планирование/карточки-шагов/✅-FUM-STEP-0082-провести-живой-распределённый-прогон-Codex-и-сохранить-передачу.md)
- [требование об автономном модельном продолжении при ожидании подтверждения](../../Требования/🟡-автономное-модельное-продолжение-при-ожидании-подтверждения.md)

## Опорные материалы

- [Отчёт о живом распределённом прогоне Codex](Живые-прогоны/2026-08-02_15-36-30_MSK/Отчёт.md)
- [Контекстно посильные исполняемые шаги](../../Требования/🚧-контекстно-посильные-исполняемые-шаги.md)
- [Карточка шага](../../Глоссарий/карточка-шага.md)
- [Паспорт документационного прототипа и первого коробочного среза](../../Документация/36-паспорт-документационного-прототипа-и-первого-коробочного-среза.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-12 11:28:35 MSK -->
<!-- content-sha256: sha256:8b12a09564643fcc22eb96d276aa25fb05928231a8274bfa2db75266afdf5722 -->
<!-- FUM-MD-RECENCY:END -->
