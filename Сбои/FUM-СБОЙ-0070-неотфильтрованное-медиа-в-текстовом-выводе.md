+++
"версия_схемы" = 1
"идентификатор_сбоя" = "FUM-СБОЙ-0070"
"статус" = "устранена"
+++
# Вывод содержимого медиа без отбора текстовых частей

## Наблюдаемый сбой

В read-only-разборе выводился полный объект сообщения или массив содержимого без отбора текстовых частей, поэтому в текстовый ответ инструмента попало закодированное изображение и вывод был обрезан.

## Граница повторения

Фильтр диагностического вывода вызывающего сценария. Это не изменение исходного пользовательского материала, не дефект классификации 0177 и не механизм угадывания путей 0009. Лимит max_output_tokens не заменяет фильтрацию до сериализации.

## Проявления

### FUM-СБОЙ-0070/ПРОЯВЛЕНИЕ-0001

Корень 0201: `print(json.dumps(d['сообщения'][-1]))` привёл к chunk 7f98e6 с original_token_count 712072. Последующий сценарий ДНК выбирал конкретные поля и только содержимое с type=input_text.

### FUM-СБОЙ-0070/ПРОЯВЛЕНИЕ-0002

Координатор FUMA: chunk 0b6a07 содержал original_token_count 359692. Исправленный фильтр в call_jnYEHXx65xlnNQJjEmSr1lD4 дал chunk e8c9b8, 648 токенов, четыре последних человеческих текста.

## Ожидание и классификация

Для восстановления текстового контекста выводятся только нужные метаданные и текстовые части. Сырой массив с медиа не должен попадать в диагностическую текстовую выдачу.

## Механизм и системное устранение

Применён положительный перечень полей и отбор content.type=input_text до json.dumps. Исходный JSONL и вложения сохранены; код 0177 не менялся. Сырые медиа в карточку и её свидетельства не копируются.

Общая автоматическая профилактика повторения не заявляется.

## Связанные шаги

Точное основание актуализации существующего [FUM-STEP-0165](../Планирование/карточки-шагов/🟡-FUM-STEP-0165-собирать-рабочий-контекст-задачи.md) — `FUM-СБОЙ-0070/ПРОЯВЛЕНИЕ-0002`; зеркальная запись сохранена в источниках шага. Это уточнение происхождения проверенного ограниченного восстановления.

Атрибутированный материал для существующего [рабочего контекста FUM-STEP-0165](../Планирование/карточки-шагов/🟡-FUM-STEP-0165-собирать-рабочий-контекст-задачи.md); нового поручения, реализации или нового STEP не создаёт. Направление диагностики 0009 не поглощает этот иной механизм.

## Критерии закрытия

Каждое из двух проявлений имеет адресный исправленный вызов, который выбирает текстовые части и сохраняет нужные сообщения без сериализации медиа.

## Подтверждение устранения

У корня 0201 call_spdW6SiS1b1esiIaRVhaKLbm явно фильтрует type=input_text; итог 12acd8 успешен. У координатора исправленный e8c9b8 имеет код 0 оболочки и 648 токенов; вложенный остаток имеет штатный код 3, который не выдан за завершение FUMA. Это два ограниченных восстановления, не созданная системная автоматизация.

## Источники

[Текущая регистрация и происхождение](../Журнал/2026-09-11_09-36-55_MSK_сохранить-оставшуюся-диагностику-приёма/запрос.md).

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-11 09:58:11 MSK -->
<!-- content-sha256: sha256:3e0524adeb9907aea2047ebea63b7027cf18c4e2b364270bab07cde0ccc44188 -->
<!-- FUM-MD-RECENCY:END -->
