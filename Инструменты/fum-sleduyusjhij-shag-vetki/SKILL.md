---
name: fum-sleduyusjhij-shag-vetki
description: Проверяет рабочий набор карточек, во время автозапуска вычисляет готовность по точным завершённым зависимостям, детерминированно ранжирует готовый пул по недавней Git-истории и атомарно резервирует точное поколение выбора активной именованной ветки FUM. Используй при создании или обновлении карточек в Планирование/карточки-шагов и селекторов в Планирование/следующие-шаги-веток, при диспетчеризации фоновой задачи Codex, повторной проверке branch_ref, step_id и selection_id, а также при fenced-восстановлении claim.
---

# Следующий шаг Git-ветки

Используй сценарий `scripts/branch-next-step.py` как fail-closed контракт между каноническими карточками шага, веточным рабочим набором и внешним диспетчером Codex. Селектор не дублирует задачи и критерии: он хранит конечный whitelist с точными `card_id`, `card_content_sha256`, режимом диспетчеризации и машинными зависимостями. `validate`, `show` и `claim` заново вычисляют runtime-готовность, а `show` выбирает один вычисленный `ready` в момент запуска. Явные `paused` и `blocked` остаются наблюдаемыми в том же наборе и никогда автоматически не открываются. Список активных задач Codex проверяется только штатным host-инструментом и не выводится из Git, процессов или приватных файлов среды.

## Формат карточки и рабочего набора

Карточка в `Планирование/карточки-шагов/` имеет имя `<эмодзи>-FUM-STEP-NNNN-<краткое-название>.md` и exact TOML-блок с `schema_version = 1`, неизменяемым `card_id` вида `FUM-STEP-NNNN` и статусом `active`, `completed`, `absorbed` или `withdrawn`. Имя перекрёстно проверяется: `🟡`, `✅`, `🧩` и `🗑️` соответствуют этим четырём статусам, номер совпадает с `card_id`, непустое описание состоит из Unicode-букв и цифр с одиночными дефисами, а полное имя не превышает 255 байт UTF-8. Каталог карточек плоский, и только его точный корневой `README.md` не разбирается как карточка. Активная карточка содержит один H1 и непустые разделы `Задача`, `Почему сейчас`, `Критерии завершения` и `Источники`; каждый критерий и источник задаются пунктами `- ...`. Исторические статусы вместо причины и критериев требуют непустой `Результат`.

Селектор в `Планирование/следующие-шаги-веток/` имеет `schema_version = 5`, обязательные `branch_ref`, `state`, `project_path` и массив `candidates`. Состояние `open` требует хотя бы одного кандидата, `done` — пустого массива. Каждый кандидат хранит собственные версионный claim-fence `step_id`, `dispatch = automatic | paused | blocked`, `card_id` активной карточки, `card_content_sha256` её полного содержания без конечного `FUM-MD-RECENCY` и массив `requires_completed_card_ids`; дайджест записывается как `sha256:<64 hex>`. Для явных `paused` и `blocked` также обязателен непустой `resume_condition`, а для `automatic` это поле запрещено.

В одном открытом наборе разрешено любое число кандидатов каждого режима. `automatic` является предварительной долговечной аттестацией, что карточка безопасна, полномочна и контекстно посильна при выполнении перечисленных машинных зависимостей. Пустой `requires_completed_card_ids` даёт runtime-`ready` без карточечных предпосылок; непустой ALL-of даёт `ready` только когда каждая точная карточка имеет жизненный статус `completed`, а при `active`, `absorbed` или `withdrawn` кандидат вычисляется как `paused`. Явные `paused` и `blocked` не вычисляются. Свободный `resume_condition`, ссылки `Источники`, время, сеть, переменные среды, наличие секрета и вывод модели не являются предикатами готовности.

`card_id` и `step_id` внутри набора уникальны. Массив зависимостей отклоняет неизвестные карточки, повторы, самозависимость и циклы. Все кандидаты, включая неготовых и проигравших выбор, полностью проверяются: отсутствующая или историческая целевая карточка, несовпадающий хэш, неизвестное поле, небезопасный готовый prompt-payload либо пустое условие возобновления закрывают весь запуск. Селектор не содержит разделов `Задача` и `Критерии завершения`. Любая смена карточки, её пути, дайджеста, `dispatch`, массива зависимостей или условия возобновления требует нового `step_id`; чистое переименование при прежнем хэше тоже выпускает новое поколение, потому что `card_path` входит в prompt создаваемой задачи. Изменение только наблюдаемого статуса обязательной карточки сохраняет `step_id`, но меняет вычисленный пул и `selection.id`.

Контекст кандидата строится только из дедуплицированных локальных Markdown-ссылок его раздела `Источники`, нормализованных относительно каталога карточки с точным регистром пути. Внешние URL не участвуют. Из свидетельств исключаются корневая `.obsidian/`, корневой `README.md`, индексы и реестры планирования, веточные селекторы и собственная карточка кандидата: механическое обслуживание этих файлов не должно создавать содержательную близость.

## Проверить активную ветку

Структурно проверить все записи, наличие ровно одного совпадения для активной ветки и вычисленный runtime-пул, не читая историю и не выбирая победителя:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  validate --repo-root . --json
```

Выбрать готовый шаг на точной текущей вершине ветки:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  show --repo-root . --json
```

Успешный `show` возвращает `record_path` селектора, `card_id`, `card_path`, проверенный `card_content_sha256`, объявленный `dispatch`, точные `requires_completed_card_ids`, пустой для победителя `unmet_required_card_ids`, заголовок, задачу и критерии выбранного runtime-`ready`, а также `project_path` и объект `selection`. До выбора весь payload каждого вычисленного `ready` рекурсивно проверяется как динамическая часть дочернего prompt: POSIX-пути, Windows drive, UNC, `file://`, home-expansion и переменные домашнего каталога запрещены, а обычные HTTPS URL не считаются локальными путями. Ошибка называет только поле и тип находки, не повторяя сам литерал. Та же проверка выполняется до записи claim, поэтому небезопасный кандидат не может проиграть ранжирование и скрыто оставить служебную ссылку. Если готового кандидата нет, ответ `not_ready` возвращает сводку рабочего набора с вычисленными статусами, незавершёнными зависимостями и условиями явного возобновления.

Команда `validate` при доказанном структурном контракте всегда возвращает код `0`, `state=valid`, общее число кандидатов и вычисленные `ready_count`, `paused_count`, `blocked_count`, в том числе нулевой готовый пул. Для `show` код `0` означает наличие хотя бы одного runtime-`ready`, а код `3` — корректный открытый набор без готового кандидата либо `done`; код `2` для обеих команд означает недоказанный контракт. Detached HEAD, отсутствие селектора или карточки, дубликат `branch_ref`, `card_id` или `step_id`, неверный ref, исторический статус целевой карточки, несовпадающий дайджест, неизвестная обязательная карточка, повтор, self-edge, цикл, пустая задача, отсутствие критериев, проекта или ожидаемого паспорта ветки закрывают запуск. Любая невалидная карточка или запись кандидата, даже отложенная или невыбранная, закрывает общий `validate`.

Политика `dynamic-readiness-source-history-first-parent-v2` сначала вычисляет готовность всего whitelist, а затем рассматривает не более 16 коммитов first-parent от точного `head` только для runtime-`ready`. При единственном готовом кандидате история не читается, причина равна `only_ready`. При нескольких кандидатах сначала предпочитается связь с изменённой завершённой или поглощённой карточкой-источником (`completed_step_source`), затем изменение иного источника (`changed_source`); внутри класса выигрывает меньшая дистанция от `head`, затем большее число совпавших путей, затем лексикографическая пара `card_id`, `step_id`. Если свидетельств нет, применяется устойчивый порядок `card_id`, `step_id` с причиной `stable_fallback`. Сообщения, авторы и время коммитов, сходство имён каталогов, embeddings, свободный текст и накопленные веса не используются.

Объект `selection` содержит точные `id`, `policy`, `head`, `ready_count`, `reason`, `commit`, `distance` и отсортированные `matched_paths`. Для `only_ready` и `stable_fallback` поля `commit` и `distance` равны `null`, а `matched_paths` пуст. `selection.id` имеет вид `sha256:<64 hex>` и хэширует канонический снимок всех влияющих входов: политику, точный `head`, содержимое рабочего набора, объявление и вычисленный статус каждого кандидата, путь и хэш его собственной карточки, условие явного возобновления, незавершённые зависимости, точные `card_id`, пути, статусы и хэши всех обязательных карточек, полный runtime-пул `ready`, выбранное поколение и свидетельство ранжирования. Любая смена вершины, результата зависимости, пула, карточки или результата ранжирования создаёт новую идентичность даже при прежнем `step_id` победителя.

## Зарезервировать запуск

После двух проверок штатного списка задач Codex и проверки сохранённого проекта зарезервировать только уже прочитанную пару:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  claim \
  --repo-root . \
  --expected-branch-ref refs/heads/<ветка> \
  --expected-step-id <step_id> \
  --expected-selection-id sha256:<идентичность-выбора> \
  --lease-id <UUID-логической-попытки> \
  --json
```

Каждый новый heartbeat-тик постоянной диспетчерской задачи начинает отдельную логическую попытку и не наследует из прежних тиков `lease_id` либо признак вызванного `create_thread`. История сообщений остаётся свидетельством, но не оперативным блокирующим состоянием: успешный вызов `create_thread` в прежнем тике не запрещает новый запуск после смены `selection.id`. Защиту прежнего поколения определяет актуальный fenced claim.

До первого мутирующего вызова текущего тика клиент создаёт свежий канонический UUID одной логической попытки, например командой `python3 -I -c 'import uuid; print(uuid.uuid4())'`, и передаёт его как `--lease-id`. Код `0`, `state=claimed`, точное совпадение `selection_id` и `lease_id` разрешают один запуск: `ownership=new` означает первую запись, `ownership=existing` — идемпотентное подтверждение точного повтора той же попытки. Повтор атомарно подтверждает как неизменность `branch_ref == selection_head`, так и неизменность служебной ссылки и отклоняет противоречивые сохранённые `step_id` или `selection_head`. Если ответ `claim` потерян или неоднозначен до первого вызова `create_thread`, клиент повторяет ту же команду с теми же `branch_ref`, `step_id`, `selection_id` и `lease_id`; новый UUID в этой ситуации создавать нельзя.

Код `4` и `state=already_claimed` означают, что точное текущее поколение выбора уже принадлежит другой логической попытке, не раскрывают её `lease_id` и запрещают запуск. В пределах одной ветки один `lease_id` нельзя переиспользовать для нового `selection_id`; новое поколение выбора требует свежего UUID. После первого вызова `create_thread` в пределах текущего heartbeat-тика повторять `claim` или создание задачи нельзя: неоднозначный результат создания по-прежнему сохраняет claim до отдельного внешне подтверждённого fenced-восстановления. Следующий тик заново проходит инвентаризацию и `show`; новый `selection_id` с новым `lease_id` атомарно заменяет прежний claim ветки, в том числе когда выбран прежний `step_id` на новой вершине.

Claim схемы `2` хранит `branch_ref`, `step_id`, `selection_id`, `selection_head` и `lease_id` вне рабочей копии как канонический JSON blob под служебной ссылкой `refs/fum/worktree-next-step-claims/<sha256-physical-checkout>/<sha256-branch-ref>`. Публикация, замена и подтверждение точного повтора выполняются одной транзакцией `git update-ref --stdin`: она одновременно проверяет точный `branch_ref == selection_head` и compare-and-swap служебной ссылки, используя для повтора одинаковые old/new object ID. Удаление также выполняется только compare-and-swap; символическая служебная ссылка отклоняется, а запись использует `--no-deref`. После проигранного CAS тот же вызов ничего не заменяет: для нового поколения нужны повторный `show` и свежий вызов. Claim схемы `1` безопасно читается в переходный период, но новая запись всегда имеет схему `2`. Git-вызовы игнорируют replace-объекты, optional locks и все унаследованные `GIT_*`, включая trace-файлы и перенаправления. Claim не имеет TTL, а повреждённый, расширенный неизвестными полями или противоречащий текущему selection blob автоматически не заменяется. Файлы прежней POSIX-реализации под `.git/fum-sleduyusjhij-shag-vetki/` больше не авторитетны, не импортируются и не требуют ручной миграции. Этот слой не заменяет [fum-ocheredj-zadach-git-vetki](../fum-ocheredj-zadach-git-vetki/SKILL.md), штатный список задач или проверку идентичности внутри созданной задачи.

## Выполнить созданную задачу

Созданная корневая задача первым видимым сообщением выводит строку `Автозапуск назначил карточку <card_id> — <title>; ожидаю допуск FIFO.` с точными машинно проверенными значениями из дочернего prompt. Уведомление отправляется до запуска `join`, показывает назначение даже во время последующего ожидания, но не означает начала работы. Во время неизменного ожидания задача не отправляет дополнительных сообщений.

Первым инструментальным действием задача проходит общую FIFO-очередь checkout и дожидается состояния `admitted`. Затем перед любыми изменениями она повторяет `show` со всеми ожиданиями:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  show \
  --repo-root . \
  --expected-branch-ref refs/heads/<ветка> \
  --expected-step-id <step_id> \
  --expected-selection-id sha256:<идентичность-выбора> \
  --json
```

После успешной fenced-сверки и до содержательной работы задача ровно один раз выводит `В работу взята карточка <card_id> — <title>.` с повторно подтверждёнными значениями. При mismatch эта формулировка запрещена: задача сообщает `Назначение карточки <card_id> — <title> не подтверждено; работа не начата.`, выполняет описанный ниже `finish-clean` и завершается без записи.

После успешной сверки полностью прочитать возвращённые `record_path`, `card_path` и `project_path`. Рабочий набор, карточка шага и паспорт проекта являются обязательными входами: соблюдать их источники, границы действий, доступа, публикации и проверки.

До содержательных изменений выполнить контекстный preflight контекстно ограниченной карточки по контракту `AGENTS.md` и [карточки шага](../../Глоссарий/карточка-шага.md). Учесть обязательные накладные расходы: чтение правил, навыков и источников, сохранение происхождения сессии, целевые проверки, recency, полный smoke-check и атомарную передачу. Если карточка с высокой вероятностью укладывается в одно свежее контекстное окно, выполнить её задачу и критерии. Если нет, не пытаться скрыть широту параллельным самодиалогом экземпляров одной модели: ограничить сессию устойчивой декомпозицией, перевести исходную карточку в `absorbed` с `Результатом` и ссылками на дочерние карточки и включить как `automatic` только первого кандидата, прошедшего тот же preflight, с точным массивом машинных зависимостей. Не выдавать декомпозицию за завершение исходной реализации.

До атомарного `commit` общей очереди перевести выполненную карточку в исторический статус с `Результатом`, удалить её поколение из рабочего набора и сохранить остальные корректные `automatic`/`paused`/`blocked`-кандидаты. В whitelist `automatic` можно добавлять все независимо безопасные, полномочные и контекстно ограниченные карточки со свежими `card_content_sha256`, `step_id` и точными `requires_completed_card_ids`; вычислять их текущий `ready` и ранжировать при коммите не нужно. Если вычисленного готового кандидата нет, оставить открытый набор с автоматическими ожиданиями и явными паузами или блокировками; `done` допустим лишь для пустого набора. Не оставлять выполненное поколение для повторного запуска и не позволять одному неготовому кандидату скрывать другой runtime-`ready`.

Дочерний промпт автоматически создаваемой задачи использует только пути относительно корня выбранного проекта: `AGENTS.md`, пути обоих навыков и точные значения `record_path`, `card_path` и `project_path`. Сохранённый абсолютный корень клона нужен внешнему heartbeat для выбора проекта и рабочего каталога, но в дочерний промпт не переносится. Любое запрещённое значение в `title`, `task`, `criteria`, путях и остальных полях ready-payload делает `show` и `claim` невалидными до любой записи claim и до `create_thread`; внешнему heartbeat не нужно эвристически распознавать путь после claim.

Если fenced `show` после допуска возвращает mismatch до первой записи, созданная задача не меняет запись шага и не оставляет владельца очереди: она дожидается отсутствия всех возможных писателей, выполняет документированный `finish-clean` очереди с точными `task_id` и `generation`, после успеха прекращает любые записи и завершает работу.

## Остановить и возобновить диспетчер

Управляй heartbeat через штатную карточку существующей автоматизации в прикреплённой диспетчерской задаче. Пользовательский `Stop` или `Start` не является heartbeat-тиком: это обычный корневой управляющий ход, который до изменения автоматизации входит в FIFO общей рабочей копии и дожидается допуска. Не создавай отдельный флаг паузы, обходной cron или новую автоматизацию. При программном выборе сначала потребуй ровно одну heartbeat-автоматизацию, чья целевая задача совпадает с точным `CODEX_THREAD_ID` диспетчера; ноль или несколько совпадений закрывают изменение. Непрозрачные идентификаторы используй только внутри штатного вызова и не публикуй.

До обновления прочитай и сохрани полный snapshot декларативной карточки автоматизации. Выполни `python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/automation-status-snapshot.py prepare --snapshot <JSON-или-TOML-снимок> --status PAUSED`: helper отклоняет неполный, расширенный неизвестным полем или неоднозначный snapshot, исключает служебные `created_at`, `updated_at` и `version`, нормализует единственный target alias в `targetThreadId`, переводит допустимые optional aliases в схему host-вызова, добавляет точный `mode = update`, меняет только `status` и механически сохраняет byte-exact `id`, `kind`, `name`, `prompt`, `rrule`, target и остальные декларативные поля. В одном orchestration-вызове передай полученный полный update payload штатному обновлению. Не заменяй prompt краткой фразой, не вызывай renderer как скрытую часть штатного переключения и не восстанавливай prompt ручным копированием из памяти или истории. `Stop` запрещает будущие плановые тики, но не отменяет уже выполняющийся тик или созданную им обычную задачу, не снимает claim, не освобождает владельца FIFO и не меняет `dispatch` либо вычисленный статус кандидата. Уже созданная задача продолжает обычную рабочую сессию независимо от статуса heartbeat.

`Start` применяет тот же helper с `--status ACTIVE` и в одном orchestration-вызове меняет только статус полного snapshot. Он не создаёт дубликат, не форсирует немедленный запуск и не обходит две проверки наблюдаемого простоя, `show`, claim или FIFO. Следующий плановый тик проходит весь fail-closed-протокол заново: сохранённый claim другого lease для того же `selection.id` возвращает `already_claimed` и защищает прежний шаг от повтора, а новый `selection.id` со свежим lease атомарно заменяет прежний claim.

После `Stop` или `Start` заново прочитай карточку штатным просмотром и передай исходный и итоговый снимки `automation-status-snapshot.py verify` с ожидаемым статусом. Helper допускает exact-diff только поля `status` и служебного host-поля `updated_at`; пустой декларативный diff разрешён лишь при идемпотентном повторе уже достигнутого статуса. Любое иное отличие, включая сокращённый или заменённый prompt, закрывает подтверждение. У host-обновления нет expected-version/CAS, поэтому один orchestration-вызов и post-view exact-проверка лишь сужают окно гонки и обнаруживают наблюдаемое расхождение, но не являются транзакцией. Неоднозначный ответ не считается успехом: не редактируй локальную конфигурацию вручную и не создавай замену. Claim восстанавливается отдельно по следующему разделу и только после внешнего доказательства, что прежняя созданная задача больше не исполняется и не может возобновить запись.

После завершения всех способных позднее изменить автоматизацию вызовов управляющий ход вызывает через точный HEAD-bootstrap очереди `finish-own-clean --task-id "$CODEX_THREAD_ID" --json`. Команда сама получает generation наблюдаемого владельца, но передаёт очередь только когда владельцем остаётся точный `task_id`, `HEAD` не изменился, checkout чист вне разрешённой корневой `.obsidian/` и staged-изменений нет нигде. Не объявляй `Stop` или `Start` успешным до точного результата `finished_clean`; не подставляй generation, ticket или task id из истории сообщений. Неоднозначное изменение автоматизации всё равно требует после остановки возможных внешних писателей безопасно передать собственное чистое владение, а итог остаётся неуспешным.

Каждый новый `<heartbeat>` сначала штатным снимком задач доказывает точную закреплённую идентичность собственного `CODEX_THREAD_ID`. Новый тик той же задачи является свидетельством, что её прежний пользовательский управляющий ход больше не исполняется. После этого доказательства, но до раннего выхода из-за другой active-задачи, тик read-only-командой `status` проверяет очередь. Если точным владельцем является эта же диспетчерская задача после собственного незавершённого чистого управления, тик вызывает `finish-own-clean --task-id "$CODEX_THREAD_ID" --json`; наличие ожидающих последователей не запрещает передачу собственного владельца. После точного `finished_clean` повторный статус разрешает продолжить тик лишь при пустых owner и waiting, а назначенный следующий владелец или любой ожидающий билет завершает дальнейшую диспетчеризацию без claim и новой задачи. Чужой владелец, собственный ожидающий билет без владения, грязная работа, изменившийся `HEAD`, ошибка или неоднозначное состояние запрещают clean-finish и дальнейшую диспетчеризацию; heartbeat не завершает чужого владельца, не очищает дерево и не создаёт новую задачу.

Если необходимый инструментальный вызов завершился доказанным sandbox/permission denial и среда поддерживает штатный запрос полномочий, разрешён один точно повторённый вызов. `cwd`, `argv`, `stdin`, пути, идентификаторы, expected-значения и поколение операции остаются неизменными; меняется только режим запроса разрешения. Без явного permission failure повышение полномочий, альтернативная команда или ручной обход запрещены, а повторный отказ завершает ход fail-closed.

## Восстановить claim

Сначала показать наблюдаемое поколение:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  claim-status --repo-root . --json
```

Только после внешнего подтверждения, что созданная задача больше не исполняется и не может возобновить запись, выполнить compare-and-delete:

```bash
python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/branch-next-step.py \
  release \
  --repo-root . \
  --branch-ref refs/heads/<ветка> \
  --expected-lease-id <наблюдённый-lease_id> \
  --json
```

Код `5` означает отсутствующее или уже сменившееся поколение. Повреждённый claim автоматически не удалять.

## Проверка

Запустить автономные тесты без сети и секретов:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover \
  -s Инструменты/fum-sleduyusjhij-shag-vetki/tests \
  -p 'test_*.py'
```

Набор автономных тестов проверяет exact-форматы имени карточки, её содержания и рабочего набора схемы `5`, вычисление `ready` только из literal `completed`, ложные `active`/`absorbed`/`withdrawn`, неизвестные, повторные, собственные и циклические зависимости, несколько готовых и отложенных кандидатов, выбор по first-parent-истории и устойчивый fallback, уникальность `card_id` и `step_id`, условия возобновления, content-hash fence, смену `selection.id` при изменении вычисленного пула или пути неготовой карточки, одновременный и межпоколенческий claim, атомарное подтверждение точного повтора, отказ противоречивому claim, exact-lease release, повреждённые и символические refs, dirty checkout/index, Unicode-ветку, SHA-256, очистку Git-среды и trace-файлов, все строковые поля ready-payload и отказ без claim для POSIX, Windows drive, UNC, `file://`, home-expansion и home-variable форм при положительном HTTPS-контроле, а также отсутствие POSIX-примитивов.

## Граница автоматизации

Плановый heartbeat постоянной диспетчерской задачи не является новым рабочим билетом FIFO-очереди и не вызывает `join`. Пользовательский управляющий ход в той же задаче является обычной корневой задачей и входит в очередь. Сам тик не меняет checkout, индекс, ветку или Git-историю; кроме fenced claim/release ему разрешена только узкая передача уже существующего собственного чистого владения такого завершившегося управляющего хода через `finish-own-clean` после доказательства закреплённой идентичности. Это восстановление не создаёт тикету нового владельца и не разрешает завершать чужую или грязную работу. Созданная диспетчером обычная задача сама входит в очередь до работы.

Локальный сценарий требует Python 3, Git и именованную Git-ветку, но не зависит от POSIX-блокировок, сигналов или hooks. Служебная ссылка ограничена идентичностью физического checkout; сценарий не координирует отдельные клоны и не доказывает глобальный простой Codex.

Внешний heartbeat закрепляется в боковом меню и должен дважды запрашивать снимок `codex_app.list_threads` с максимальным поддерживаемым `limit = 50`. Каждый ответ `list_threads` и ответ `list_projects` до проверки полей проходит ограниченную транспортную нормализацию. Уже полученный JSON-объект используется напрямую; строка разбирается как полный JSON-текст строго один раз и также обязана дать объект. Массив, `null`, иной тип, ошибка разбора, вложенная строка, рекурсивный разбор, извлечение из Markdown, префикса, суффикса или wrapper-поля закрывают тик до claim и создания задачи. После нормализации каждый снимок задач проверяется как объединение полного массива `pinnedThreads` и ограниченного массива недавних незакреплённых задач `threads`; `limit` относится только к `threads`. Диспетчер требует ровно одно совпадение собственного точного `CODEX_THREAD_ID` в `pinnedThreads`, запрещает его дубликат в `threads`, исключает только эту запись из объединения и считает любой другой наблюдаемый `active` занятостью. Не требуй от собственной записи состояния `active`: host может показывать прикреплённую heartbeat-задачу как `idle` или `notLoaded` во время тика, и эти известные состояния не опровергают точную идентичность диспетчера. При занятости он не публикует claim, не ставит плановую работу в FIFO-очередь и вообще не создаёт задачу. Недоступный host, неизвестное состояние, отсутствие одного из массивов, нарушенное закрепление или ошибка также закрывают запуск; `idle` и `notLoaded` являются известными неактивными состояниями. Контракт не возвращает общее число или признак полноты массива `threads`, поэтому объединённый снимок не доказывает отсутствие более старой незакреплённой активной задачи за его пределами. Ранний `validate` выполняется до поиска проекта, но история читается и победитель выбирается командой `show` только после второй инвентаризации, непосредственно перед свежим lease и `claim`. Все потенциально долгие read-only host-вызовы завершаются до claim, чтобы тайм-аут не оставлял резервацию. Каждый входной `<heartbeat>` начинает новую логическую попытку без переноса `lease_id` и признака вызванного `create_thread` из истории постоянной задачи. Потерянный ответ самого `claim` до первого `create_thread` восстанавливается только повтором той же логической попытки. После первого вызова `create_thread` повтор claim или создания запрещён только внутри текущего тика, а claim освобождается только если штатный ответ явно подтверждает, что задача не создана. Ошибка, тайм-аут или неоднозначный ответ сохраняют claim для обычного fenced-восстановления после внешней проверки. Следующий тик заново проходит весь протокол: новый `selection.id` получает свежий lease и атомарно заменяет прежний claim, а неизменившийся выбор останавливает штатный `already_claimed`. Между последней инвентаризацией и созданием задачи остаётся неатомарное окно; FIFO-очередь созданной задачи и повторная проверка `branch_ref`/`step_id`/`selection_id` уменьшают риск, но не превращают host-интерфейсы в транзакцию.

При регистрации диспетчера в новом форке или клоне не копируй [шаблон heartbeat](references/heartbeat-prompt.md) вручную. Из корня сохранённого проекта выполни `python3 Инструменты/fum-sleduyusjhij-shag-vetki/scripts/render-heartbeat-prompt.py --repo-root .` и передай stdout в автоматизацию без сокращения или редактирования. Renderer требует ровно один полный fenced-блок под точным разделом `## Шаблон`, заменяет все точные `<КОРЕНЬ_КЛОНА>` нормализованным абсолютным корнем существующего проекта и закрывается при неоднозначной структуре, отсутствии подстановки либо остаточном placeholder. Абсолютный корень используется только внешним диспетчером для выбора проекта и рабочего каталога и не копируется в промпт автоматически создаваемой задачи.

Renderer также является источником для отдельного, явно запрошенного repair уже повреждённого live prompt. Штатный `Stop`/`Start` renderer не вызывает и не скрывает расхождение: если прочитанный snapshot prompt уже не совпадает с канонически отрисованным значением, переключение статуса закрывается. Первичная in-place-попытка передаёт существующей записи полный snapshot с заменённым только prompt и принимает успех лишь по exact-diff, допускающему изменение prompt и служебного `updated_at`. Если host изменил только `updated_at`, но оставил prompt прежним, тот же update не повторяется и успех не объявляется.

Когда host запрещает второй heartbeat для той же задачи, явно разрешённый пользователем ремонт может заменить только эту повреждённую запись внутри одного допущенного управляющего хода. Заранее сохраняется полный snapshot; любое неизвестное поле закрывает замену до удаления; наличие другой точной `active`-задачи должно закрывать каждый плановый тик до claim. Старая запись удаляется, а канонический prompt создаётся под новой уникальной отображаемой латинской формой из `display`, точно полученной из русского `source` закреплённым LinguisticKit, с теми же декларативными полями. Закрытый post-view-whitelist разрешает отличаться от snapshot только полям `id`, `name`, `prompt`, `created_at` и `updated_at`: prompt обязан точно совпасть с renderer, а новая host-идентичность, имя и отметки времени имеют ожидаемые новые значения. `version`, `kind`, target, расписание, исходный status и каждое присутствующее известное опциональное декларативное поле, включая `destination` и `notificationPolicy`, сохраняются exact; потеря либо добавление поля закрывает успех. Повторное чтение также обязано доказать ровно один heartbeat для target.

Расхождение требует удалить кандидата и выполнить обязательную попытку восстановления исходной декларативной конфигурации из сохранённого snapshot, но отсутствие expected-version/CAS и воспроизводимой host-идентичности не гарантирует её успех. Восстановление подтверждается только повторным exact-view; иначе результат считается аварийным неуспехом и явно сообщается пользователю. До такого подтверждения нельзя утверждать, что исходный snapshot восстановлен. Эта замена не встраивается в плановый heartbeat и не меняет прикреплённую диспетчерскую задачу либо её историю. Новую установку без прежней записи создавай `PAUSED`, закрепляй её отдельную диспетчерскую задачу, проверяй единственное присутствие в `pinnedThreads` и активируй только после чистого коммита установки.

Repair существующей `ACTIVE`-автоматизации сохраняет точный status до успешной передачи FIFO. Требование изменить только prompt в одном orchestration-вызове относится к первичной in-place-попытке; явно разрешённый replacement-путь подчиняется отдельному закрытому whitelist выше. Текущая active/FIFO-задача закрывает плановый тик. Repair выполняется до commit+handoff этой допущенной задачи. После commit прежняя задача больше не изменяет автоматизацию: единственным допустимым внешним действием остаётся документированный publish.

## Источники требований

- [исходный запрос 2026-07-30 07:55:11 MSK — Исправить транспортный формат автозапуска](../../Запросы/2026-07-30_07-55-11_MSK_исправить-транспортный-формат-автозапуска.md)
- [исходный запрос 2026-07-29 18:39:04 MSK — Исправить возобновление автозапуска следующих шагов](../../Запросы/2026-07-29_18-39-04_MSK_исправить-возобновление-автозапуска-следующих-шагов.md)
- [исходный запрос 2026-07-29 09:04:03 MSK — Расширить динамический выбор следующего шага](../../Запросы/2026-07-29_09-04-03_MSK_расширить-динамический-выбор-следующего-шага.md)
- [исходный запрос 2026-07-27 23:52:05 MSK — Исправить межтиковую блокировку автозапуска](../../Запросы/2026-07-27_23-52-05_MSK_исправить-межтиковую-блокировку-автозапуска.md)
- [исходный запрос 2026-07-27 18:28:42 MSK — Выбирать следующий шаг при запуске с учётом истории коммитов](../../Запросы/2026-07-27_18-28-42_MSK_выбирать-следующий-шаг-при-запуске-с-учётом-истории-коммитов.md)
- [исходный запрос 2026-07-25 11:56:07 MSK — Закрепить контекстно ограниченную многоагентную реализацию FUM](../../Запросы/2026-07-25_11-56-07_MSK_закрепить-контекстно-ограниченную-многоагентную-реализацию-FUM.md)
- [исходный запрос 2026-07-24 08:42:34 MSK - Исправить поиск закреплённого heartbeat диспетчера](../../Запросы/2026-07-24_08-42-34_MSK_исправить-поиск-закреплённого-heartbeat-диспетчера.md)
- [исходный запрос 2026-07-24 07:23:50 MSK - Исправить самопроверку heartbeat диспетчера](../../Запросы/2026-07-24_07-23-50_MSK_исправить-самопроверку-heartbeat-диспетчера.md)
- [исходный запрос 2026-07-23 13:40:57 MSK - Выводить текущую карточку в сессии автозапуска](../../Запросы/2026-07-23_13-40-57_MSK_выводить-текущую-карточку-в-сессии-автозапуска.md)
- [исходный запрос 2026-07-23 09:36:31 MSK - Исправить автозапуск и предотвратить повтор ошибки](../../Запросы/2026-07-23_09-36-31_MSK_исправить-автозапуск-и-предотвратить-повтор-ошибки.md)
- [исходный запрос 2026-07-22 13:39:29 MSK - Устранить машинно-локальные пути](../../Запросы/2026-07-22_13-39-29_MSK_устранить-машинно-локальные-пути.md)
- [исходный запрос 2026-07-22 10:59:50 MSK - Управлять автозапуском шагов ветки через Stop Start](../../Запросы/2026-07-22_10-59-50_MSK_управлять-автозапуском-шагов-ветки-через-Stop-Start.md)
- [исходный запрос 2026-07-22 10:31:30 MSK - Запретить абсолютные пути в промптах автозадач](../../Запросы/2026-07-22_10-31-30_MSK_запретить-абсолютные-пути-в-промптах-автозадач.md)
- [исходный запрос 2026-07-22 03:38:35 MSK - Разрешить выполнение доступных карточек шагов](../../Запросы/2026-07-22_03-38-35_MSK_разрешить-выполнение-доступных-карточек-шагов.md)
- [исходный запрос 2026-07-22 02:59:22 MSK - Декомпозировать предложения на карточки шагов](../../Запросы/2026-07-22_02-59-22_MSK_декомпозировать-предложения-на-карточки-шагов.md)
- [исходный запрос 2026-07-20 20:06:04 MSK - Запускать следующие шаги веток](../../Запросы/2026-07-20_20-06-04_MSK_запускать-следующие-шаги-веток.md)
- [исходный запрос 2026-07-21 18:31:35 MSK — Ввести последовательную очередь сессий без hooks](../../Запросы/2026-07-21_18-31-35_MSK_ввести-последовательную-очередь-сессий-без-hooks.md)
- [исходный запрос 2026-07-22 11:48:49 MSK — Оформить карточки шагов описательными именами и эмодзи статусами](../../Запросы/2026-07-22_11-48-49_MSK_оформить-карточки-шагов-описательными-именами-и-эмодзи-статусами.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-07-30 09:20:19 MSK -->
<!-- content-sha256: sha256:e928ce9887aa97f35353acb0701c7a0236970ca7eeed124256a154075a1f9c81 -->
<!-- FUM-MD-RECENCY:END -->
