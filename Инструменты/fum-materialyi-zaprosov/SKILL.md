---
name: fum-materialyi-zaprosov
description: Сохранять устойчивые URL в общей папке Источники, а принадлежащие одному запросу материалы — внутри его папки Журнала. Использовать общий вход fum source archive для HTML-URL, специализированный ChatGPT-share-контур для расшаренных диалогов и канонический протокол публикационной очистки, снимков и ссылок.
---

# FUM Request Materials

Этот навык фиксирует прикрепляемые материалы как часть памяти FUM, сохраняя связь:
исходный запрос -> папка источников -> производная документация или инструмент -> коммит.

## Основной протокол

1. Создай или обнови файл исходного запроса `Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md` по правилам `AGENTS.md`; временной префикс имени папки обязателен, текст запроса сохраняй дословно. При импорте знаний из ChatGPT-диалога выбирай краткое название запроса по содержанию диалога после первичного извлечения или чтения, а не по общему факту импорта из ChatGPT.
2. Для материала с устойчивым URL используй общую каноническую URL-папку `Источники/URL/<scheme>/<host>/<path...>/` от корня репозитория: цепочка каталогов повторяет схему, домен и путь URL. Повторные запросы на тот же URL должны ссылаться на эту же папку, а не создавать копию. Для материала без устойчивого URL используй принадлежащую запросу папку `Журнал/<имя-запроса>/материалы/источники/<описательное-название>/`.
3. Сохраняй источник максимально сыро: URL, HTTP-заголовки, тело ответа, встроенные JSON/потоки приложения, извлечённые сообщения или текст, отчёт об извлечении.
4. Не коммить секреты и локальные служебные данные. Redact `Set-Cookie`, токены, локальные IP/геометаданные запроса, сессионные идентификаторы, служебные request-id распакованных потоков и другие значения, не являющиеся содержанием материала. Каждую редакцию фиксируй в отчёте.
5. В `запрос.md` укажи ссылку на общую URL-папку либо собственную папку материала и, когда полезно, на отчёт об извлечении или основные извлечённые файлы. Если материал влияет на требования, документацию, глоссарий или инструменты, укажи эти ссылки в разделе `## Повлиял на файлы` или отдельном разделе `## Прикрепляемые материалы` файла запроса.
6. Перед коммитом выполни `git status --short` и включи только осмысленные изменения текущей сессии.

## Общий вход для устойчивого HTML-URL

Первый общий пользовательский вход архиватора запускается из корня репозитория:

```bash
./fum source archive "https://example.com/article" \
  --request "Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md"
```

Если корень репозитория добавлен в `PATH`, та же команда вызывается как `fum source archive`. Тонкий корневой вход передаёт URL и файл запроса переносимому модулю `scripts/source_archive.py`. Обычный транспорт использует `curl`, а оркестрация не зависит от транспорта: сначала в соседнем staging-каталоге полностью собирается и проверяется снимок, затем он устанавливается атомарно и только после этого файл запроса получает ссылки.

Для HTML-URL общий снимок содержит:

- `source-url.txt` — исходный устойчивый URL;
- `response.headers.txt` — HTTP-заголовки с удалёнными значениями `Set-Cookie`;
- `response.body.html` — тело HTML-ответа в исходных байтах транспорта;
- `extracted-text.md` — видимый текст без `script`, `style`, `template` и `svg`;
- `structured-data.json` — распознанные JSON-LD-данные, если они присутствуют и корректны;
- `source-index.md` — человекочитаемый вход в снимок;
- `extraction-report.md` — транспорт, результат извлечения, редакции и ограничения;
- `snapshot-manifest.json` — точный перечень всех файлов установленного снимка.

Ответы с точным MIME `text/plain` или `application/json` сохраняются как дословные данные: тело не проходит HTML-очистку, а извлечённое представление помещается в ограду `text`, длиннее любой последовательности обратных кавычек источника. Историческое имя `response.body.html` сохраняется ради совместимости формата, фактический MIME остаётся в заголовках. Это позволяет архивировать preset, манифест и лицензию без превращения их относительных ссылок в ссылки FUM. Публикационная проверка выбранного публичного текстового источника остаётся обязанностью вызывающего слоя. Источник служит свидетельством планирования и не заменяет регистрацию Git-зависимости.

Переменные `FUM_SOURCE_ARCHIVE_TEST_FIXTURE_DIR` и `FUM_SOURCE_ARCHIVE_TEST_FAILPOINT` являются только автономными тестовыми точками подмены. Первая направляет тот же CLI на локальные `response.headers.txt` и `response.body.html`, вторая допускает только поздний сбой `after-build` после полной проверки staging и до установки. Они не являются альтернативным пользовательским протоколом и не нужны при обычном архивировании.

## Целостность повторного снимка

`archive-chatgpt-share.py` собирает каждый новый снимок в скрытом соседнем staging-каталоге на той же файловой системе, не записывая промежуточные результаты в каноническую URL-папку. Перед установкой скрипт создаёт `snapshot-manifest.json` схемы `fum.request-materials.snapshot-manifest.v1` и проверяет точное равенство отсортированного списка `managed_files` фактическому набору файлов staging.

Первый снимок устанавливается атомарным переименованием каталога. Повторный снимок заменяет существующий каталог одним обменом директорий: `RENAME_SWAP` на macOS или `RENAME_EXCHANGE` на Linux. Если системный вызов или файловая система не поддерживает атомарный обмен, повтор закрывается с ошибкой до изменения канонического снимка; двухшаговая замена через временно отсутствующий канонический путь не допускается.

Существующий снимок проверяется до обмена. Самостоятельные вложенные URL с собственными `source-url.txt` и манифестом имеют отдельный инвентарь; при замене родителя они проверяются и копируются в staging без изменения байтов. Повреждённый вложенный снимок или конфликт назначения закрывает повтор отказом. PDF определяется по сигнатуре и Content-Type, даже если расширения в адресе нет: HTML-вход отказывает, нужен отдельный захват PDF и постраничное извлечение.

До извлечения HTML редактируются известные CSRF/XSRF-поля, nonce и диагностические значения запроса. `timestamp` очищается только в распознанном контейнере CAPTCHA с формой по корневому web-пути `checkcaptcha`, маркером `CheckboxCaptcha` и парным `unique-key`. Нетронутые сырые байты, в том числе не-UTF-8, сохраняются обратимым `surrogateescape`; текстовое представление по-прежнему может содержать замену неподдержанного символа. Список известных заголовков и редакций выводится в отчёте каждого нового HTML-снимка. Это ограниченная очистка, смысловой аудит перед публикацией остаётся обязательным.

Адресный [набор регрессий источников поддержки](tests/test_очистка_источников_поддержки.py) использует только синтетические служебные значения. [Профиль очистки](tests/профиль_очистки_поддержки.py) с `--выход <путь.json>` повторяет обработку сохранённого набора без сети; PDF явно исключаются из границы HTML.

После успешного обмена новый канонический каталог уже является зафиксированным целым снимком. Ссылка добавляется в файл запроса через временный sibling и атомарный `os.replace`, поэтому I/O-сбой не должен усекать прежний текст запроса. Невозможность добавить ссылку выводится как отдельное предупреждение `request_file_linked error` и не выдаётся за неуспешное архивирование уже установленного снимка. Очистка прежнего снимка из staging выполняется best-effort; если скрытый соседний `.staging-*` остался после commit, скрипт явно предупреждает об этом. Такой каталог не входит в новый манифест, не считается каноническим источником и должен быть отдельно проверен как локальный временный остаток перед коммитом.

## Проверки

Оба архиватора используют общий `source_path_segment`: `.git`, `.gitmodules`, `.gitattributes` и распознаваемые Git регистровые/NTFS-псевдонимы этих имён получают прежний суффикс SHA-256. Обычные `.github`, `.gitignore` и `.gitmodules.txt` не меняются. Это узкая граница имён дерева Git, а не общий фильтр имён Windows. Исходный URL сохраняется в `source-url.txt`; совпадение вычисленного пути другого URL по-прежнему закрывает повтор до перезаписи.

Для уже сохранённого плоского снимка в таком **конечном** каталоге есть вспомогательная команда `scripts/перенести_снимки.py`. Входной JSON содержит `каталоги` (точные старые пути), `markdown` (полный выбранный набор входящих Markdown-ссылателей) и `манифесты` (JSON с локальными полями `путь`). Все пути относительны корню. Команда строит новый путь штатным генератором по `source-url.txt`, проверяет manifest и хэши каждого файла, запрещает коллизии, symlink и перенос промежуточного сегмента. Снимки переносятся побайтно; в производных файлах меняются только разрешённые цели Markdown-ссылок вне дословного запроса и поля `путь`. Произвольные строковые ссылки, wiki-ссылки и другие JSON-контракты не поддержаны; полноту явного набора подтверждает вызывающая сессия. Индекс Markdown пересобирается штатной автоматизацией recency.

```text
python3 -B Инструменты/fum-materialyi-zaprosov/scripts/перенести_снимки.py --корень-репозитория . --вход <вход.json> > <план.json>
python3 -B Инструменты/fum-materialyi-zaprosov/scripts/перенести_снимки.py --корень-репозитория . --вход <вход.json> --применить-план <план.json>
```

Сначала прочитайте полный план. Применение повторно проверяет его перед первой записью и восстанавливает исходные файлы при наблюдаемом исключении; это не гарантия восстановления после потери питания. Команда не меняет индекс, историю или refs. Если Git уже отклонил коммит со служебными каталогами, исправленный потомок всё ещё передаёт их в истории: публикационная проверка должна охватывать изолированный граф выбранного коммита. Граница и воспроизводимая регрессия сохранены в [этапе исправления публикации Swift](../../Журнал/2026-09-12_05-16-46_MSK_исправить-публикацию-снимков-Swift/запрос.md).

Адресные сценарии `tests/test_служебные_имена_Git.py` и `tests/test_перенос_снимков.py` не требуют сети; первый использует Git во временном репозитории. Малый профиль `tests/профиль_служебных_имён.py --выход <профиль.json>` измеряет кодирование, план и применение при неизменных байтах. Проверочные команды запускаются через отчётную обёртку текущего Журнала.

Локальные тесты скриптов навыка запускаются без сетевых вызовов:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s Инструменты/fum-materialyi-zaprosov/tests -p 'test_*.py'
```

При изменении скриптов сначала добавляй или обновляй тест ожидаемого поведения, затем меняй реализацию и повторяй запуск тестов. Сквозной автономный контракт общего входа использует фикстуру `tests/fixtures/simple-html/` и последовательность `v1 -> v2 -> поздний сбой`: первый снимок содержит JSON-LD, второй удаляет отсутствующий структурный файл, а поздний сбой оставляет второй снимок побайтно неизменным. Регрессионный набор специализированного ChatGPT-share-входа дополнительно включает последовательности `полный снимок -> неполный снимок`, `полный снимок -> поздний сбой staging` и отказ без записи, когда атомарный обмен каталогов недоступен.

## ChatGPT share

Для расшаренных чатов ChatGPT используй скрипт:

```bash
python3 Инструменты/fum-materialyi-zaprosov/scripts/archive-chatgpt-share.py \
  "https://chatgpt.com/share/<id>" \
  --request-file "Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md"
```

Скрипт по умолчанию сохраняет в папку источников `Источники/URL/https/chatgpt.com/share/<id>/`. `--output-dir` используется только для явного нестандартного размещения, а `--source-name` сохранён для совместимости и не нужен для обычного URL-пути.

В папке источников сохраняются:

- `source-url.txt` - исходная ссылка.
- `chatgpt-share.headers.txt` - HTTP-заголовки с редактированными cookie.
- `chatgpt-share.html` - HTML-ответ с редактированным локальным bootstrap-состоянием.
- `chatgpt-share.script-XX.txt` - крупные скриптовые блоки и потоковые данные приложения.
- `chatgpt-share.initial-state.json` - редактированное начальное состояние страницы, если оно найдено.
- `chatgpt-share.react-router-stream.txt` - извлечённый поток React Router.
- `chatgpt-share.decoded-data.json` - распакованные данные диалога, если формат распознан.
- `chatgpt-share.messages.json` - полный структурный слой извлечённых сообщений в порядке диалога.
- `<название-диалога-в-kebab-case>.md` - оформленный человекочитаемый слой диалога.
- `source-index.md` - человекочитаемый индекс источника со ссылками на оформленный слой, отчёт и структурные файлы.
- `extraction-report.md` - отчёт о способе извлечения, количестве сообщений и редакциях.
- `snapshot-manifest.json` - точный отсортированный перечень всех файлов, которыми управляет установленный снимок, включая сам манифест.

Оформленный Markdown-файл должен быть назван по человекочитаемому названию диалога из источника или безопасному fallback-названию, например `запуск-долгоживущей-цепочки.md`. Заголовок первого уровня равен этому названию, а сообщения имеют читаемые русские подписи ролей. Дословный диапазон сообщений ограничивается единственной парой `<!-- FUM-CHATGPT-SHARE-VERBATIM:BEGIN -->` и `<!-- FUM-CHATGPT-SHARE-VERBATIM:END -->`: относительные Markdown-ссылки внутри него являются данными внешнего разговора и не переинтерпретируются как ссылки памяти FUM, тогда как служебная шапка, индекс и отчёт архива остаются обычным проверяемым Markdown. Повторный или непарный маркер не создаёт исключения. Служебные сообщения, выводы инструментов и машинные JSON-вызовы не включаются в оформленный слой; они сохраняются в `chatgpt-share.messages.json`.

TeX-формулы в оформленном Markdown-слое приводятся к формату, который Obsidian отображает через MathJax: блочные формулы пишутся в `$$ ... $$`, а строчные формулы - в `$...$`. Сырой структурный слой не нормализуется ради отображения.

Если в папке источника есть `source-index.md`, укажи в нём человекочитаемое название диалога и ссылку на оформленный Markdown-файл с названием диалога, чтобы источник можно было понять без открытия сырого JSON или HTML.

Скрипт также обновляет файл, переданный через `--request-file`: добавляет раздел `## Прикрепляемые материалы` со ссылками на папку источника, `source-index.md` и `extraction-report.md`. Повторный запуск для того же запроса и источника не должен дублировать эти ссылки.

Если скрипт не смог извлечь сообщения, оставь сырой HTML, заголовки, потоковые блоки и отчёт с причиной. Не выдумывай отсутствующий текст диалога.

Если диалог должен передать изменение репозитория от внешнего агента, архивирование остаётся первым шагом и не считается приёмкой изменения. После него используй [навык приёма внешнего вклада](../fum-priyom-vneshnego-vklada/SKILL.md): он ищет в структурном слое ровно один inline-пакет, проверяет точную Git-базу, manifest и хэш и материализует недоверенный patch только в явно указанное место. Вложения `sandbox:/...`, устные утверждения о коммите и внешние самоотчёты этим контуром не восстанавливаются.

## Другие материалы

Для нераспознанного формата сохраняй наиболее близкий к сырому слой, который реально получен:

- для URL - заголовки, тело ответа, effective URL, код ответа и дату извлечения;
- для файлов - оригинальный файл без нормализации и отдельный отчёт о происхождении;
- для динамических страниц - HTML, скриншот или текст DOM, если они доступны, плюс описание ограничения.

Если материал невозможно получить из-за доступа, истечения share-ссылки или защиты, зафиксируй это в отчёте и в файле запроса.

## Источники требований

- [исходный запрос 2026-07-20 22:05:19 MSK - Сделать повторное архивирование источника атомарным](../../Журнал/2026-07-20_22-05-19_MSK_сделать-повторное-архивирование-источника-атомарным/запрос.md)
- [исходный запрос 2026-07-21 10:36:18 MSK - Завершить сквозную приёмку архиватора источников](../../Журнал/2026-07-21_10-36-18_MSK_завершить-сквозную-приёмку-архиватора-источников/запрос.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-12 05:45:55 MSK -->
<!-- content-sha256: sha256:f25566012a7a7db9766b9d46a182888f823aa8b8d92773c5020123e435801703 -->
<!-- FUM-MD-RECENCY:END -->
