---
name: fum-kompleksnaya-proverka-repozitoriya
description: Запускать единый локальный smoke-check репозитория FUM с хэшированной историей тестов, аналитическим порядком, длительностями подготовки, каждого шага и полного процесса: тесты автоматизаций, Git-зависимости, реестры, машинно-локальные пути, остаток непереведённых объявлений кода, корневую инструкцию и индекс документации, вопросы, recency и связность рабочей сессии.
---

# FUM Smoke Check

Эта локальная [автоматизация FUM](../../Глоссарий/автоматизация-FUM.md) собирает основной проверочный контур репозитория в одну команду. Она нужна перед коммитами и публикационными сессиями, когда важно быстро подтвердить сохранность проектной настройки, отключающей внешний каталог навыков, и локальность разрешённых путей `SKILL.md`, прохождение тестов локальных автоматизаций, сборку SwiftPM-прототипов, каноническую структуру папок запросов, соответствие Git-зависимости LinguisticKit fork/upstream-контракту, воспроизводимость проверяемых реестров, согласованность имён автоматизаций с каталогами, отсутствие машинно-локальных путей в публикуемом содержимом, отсутствие новых непереведённых first-party объявлений кода, компактность корневой инструкции и полноту отдельного индекса документации, двунаправленность вопросов, свежесть Markdown-recency и связность выбранной [рабочей сессии](../../Глоссарий/рабочая-сессия.md) с `запрос.md`, соседним `отчёт.md` и Git-состоянием.

По умолчанию smoke-check работает локально: он не требует секретов, сетевых запросов и внешних сервисов. Все шаги запускаются через явные команды Python и уже существующие локальные автоматизации. Исполнитель измеряет монотонное wall-clock-время подготовки, каждого объявленного шага и полного процесса, чтобы журнальный профиль мог показывать внутренние узкие места единого запуска. После подготовки он сначала выполняет стабильный ранний префикс репозиторных валидаторов, затем аналитически упорядоченные Python- и Swift-тесты и только после них фиксированный хвост сборок и lint. Благодаря этому опечатка или структурное расхождение в отчёте, снимке, recency либо другом репозиторном контракте останавливает прогон до дорогих наборов.

Нулевой код внутреннего проверочного процесса означает только, что объявленный проверочный план пройден. Сам `run-smoke-check.py` не выполняет Git-коммит и не передаёт FIFO: после его выхода обязательная отчётная обёртка ещё сохраняет терминальную запись запуска, закрывает хэшированный снимок и допускает ограниченные проверки замыкания. Полная smoke-сессия с осмысленным diff считается успешно завершённой только при результате `committed` от атомарного `commit+handoff` в точной ветке текущей [карточки цепочки шагов](../../Глоссарий/карточка-цепочки-шагов.md). Ошибка внутреннего smoke, закрытия отчёта или передачи не превращается в успех; после `committed` никакие записи в checkout, индекс, refs или внешнее состояние не выполняются.

## Когда использовать

Используй эту автоматизацию, когда нужно:

- выполнить единый регрессионный прогон перед коммитом рабочей сессии;
- убедиться, что `.codex/config.toml` сохраняет `skills.include_instructions = false` и не возвращает внешний каталог навыков в агентский контекст;
- проверить, что новые или изменённые локальные автоматизации покрыты тестами;
- проверить тесты, исполняемые продукты и lint-контракт всех SwiftPM-пакетов верхнего уровня в `Прототипы/`;
- проверить, что запросы находятся в `Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_slug]>/запрос.md`, отчёты лежат рядом, а старого `Запросы/` и верхнеуровневых журнальных отчётов нет;
- пересобрать и сверить проверяемые реестры репозитория;
- проверить полный учёт новых и legacy-имён автоматизаций, их slug, эталоны и состояние зависимости LinguisticKit;
- проверить действующее содержимое на машинно-локальные пути и раскрытие Swift `#filePath`;
- проверить, что в коде не появились новые непереведённые first-party объявления, а точный временный остаток не вырос;
- проверить компактность корневой инструкции и полноту тематического индекса в `Документация/README.md`;
- проверить существование, регистр и обратные ссылки заявленных целей открытых и частично прояснённых вопросов;
- убедиться, что `FUM-MD-RECENCY` и индекс Markdown-файлов свежие;
- проверить связность выбранного файла `Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md`.

## Команда запуска

Внутренний полный проверочный этап рабочей сессии:

```bash
python3 Инструменты/fum-kompleksnaya-proverka-repozitoriya/scripts/run-smoke-check.py \
  --request Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md \
  --commit-message-file <путь-к-файлу-сообщения> \
  --codex-thread-id <корневой-CODEX_THREAD_ID>
```

Для запросов с именем родительской папки начиная с `2026-07-14_02-31-47_MSK_добавлять-идентификатор-сеанса-Codex` оба новых параметра обязательны; исторические запросы можно проверять без них. Список проверочных команд без запуска тестов, сборки и lint:

```bash
python3 Инструменты/fum-kompleksnaya-proverka-repozitoriya/scripts/run-smoke-check.py \
  --request Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md \
  --list
```

При построении списка `Package.swift` оцениваются локальным `swift package dump-package` с полным набором offline-флагов, чтобы показать фактические продукты, цели и зависимости без обновления, prefetch, `.netrc`, keychain, общего кэша зависимостей и кэша манифеста. Тесты, сборка и lint в режиме `--list` не запускаются.

Частичный локальный запуск без проверки конкретной рабочей сессии:

```bash
python3 Инструменты/fum-kompleksnaya-proverka-repozitoriya/scripts/run-smoke-check.py \
  --skip-session-coherence
```

## Что запускается

Автоматизация сначала строит весь канонический план, а затем исполняет четыре упорядоченные фазы:

1. Подготовка проверяет проектную изоляцию навыков, обнаруживает наборы и пакеты, выполняет offline-разбор SwiftPM-манифестов, сверяет политику пакетов и загружает только закрытую проверенную историю.
2. Ранний фиксированный префикс в точном стабильном порядке проверяет структуру папок запросов; собирает и сразу валидирует плановый реестр; проверяет реестр названий автоматизаций; машинно-локальные пути; остаток перевода объявлений кода; Git-зависимость LinguisticKit; скрипты запуска прототипов; двунаправленность вопросов; тематический индекс README; recency Markdown; тепловую карту графа; связность рабочей сессии. Порядок сохраняет зависимости `сборка → проверка`, `пути → объявления` и `recency → граф → связность`.
3. Независимые Python- и Swift-тесты упорядочиваются по проверенной истории. Известные наборы хотя бы с одной ошибкой сортируются по возрастанию эмпирической вероятности успеха, затем по средней длительности и относительному POSIX-ключу. Наборы без учтённых исходов следуют отдельным исследовательским блоком по ключу; они не смешиваются с известным хвостом без наблюдавшихся ошибок. Известные наборы без ошибок завершают тестовую очередь по длительности и ключу. Ненулевой код, естественное аварийное завершение теста сигналом и `не завершено` считаются наблюдаемым неуспехом; внешнее `прервано` остаётся цензурированным исходом.
4. Фиксированный Swift-хвост сохраняет прежний взаимный порядок: сборка каждого исполняемого продукта пакета, затем строгий lint либо проверенное lint-исключение. Ранние проверки не объявляются аналитическими тестами, а сборки и lint не поднимаются перед тестами.

Ниже команды перечислены по видам проверок для справки; фактическую хронологию определяют эти четыре фазы.

Статистика читается только из записей `fum.test-run.v2`, входящих в полностью проверенные закрытые `снимок.json`. Открытые сессии, legacy-записи `fum.test-run.v1` и терминальные записи `fum.осиротевший-запуск-проверки.1` не дают наблюдений: для осиротевшего запуска не наблюдались ни длительность, ни код завершения. Перед пропуском записи аналитика всё равно проверяет целостность всего закрытого журнала штатным валидатором отчётов. Подготовленный, восстанавливаемый или повреждённый закрытый журнал останавливает подготовку. Первый запуск нового набора поэтому детерминирован, но не выдаёт отсутствие истории за надёжность.

При полном запуске через обязательную обёртку отчётов сначала создаётся канонический конверт `fum.smoke-test-observations.v1` с UUID и `план: null`. Это сохраняет исходный код раннего отказа подготовки. До исполнения первого раннего шага smoke-check атомарно добавляет точный упорядоченный план только аналитических тестов. Если ранняя фиксированная проверка отказывает, терминальная v2-запись сохраняет непустой план и пустые наблюдения: это допустимый fail-fast до первого теста, а не повреждение протокола. Перед каждым достигнутым тестом сохраняется текущий шаг, а после исхода — фактический префикс. Обёртка проверяет каноничность ключей, полноту успешного плана, единственный последний неуспех при fail-fast и согласованность длительностей. При тайм-ауте достигнутый текущий тест добавляется как `не завершено`, при внешнем сигнале — как `прервано`. План и наблюдения хэшируемо встраиваются в самопроверяемую v2-запись и питают сортировку следующего запуска. Переменные capability удаляются из окружения каждого вложенного шага.

Перед построением списка шагов автоматизация разбирает `.codex/config.toml` стандартным TOML-парсером и требует точное логическое значение `skills.include_instructions = false`. Затем она разрешает символические ссылки каждого `Инструменты/*/SKILL.md` и отклоняет путь, вышедший за корень checkout. Отсутствующий, синтаксически неверный или ослабленный проектный конфиг, сломанная ссылка и внешний путь останавливают как обычный запуск, так и режим `--list` до остальных проверок. Эта статическая проверка защищает сохранность проектного контракта, но не обещает поведение неизвестной будущей версии Codex или запуска с явным переопределением настройки; применимость текущих runtime отдельно проверяется через их модельный вход.

Обычный запуск печатает машиночитаемые строки `smoke-timing <JSON>`. Записи `manifest` измеряют каждый вызов `swift package dump-package`, `preparation` — всю подготовку списка, `step` — каждый объявленный шаг, а `total` — полный интервал от входа в исполнитель до успеха или остановки на ошибке. Поле `result` сохраняет исход каждой границы; `exit_code` добавляется, если вложенный процесс завершился ненулевым кодом или не смог запуститься. Невозможность запустить вложенный процесс стабильно обозначается `exit_code: 127`, а ошибка фазы подготовки завершает внешний smoke-check с кодом `2`. `duration_seconds` содержит неотрицательные секунды с миллисекундной точностью. `manifest`, `preparation` и `step` являются вложенной детализацией `total` и не складываются с ним как независимые вызовы. Режим `--list` не запускает объявленные шаги, но по-прежнему измеряет фактически выполненную подготовку и разбор SwiftPM-манифестов.

1. Находит все каталоги `Инструменты/*/tests`, содержащие `test_*.py`, и запускает для каждого:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s <tests-dir> -p 'test_*.py'
```

2. Находит все `Прототипы/*/Package.swift`, получает фактические продукты и пути целей через:

```bash
swift package \
  --package-path <путь-к-пакету> \
  --disable-dependency-cache \
  --manifest-cache none \
  --disable-prefetching \
  --disable-netrc \
  --disable-keychain \
  --disable-automatic-resolution \
  dump-package
```

Для каждого пакета автоматизация запускает автономные тесты:

```bash
swift test \
  --package-path <путь-к-пакету> \
  --disable-dependency-cache \
  --manifest-cache none \
  --disable-prefetching \
  --disable-netrc \
  --disable-keychain \
  --disable-automatic-resolution
```

Каждый объявленный исполняемый продукт собирается отдельно:

```bash
swift build \
  --package-path <путь-к-пакету> \
  --disable-dependency-cache \
  --manifest-cache none \
  --disable-prefetching \
  --disable-netrc \
  --disable-keychain \
  --disable-automatic-resolution \
  --product <имя-продукта>
```

По умолчанию все пути целей и `Package.swift` проходят строгий lint:

```bash
swift format lint \
  --configuration Инструменты/fum-kompleksnaya-proverka-repozitoriya/swift-format.json \
  --strict \
  --recursive \
  <путь-к-пакету>/Package.swift \
  <пути-целей>
```

Центральная [конфигурация форматтера](swift-format.json) фиксирует набор правил и параметров вместо зависимости от неявного файла среды. Файлы `.swift-format-ignore` запрещены во всей применимой иерархии, потому что они могут молча исключить явно переданный Swift-файл даже из строгого lint.

Правило `IdentifiersMustBeASCII` явно отключено, чтобы строгий lint разрешал кириллические Swift-идентификаторы. Исполнитель по-прежнему передаёт `--strict`; остальные правила центральной конфигурации не ослабляются.

[Политика SwiftPM-пакетов](swift-package-policy.json) схемы `2` хранит ожидаемый инвентарь пакетов, исполняемых продуктов и точный allowlist прямых локальных package- и product-связей. Исчезнувший пакет или продукт, новый незарегистрированный пакет и несовпадение с фактическим `dump-package` останавливают smoke-check. Пакет без зависимостей обязан явно хранить `"localDependencies": []`: прежняя строгая граница для него не ослабляется.

Минимальная разрешённая композиция выглядит так:

```json
{
  "schemaVersion": 2,
  "defaultMode": "strict",
  "packages": [
    {
      "package": "Прототипы/consumer",
      "executableProducts": ["ConsumerCLI"],
      "localDependencies": [
        {
          "package": "Прототипы/provider",
          "identity": "provider",
          "products": [
            {
              "target": "ConsumerCLI",
              "product": "ProviderLibrary"
            }
          ]
        }
      ]
    },
    {
      "package": "Прототипы/provider",
      "executableProducts": ["ProviderCLI"],
      "localDependencies": []
    }
  ],
  "exceptions": []
}
```

`package` всегда является каноническим repo-relative-путём ровно к одному соседнему верхнеуровневому пакету `Прототипы/<имя>`. `identity` совпадает с фактической package identity из `dump-package`. Каждый элемент `products` задаёт точную связь потребляющей цели `target` с библиотечным продуктом `product` провайдера; лишняя, исчезнувшая или переименованная связь считается дрейфом. Зависимость в `Package.swift` записывается единственной переносимой литеральной формой `.package(path: "../provider")`. Именованные, вычисляемые, абсолютные, неканонические и дополнительные аргументы не допускаются.

Чтобы статическая граница не расходилась с итоговым объектом SwiftPM, манифест ограничен декларативной формой: после директивы tools version разрешён только `import PackageDescription`, затем ровно один `let package = Package(...)` и никакого кода после закрывающей скобки. Поле `dependencies:` самого этого инициализатора является литеральным массивом разрешённых вызовов. Вспомогательная фабрика, alias, backtick-вызов, неиспользуемый allowlist-маркер и последующая мутация `package.dependencies` закрываются отказом.

SwiftPM неизбежно возвращает `fileSystem.path` абсолютным. Smoke-check раскрывает символические ссылки, выполняет `realpath`-проверку существования и нахождения внутри корня репозитория и `Прототипы/`, после чего нормализует путь обратно в точный repo-relative-вид и сопоставляет его с политикой и исходным литералом. Выход за любую границу, self-dependency, повтор package path или identity, повтор product-связи и цикл закрываются отказом. Так же до тестов и сборки отклоняются source-control-, registry-, неизвестные и binary-зависимости, неоднозначная внешняя `byName`-связь, условная product-связь и product, который провайдер фактически не экспортирует как библиотеку.

Чтобы добавить зависимость:

1. Зарегистрируй потребителя и провайдера в `packages`; для остальных пакетов сохрани пустые allowlist.
2. Добавь потребителю канонический `.package(path: "../<сосед>")` и явную `.product(name: ..., package: ...)` в конкретную цель.
3. В `localDependencies` зафиксируй repo-relative-путь провайдера, identity из свежего локального `dump-package` и все точные пары `target`/`product`.
4. Запусти тесты автоматизации, затем полный smoke-check. Не добавляй разрешение ради уже наблюдаемого дрейфа: сначала проверь происхождение изменившейся identity или связи.

Offline-флаги одинаково применяются к `dump-package`, `swift test` и `swift build`. Контур не загружает зависимости и не использует пользовательские credential- и dependency-кэши; локальный путь является единственным разрешённым транспортом между пакетами.

Если временное lint-исключение необходимо, оно обязано содержать причину, критерий снятия, существующий источник и SHA-256 центральной конфигурации, `Package.swift` и Swift-файлов целей. Исключение печатается в обычном запуске и `--list`; любое изменение защищённого снимка останавливает smoke-check как устаревшая политика. Текущая политика не содержит исключений: все действующие SwiftPM-пакеты проходят строгий lint вместе с обязательными тестами и сборками исполняемых продуктов.

3. Проверяет структуру папок запросов и контракт их канонических шаблонов:

```bash
python3 Инструменты/fum-struktura-papok-zaprosov/scripts/struktura-papok-zaprosov.py \
  validate \
  --repo-root .
```

Проверка требует обязательный временной префикс имени родительской папки и `запрос.md`, допускает `материалы/`, запрещает прежний `Запросы/` и разрешает непосредственно в `Журнал/` только `README.md` среди Markdown-файлов. Для новой рабочей сессии обязателен соседний `отчёт.md`; историческая папка может оставаться без него только при отсутствии отчёта до миграции. Тем же вызовом закрыто проверяются точные поля, активный каркас, обязательные разделы, порядок заголовков и выравнивание таблиц хранимых шаблонов `запрос.md.шаблон` и `отчёт.md.шаблон`; автопоиск тестов отдельно подтверждает генерацию по этим файлам, отказ при незаполненном маркере и независимую валидацию результата.

4. Пересобирает машинно читаемый плановый реестр:

```bash
python3 Инструменты/fum-reyestr-planirovaniya/scripts/build-planning-registry.py build \
  --output Планирование/реестр-требований-вариантов-и-кандидатов.json
```

5. Проверяет свежесть планового реестра:

```bash
python3 Инструменты/fum-reyestr-planirovaniya/scripts/build-planning-registry.py validate \
  --registry Планирование/реестр-требований-вариантов-и-кандидатов.json
```

6. Проверяет реестр названий автоматизаций:

```bash
python3 Инструменты/fum-proverka-nazvanij-avtomatizacij/scripts/proveritj-nazvaniya-avtomatizacij.py \
  --repo-root . \
  --registry Инструменты/реестр-названий-автоматизаций.json
```

Канонический реестр находится в состоянии `ready`, поэтому полный smoke-check требует материализованный пакет и живой вызов LinguisticKit. После свежего клонирования сначала нужно [инициализировать submodule и локальный `upstream`](../../Зависимости/README.md); перевод реестра в `blocked` не является допустимым обходом этой операции.

7. Проверяет содержимое на машинно-локальные пути:

```bash
python3 Инструменты/fum-proverka-mashinno-lokaljnyikh-putej/scripts/proveritj-mashinno-lokaljnyiye-puti.py \
  --repo-root .
```

Проверка использует Git-инвентарь, печатает стабильные обезличенные категории и отличает действующие нарушения от типизированных системных, тестовых, исторических и внешних случаев. Полный smoke-check останавливается на любой новой first-party-регрессии.

8. Проверяет перевод first-party объявлений кода сразу после сканера машинно-локальных путей:

```bash
python3 Инструменты/fum-perevod-obyyavlenij-koda-na-russkij-yazyik/scripts/перевести-объявления-кода.py \
  проверить \
  --корень-репозитория . \
  --снимок Инструменты/fum-perevod-obyyavlenij-koda-na-russkij-yazyik/остаток-объявлений-кода.json
```

Снимок хранит точный временный остаток ещё не переведённых объявлений на момент поэтапной миграции. Он не является общим allowlist или постоянным исключением: любое новое непереведённое объявление или расширение остатка останавливает smoke-check. По мере перевода снимок осмысленно сокращают вместе с кодом; его рост запрещён.

9. Автономно проверяет Git-топологию подключённого LinguisticKit:

```bash
python3 Инструменты/fum-proverka-git-zavisimostej/scripts/proveritj-git-zavisimostj.py check \
  --repo-root . \
  --fork-url https://github.com/fum-lab/LinguisticKit.git \
  --upstream-url https://github.com/Roman-Kerimov/LinguisticKit.git \
  --path Зависимости/LinguisticKit \
  --revision 837e2ce107b97ee7b9d3344c9fe99142281fe393
```

10. Проверяет обязательные точки входа всех устойчивых прототипов:

```bash
python3 Инструменты/fum-zapusk-prototipov/scripts/check-prototype-launchers.py
```

11. Проверяет двунаправленность открытых и частично прояснённых вопросов:

```bash
python3 Инструменты/fum-obratnyiye-ssyilki-voprosov/scripts/check-question-backlinks.py
```

12. Проверяет корневую инструкцию и отдельный тематический индекс документации:

```bash
python3 Инструменты/fum-indeks-readme/scripts/check-readme-index.py \
  --repo-root .
```

13. Проверяет recency-метки без записи:

```bash
python3 Инструменты/fum-svezhestj-markdown/scripts/update-md-recency.py --check
```

14. Проверяет тепловую карту графа Obsidian без записи:

```bash
python3 Инструменты/fum-svezhestj-grafa-obsidian/scripts/build-obsidian-graph-recency.py \
  --check
```

Проверка использует опорную дату тепловой карты из `.obsidian/fum-recency-reference-date`. Поэтому переход календарного дня сам по себе не делает неизменный снимок ошибочным; для перехода к новому календарному срезу генератор сначала запускается с явным `--today YYYY-MM-DD`.

15. Проверяет связность выбранной рабочей сессии:

```bash
python3 Инструменты/fum-svyaznostj-rabochej-sessii/scripts/check-session-coherence.py \
  --request Журнал/<YYYY-MM-DD_HH-MM-SS_MSK[_краткое-название-запроса]>/запрос.md \
  --commit-message-file <путь-к-файлу-сообщения> \
  --codex-thread-id <корневой-CODEX_THREAD_ID>
```

## Проверки автоматизации

Локальные тесты самой автоматизации запускаются без сети и секретов:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s Инструменты/fum-kompleksnaya-proverka-repozitoriya/tests -p 'test_*.py'
```

Тесты фиксируют базовый контракт: smoke-check требует отключённую передачу общего каталога навыков в проектной конфигурации, отклоняет выход локального `SKILL.md` за корень через символическую ссылку, обнаруживает тесты локальных автоматизаций и SwiftPM-пакеты, выполняет точный ранний префикс до первого аналитического набора, не запускает при его отказе тесты, сборку и lint, сохраняет риск-сортировку тестов и взаимный порядок фиксированного Swift-хвоста, ставит известные отказы до исследовательского блока, а известные наборы без наблюдавшихся ошибок после него, считает аварийный сигнал и незавершившийся достигнутый тест ошибкой, не считает внешнее прерывание ошибкой, сохраняет только достигнутый fail-fast-префикс, читает историю только из закрытых хэшированных снимков, не включает в аналитику осиротевшие запуски с ненаблюдённой длительностью и не передаёт capability-переменные дочерним шагам. Набор также сверяет инвентарь пакетов и продуктов, читает продукты из результата `dump-package`, запускает `swift test`, отдельно собирает каждый исполняемый продукт, применяет центральный строгий lint по умолчанию с разрешёнными кириллическими Swift-идентификаторами, запрещает `.swift-format-ignore`, проверяет хэш временного исключения и его видимый проход через исполнитель шагов, включает явную проверку структуры папок запросов, добавляет сборку и проверку планового реестра, явные проверки реестра названий автоматизаций, машинно-локальных путей и точного временного остатка непереведённых объявлений кода, точки входа прототипов, корневую инструкцию и отдельный индекс документации, запускает проверку двунаправленности вопросов и recency-проверки, требует `--request` для проверки связности рабочей сессии, разбирает обязательность commit-context по имени родительской папки и отклоняет папку без корректного временного префикса как новую идентичность, для новых запросов требует файл сообщения и корневой Codex-Thread-ID, передаёт их в `fum-svyaznostj-rabochej-sessii`, сохраняет совместимость с историческими запросами и допускает частичный запуск с `--skip-session-coherence`.

Регрессия схемы `2` отдельно начинает с прежнего отказа схемы `1`, создаёт настоящую локальную SwiftPM-композицию и подтверждает успешную подготовку. Затем она проверяет обязательный пустой allowlist, точное сопоставление identity и product-связей, абсолютный, неканонический, отсутствующий и вышедший через символическую ссылку путь, выход за репозиторий и `Прототипы/`, self-dependency, дубликаты, collision identity, цикл, source-control-, registry-, неизвестные и binary-зависимости, неявную внешнюю `byName`-связь, backtick/unused-marker и последующую мутацию итогового объекта `Package`. Внедряемый монотонный таймер закрепляет стабильные записи длительности каждого шага и полного интервала при успехе и ошибке, а режим `--list` отдельно подтверждает измерение разбора манифеста и подготовки без запуска тестов, сборки или lint. Отдельный end-to-end-тест создаёт отслеживаемую first-party-регрессию и подтверждает остановку полного исполнителя именно на новом сканере.

## Граница автоматизации

Smoke-check не заменяет смысловую проверку изменений, публикационную чистоту diff и решение агента о том, какие файлы нужно коммитить. Он объединяет локальные структурные проверки и останавливается на первом упавшем шаге, чтобы ошибка оставалась наблюдаемой и не маскировалась следующими командами.

Прямой нулевой выход этой команды нельзя сообщать как завершённый успех smoke-сессии. Корневая задача сначала замыкает журнальные доказательства без повторного полного прогона, индексирует только проверенное состояние и вызывает очередь. Для задачи, связанной с карточкой цепочки, `branch_ref` допуска, текущий symbolic `HEAD` и `ветка` карточки должны совпадать; атомарный `commit+handoff` двигает именно этот ref. Единственное bootstrap-исключение — уже допущенная на `refs/heads/master` сессия, которая вводит сам веточный переход: она остаётся на исходной ветке и завершает миграцию прежним атомарным протоколом.

Если во время выполняющегося smoke-check обнаружен уже подтверждённый дефект, делающий проверяемый снимок заведомо непринимаемым, корневой агент немедленно прерывает дочерний процесс штатным сигналом. Обязательная обёртка отчётов сохраняет исход `прервано`; после исправления полный smoke-check запускается заново и именно новый успешный прогон становится приёмочным. Продолжать старый прогон разрешено только ради заранее названной диагностики его оставшихся шагов, когда эта диагностика действительно нужна для исправления; сбор дополнительных зелёных результатов сам по себе не оправдывает продолжение.

Если в репозитории появляется новый проверяемый реестр, его нужно добавить в `run-smoke-check.py` и закрепить ожидание тестом. Если у новой локальной автоматизации появляются тесты в `Инструменты/<имя>/tests`, smoke-check начнёт запускать их автоматически.

Обнаружение SwiftPM намеренно ограничено устойчивыми прототипами вида `Прототипы/<имя>/Package.swift`, поэтому кэши `.build`, `.swiftpm` и пакеты транзитивных зависимостей не становятся самостоятельными входами smoke-check. Разрешаются только прямые локальные связи между зарегистрированными соседними пакетами; транзитивная достижимость не заменяет allowlist прямой связи. Локальная служебная обёртка использует LinguisticKit только через отдельно проверяемый Git submodule. Результат `dump-package` зависит от локального Swift toolchain и платформы, поэтому неизвестная структура манифеста или отсутствие исполняемого продукта считается ошибкой, а не основанием тихо пропустить сборку.

Текущий Swift-контур предназначен для macOS: все пакеты требуют macOS 14 или новее, Swift 6.0 или новее и Xcode с командами `swift` и `swift format`, а сборки smoke-check являются отладочными сборками SwiftPM по умолчанию. Центральная конфигурация не содержит имён правил, отсутствующих в Swift 6.0, но более старый форматтер может игнорировать неизвестные ему дополнительные параметры, поэтому идентичная семантика lint между разными версиями toolchain не обещается. Принятый снимок полностью проверен на Swift 6.4 и Xcode 27.0; это наблюдаемая текущая среда, а не обещание бинарной совместимости со всеми будущими toolchain.

## Источники требований

- [исходный запрос 2026-08-14 20:08:55 MSK — Учесть аудит доверия проверок и сопровождаемости FUM](../../Журнал/2026-08-14_20-08-55_MSK_учесть-аудит-доверия-проверок-и-сопровождаемости-FUM/запрос.md)
- [исходный запрос 2026-08-08 13:37:10 MSK — Внедрить веточные цепочки шагов](../../Журнал/2026-08-08_13-37-10_MSK_внедрить-веточные-цепочки-шагов/запрос.md)
- [исходный запрос 2026-08-08 07:56:16 MSK — Починить автозапуск FUM](../../Журнал/2026-08-08_07-56-16_MSK_починить-автозапуск-FUM/запрос.md)
- [исходный запрос 2026-08-06 20:56:43 MSK — Оптимизировать работу тестов](../../Журнал/2026-08-06_20-56-43_MSK_оптимизировать-работу-тестов/запрос.md)
- [исходный запрос 2026-08-06 15:14:50 MSK — Сделать README инструкцией использования FUM](../../Журнал/2026-08-06_15-14-50_MSK_сделать-README-инструкцией-использования-FUM/запрос.md)
- [исходный запрос 2026-08-06 11:22:33 MSK - Добавить аналитику порядка запуска тестов](../../Журнал/2026-08-06_11-22-33_MSK_добавить-аналитику-порядка-запуска-тестов/запрос.md)
- [исходный запрос 2026-08-04 15:48:19 MSK - Шаблонизировать файлы запросов и отчётов](../../Журнал/2026-08-04_15-48-19_MSK_шаблонизировать-файлы-запросов-и-отчётов/запрос.md)
- [исходный запрос 2026-07-27 16:12:29 MSK - Учитывать все проверочные вызовы в профиле времени](../../Журнал/2026-07-27_16-12-29_MSK_учитывать-все-проверочные-вызовы-в-профиле-времени/запрос.md)
- [исходный запрос 2026-07-23 18:12:05 MSK - Проверить контракт чистого модельного шага для исполняемого агентского цикла](../../Журнал/2026-07-23_18-12-05_MSK_проверить-контракт-чистого-модельного-шага-для-исполняемого-агентского-цикла/запрос.md)
- [исходный запрос 2026-07-23 15:26:35 MSK - Запретить внешние навыки в репозитории](../../Журнал/2026-07-23_15-26-35_MSK_запретить-внешние-навыки-в-репозитории/запрос.md)
- [исходный запрос 2026-07-14 02:31:47 MSK - Добавлять идентификатор сеанса Codex](../../Журнал/2026-07-14_02-31-47_MSK_добавлять-идентификатор-сеанса-Codex/запрос.md)
- [исходный запрос 2026-07-17 12:20:17 MSK - Создать скрипты запуска прототипов](../../Журнал/2026-07-17_12-20-17_MSK_создать-скрипты-запуска-прототипов/запрос.md)
- [исходный запрос 2026-07-20 15:34:46 MSK - Включить SwiftPM в общий smoke check](../../Журнал/2026-07-20_15-34-46_MSK_включить-SwiftPM-в-общий-smoke-check/запрос.md)
- [исходный запрос 2026-07-20 23:08:44 MSK - Восстановить обратные ссылки вопросов](../../Журнал/2026-07-20_23-08-44_MSK_восстановить-обратные-ссылки-вопросов/запрос.md)
- [исходный запрос 2026-07-21 05:39:00 MSK - Сделать служебные генераторы воспроизводимыми](../../Журнал/2026-07-21_05-39-00_MSK_сделать-служебные-генераторы-воспроизводимыми/запрос.md)
- [исходный запрос 2026-07-21 11:32:46 MSK - Актуализировать входные описания FUM](../../Журнал/2026-07-21_11-32-46_MSK_актуализировать-входные-описания-FUM/запрос.md)
- [исходный запрос 2026-07-21 12:18:37 MSK - Закрепить транслитерацию названий автоматизаций](../../Журнал/2026-07-21_12-18-37_MSK_закрепить-транслитерацию-названий-автоматизаций/запрос.md)
- [исходный запрос 2026-07-21 13:40:42 MSK — Актуализировать форк и подключить LinguisticKit](../../Журнал/2026-07-21_13-40-42_MSK_актуализировать-форк-и-подключить-LinguisticKit/запрос.md)
- [исходный запрос 2026-07-22 09:33:05 MSK - Снять lint исключение теневого редактора продолжений](../../Журнал/2026-07-22_09-33-05_MSK_снять-lint-исключение-теневого-редактора-продолжений/запрос.md)
- [исходный запрос 2026-07-22 13:39:29 MSK — Устранить машинно-локальные пути](../../Журнал/2026-07-22_13-39-29_MSK_устранить-машинно-локальные-пути/запрос.md)
- [карточка шага FUM-STEP-0070](../../Планирование/карточки-шагов/✅-FUM-STEP-0070-устранить-машинно-локальные-абсолютные-пути-и-добавить-их-автоматическую-проверку.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-15 02:06:29 MSK -->
<!-- content-sha256: sha256:c2d2620bfdd808bf61c60d99ee984601629239252c2eb7b2ed1e0007e7e69884 -->
<!-- FUM-MD-RECENCY:END -->
