---
name: fum-proverka-mashinno-lokaljnyikh-putej
description: Проверять содержимое репозитория FUM на машинно-локальные абсолютные пути, домашние сокращения, file URI и раскрытие Swift #filePath.
---

# Проверка машинно-локальных путей

Эта локальная [автоматизация FUM](../../Глоссарий/автоматизация-FUM.md) выполняет read-only-аудит опубликованного содержимого репозитория. Она строит инвентарь через `git ls-files`, поэтому одинаково видит пути из индекса и новые неигнорируемые файлы, но не обходит Git submodule и не зависит от сети или секретов. Результат детерминированно сортируется и не повторяет найденные буквальные значения: каждая строка имеет форму `путь:строка:категория`.

## Когда использовать

Используй проверку перед фиксацией рабочей сессии, после добавления генератора Markdown, нового исходного кода или проверки путей, а также при изменении типизированной политики допустимых случаев. Общий smoke-check запускает её отдельным обязательным шагом.

## Команда запуска

Из корня репозитория:

```bash
python3 Инструменты/fum-proverka-mashinno-lokaljnyikh-putej/scripts/proveritj-mashinno-lokaljnyiye-puti.py \
  --repo-root .
```

Код `0` означает отсутствие действующих нарушений, даже если отчёт содержит типизированные `allow.*` и `report.*`. Код `1` означает нарушение содержимого, а код `2` — недостоверный Git-инвентарь или ошибку контракта политики. Сканер отклоняет произвольные POSIX-абсолюты, домашние каталоги пользователей, Windows drive, обратносолэшевые и прямослэшевые UNC, `file://`, одиночный `~`, именные формы вроде `~user/path`, POSIX-, Windows- и PowerShell-переменные домашнего каталога и Swift `#filePath`.

## Типизированная граница

Дословное содержимое только раздела `## Текст запроса` в `Запросы/` и файлы под `Источники/` остаются происхождением в режиме `report.*`. Обычные URL маскируются до распознавания локальных путей. Системные runtime-пути, shebang, якоря `.gitignore` и документированные обезличенные примеры получают отдельные контекстные категории; произвольный системный путь в first-party-коде не разрешается. Само нахождение строки в каталоге сканера, в файле другой проверки или в `tests` ничего не разрешает.

Gitlink выводится как граница `report.gitlink`: сканер не входит в vendored историю. Поэтому известное upstream-использование `#filePath` в `LinguisticKitBuildTool` фиксируется как ограничение зависимости, а first-party Swift-файл с `#filePath` останавливает проверку.

Исторические доказательные цитаты, определения распознавателя и тестовые фикстуры вне структурных блоков происхождения допускаются только через [policy.json](policy.json). Каждая запись схемы v2 закрепляет точный относительный путь, вид формы, SHA-256 всей строки, ожидаемое число совпадений, одну базовую категорию из закрытого списка `report.historical`, `allow.path-validation-definition` или `allow.test-fixture` и содержательную причину. Поэтому фикстура не переименовывается в историческую цитату. Неиспользуемая запись, изменившаяся строка, неизвестное поле или категория, wildcard, выход из репозитория, дубликат или чрезмерный счётчик делают политику ошибочной; новая соседняя строка остаётся нарушением.

## Автономные тесты

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover \
  -s Инструменты/fum-proverka-mashinno-lokaljnyikh-putej/tests \
  -p 'test_*.py'
```

Тесты без сети и секретов проверяют все распознаваемые формы, Git-инвентарь, стабильность и обезличивание отчёта, структурные категории происхождения, узкие допустимые случаи, строгий fingerprint-контракт политики и отказ на искусственной first-party-регрессии.

## Источники требований

- [исходный запрос 2026-07-22 13:39:29 MSK — Устранить машинно-локальные пути](../../Запросы/2026-07-22_13-39-29_MSK_устранить-машинно-локальные-пути.md)
- [карточка шага FUM-STEP-0070](../../Планирование/карточки-шагов/✅-FUM-STEP-0070-устранить-машинно-локальные-абсолютные-пути-и-добавить-их-автоматическую-проверку.md)
- [аудит абсолютных путей](../../Ревью/2026-07-22_12-35-05_MSK_аудит-абсолютных-путей.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-07-22 14:46:18 MSK -->
<!-- content-sha256: sha256:71046e8e7109a49ed85112939e73ca234397cf86bbb10ff784b96ef819431266 -->
<!-- FUM-MD-RECENCY:END -->
