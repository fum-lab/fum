---
name: fum-session-time
description: Формирует канонические временной префикс и заголовочную метку рабочей сессии FUM в зоне Europe/Moscow. Используй перед созданием файлов в Запросы/, Журнал/ и Источники/, если их имя содержит суффикс _MSK, особенно когда локальная зона среды отличается от московской.
---

# Московское время рабочей сессии FUM

Получай обе формы времени одним вызовом:

```bash
python3 Инструменты/fum-session-time/scripts/get-session-time.py --format both
```

Скопируй значение `prefix` без изменений в имена файлов запроса, журнала и связанных материалов. Используй соответствующее значение `label` в заголовках и текстовых ссылках. Не выводи MSK-время из локальных часов среды и не заменяй `Europe/Moscow` текущей зоной хоста.

Для воспроизводимой проверки конкретного момента передавай ISO 8601 с `Z` или явным смещением:

```bash
TZ=Europe/Saratov python3 Инструменты/fum-session-time/scripts/get-session-time.py \
  --at 2026-07-17T07:07:09Z \
  --format both
```

Ожидаемый результат:

```text
prefix=2026-07-17_10-07-09_MSK
label=2026-07-17 10:07:09 MSK
```

## Проверка

Запускай автономные тесты без сети и секретов:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover \
  -s Инструменты/fum-session-time/tests \
  -p 'test_*.py'
```

Тесты закрепляют преобразование UTC-момента в `Europe/Moscow`, независимость от зоны хоста, согласованность двух форм и отклонение неоднозначного времени без зоны.

## Граница автоматизации

Скрипт только вычисляет канонические формы одного момента. Создание файлов, навигация запросов, журналирование и проверка ссылок остаются отдельными шагами рабочей сессии.

## Источники требований

- [исходный запрос 2026-07-17 10:25:41 MSK - Предотвращать смещение времени сессий](../../Запросы/2026-07-17_10-25-41_MSK_предотвращать-смещение-времени-сессий.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-07-17 10:32:56 MSK -->
<!-- content-sha256: sha256:f85cfcad177252a5b83de0d592d3744e58a3ba3421aeb0b08e34157cbcfdad01 -->
<!-- FUM-MD-RECENCY:END -->
