---
name: fum-dispetcher-avtomatizacij-fum
description: Проверяет закрытый реестр заданий диспетчера автоматизаций FUM, чисто выбирает не более одного готового запуска и управляет защищённой Git-резервацией его точного поколения без вызова host или исполнения эффекта.
---

# Диспетчер автоматизаций FUM

Локальный навык закрепляет машинный контракт [диспетчера автоматизаций FUM](../../Глоссарий/диспетчер-автоматизаций-FUM.md) отдельно от карточочного `fum-sleduyusjhij-shag-vetki`. Он проверяет версионированную декларацию заданий, чисто моделирует готовность и выбирает не более одного запуска, а затем позволяет compare-and-swap-операциями закрепить точное поколение в служебной Git-ссылке. Сам навык не вызывает host, не создаёт задачу и не исполняет заявленный эффект.

Каноническое русское смысловое название автоматизации — `диспетчер автоматизаций FUM`. Закреплённое LinguisticKit-преобразование `.Cyrl → .Latn` по таблице `.ru` даёт `dispetcher avtomatizacij FUM`, а нормализация FUM — технический slug `fum-dispetcher-avtomatizacij-fum`. Существующий навык следующего шага ветки сохраняет своё имя и контракт.

## Версия и расположение реестра

Схема версии `1` имеет устойчивый идентификатор `fum.automation-job-registry.v1` и хранится в [реестр-заданий-v1.schema.json](схемы/реестр-заданий-v1.schema.json). Веточные реестры размещаются в `Планирование/реестры-заданий-автоматизаций/`; контракт `master` находится в [master.json](../../Планирование/реестры-заданий-автоматизаций/master.json).

Имя файла служит навигацией, а не доказательством цели. Корень реестра требует полный `branch_ref` из строгого безопасного ASCII-подмножества именованных Git-ref, якорь рабочей копии `.`, устойчивый идентификатор проекта и относительный путь его паспорта. Каждое задание повторяет те же четыре поля в `цель`; валидатор требует их точного совпадения с реестром. Команда также получает явный `--корень-рабочей-копии`, требует в нём обычный маркер `.git`, разрешает паспорт внутри этой рабочей копии и отвергает отсутствующий файл, символическую ссылку в любом компоненте пути или выход наружу. Поэтому запись однозначно относится к переданной Git-рабочей копии, полному ref и декларативно названному проекту, а абсолютный путь машины в публикуемый контракт не попадает.

## Закрытая запись задания

Каждый объект и каждая вложенная структура имеют `additionalProperties = false`. Точный набор обязательных полей задания:

| Поле                       | Смысл                                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------------------ |
| `job_id`                   | Устойчивый идентификатор задания; это явно закреплённое латинское поле машинного контракта.            |
| `поколение`                | Положительное поколение спецификации, меняющееся при содержательном изменении задания.                 |
| `адаптер`                  | Тип источника работы и версия его отдельного контракта.                                                |
| `цель`                     | Полный `branch_ref`, рабочая копия, проект и относительный путь проекта.                                |
| `триггер`                  | Ровно одна форма наступления: расписание либо порог подтверждённых событий.                            |
| `условия_допуска`          | Отдельные наблюдаемые логические предикаты, проверяемые только после наступления триггера.              |
| `состояние`                | Точное `active`, `paused`, `blocked` или историческое `retired`.                                      |
| `эффект`                   | Класс `только_чтение`, `изменение_репозитория` или `внешний_эффект` и обязательная политика эффекта.   |
| `исполнитель`              | Один из двух версионированных типов; мутирующий эффект требует обычную корневую задачу.                |
| `защита_поколения`         | Fence копирует поколение задания и проверяется общей Git-резервацией перед границей вызова.             |
| `курсор_результата`        | Закрытая форма расписания либо событий, согласованная с формой триггера.                               |
| `политика_ошибки`          | Закрытый отказ, повтор и поведение при неоднозначном результате.                                       |

Область fence имеет точное значение `рабочая_копия_ветка_задание`. Исполнитель является закрытым объединением точных пар `локальный_пробник` / `локальный-симулятор.1` и `обычная_корневая_задача_Codex` / `обычная-корневая-задача-Codex.1`; классы `изменение_репозитория` и `внешний_эффект` принимают только вторую пару. Поле `приоритет` обязательно и даёт явную очерёдность после срока. Симулятор по-прежнему возвращает состояние каждого задания в порядке `приоритет`, `job_id`, а отдельная команда выбора применяет порядок запуска.

Канонический реестр `master` поколения `2` содержит ровно одно активное задание `master.next-step` поколения `1`. Оно запускается каждые 300 секунд, указывает адаптер `следующий_шаг_ветки` с контрактом `fum-sleduyusjhij-shag-vetki.1`, точную цель текущего checkout и ветки и обычную корневую задачу Codex для изменения репозитория. В общий реестр не копируются `card_id`, задача или критерии карточки. Шесть условий допуска отдельно закрепляют свободную очередь, первую проверку простоя Codex, чистую рабочую копию, готовый карточочный рабочий набор, однозначный проект и вторую проверку простоя Codex.

## Триггер, условия и состояния

`расписание` содержит UTC-якорь и положительный интервал в секундах; ненулевой курсор обязан совпадать с точным наступлением `якорь + k × интервал`. `порог_подтверждённых_событий` содержит тип события и положительное `каждые`; курсор закрепляет следующее кратное пороговое значение. Формы взаимоисключающие: поле одной формы внутри другой является противоречием, а не неявной комбинацией.

Наступивший срок или достигнутый порог только создаёт occurrence. Отдельно вычисляются `триггер_наступил`, `условия_выполнены` и `состояние_разрешает`; `готово` истинно только при одновременном выполнении всех трёх условий. Невыполненное условие даёт `ожидание_условий`, но не меняет триггер и не продвигает курсор. Для `paused`, `blocked` и `retired` симулятор честно показывает наступивший срок, но `состояние_разрешает = false` и `готово = false`.

## Чистый выбор

Команда `выбрать` читает только уже проверенные реестр и наблюдения. Наблюдения можно передать файлом или точным однострочным `--наблюдения-json`; время и счётчики всегда являются явным входом, системные часы внутри чистого ядра не читаются. Для готового задания строится типизированное `trigger_occurrence`: расписание хранит точный UTC-момент, а счётный триггер — тип события и закрытый интервал подтверждённых событий. `run_key` является SHA-256 канонического объекта из точных `job_id`, `spec_generation` и `trigger_occurrence`.

Выбирается не более одного задания. Устойчивый ключ порядка состоит из ранга типа наступления, собственного срока внутри типа, `приоритет` и `job_id`. Расписание имеет ранг `0`, порог событий — ранг `1`; этим документируется порядок между несравнимыми временной и счётной шкалами. Внутри расписания раньше идёт более ранний UTC-момент, внутри событий — лексикографически меньший тип события и меньшая граница порога. Неготовое задание не участвует в минимуме и потому не блокирует независимое готовое. Положительный результат дополнительно возвращает неизменные `адаптер`, `цель`, `эффект`, `исполнитель` и текущий `selection_head`, чтобы внешний тик маршрутизировал адаптер и ограждал точную вершину без повторного вывода из свободного текста.

## Защищённая резервация

Мутирующие команды принимают только реестр, побайтово совпадающий с файлом из текущей вершины активной именованной ветки. Разобранный объект, по которому вычислен выбор, обязан совпасть с тем же проверенным Git-снимком; конкурентная смена вершины и файла между чтением и сравнением закрывает операцию до резервации. Резервация схемы `2` хранится вне рабочей копии и индекса под `refs/fum/резервации-запусков-автоматизаций/<хэш-рабочей-копии>/<хэш-ветки>/<хэш-job_id>`. Такая область не зависит от карточочных `step_id` и `selection.id`, но точное содержимое закрепляет `branch_ref`, `selection_head`, идентичность, схему, поколение и хэш реестра, `job_id`, `spec_generation`, `trigger_occurrence`, `run_key`, курсор до запуска, канонический UUID попытки и отдельные `task_id`/`generation` фактического FIFO-исполнителя.

Первичная запись и каждый переход выполняются сравнением и заменой Git-ссылки; переход перед вызовом дополнительно одной транзакцией проверяет неизменную вершину ветки. Точный повтор той же клиентской попытки до границы вызова идемпотентен. Чужая попытка, старое поколение и любое нетерминальное либо неопределённое прежнее владение получают код `4` и состояние `уже_зарезервировано`.

| Фаза                   | Разрешённый следующий переход                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------- |
| `зарезервирован`       | Безопасное `освободить` до host-грани либо `начать-вызов-среды`.                                                 |
| `вызов_мог_состояться` | `подтвердить-создание`, общий `bind-run` фактической задачи или закрытый внешний recovery.                       |
| `задача_создана`       | Общие `bind-run` и `verify-run`, затем терминализация по точному `last_completion` очереди.                      |
| `завершён`             | Новая попытка после безопасного отказа либо после успеха только для нового `run_key`.                            |

`начать-вызов-среды` является только долговечной меткой перед внешней границей: команда не вызывает host. После неё повторный host-вызов запрещён, пока внешний наблюдатель не подтвердит исход. `подтвердить-создание` сохраняет ровно наблюдённый host-идентификатор, но не считает его фактическим `task_id`, не считает успехом и не продвигает курсор. В частности, подготовительный `clientThreadId` не подменяет собственный `CODEX_THREAD_ID` созданной задачи.

После FIFO-допуска дочерняя задача сама вызывает общий `bind-run` с собственным `task_id`. Общий `verify-run` одной Git-транзакцией проверяет неизменные резервацию и ветку, точного владельца queue ref, совпадение `task_id` и `generation`, а также `base_head == selection_head`; грязная рабочая копия закрывает fence. Только после этого адаптер подтверждает свой отдельный run-fence, и исполнитель начинает работу. Для адаптера следующего шага один UUID одновременно является общей попыткой и карточочным `lease_id`.

После локального `commit`+handoff исполнитель больше ничего не записывает. Следующий тик вызывает `подтвердить-завершение-исполнителя`: `committed` с точными `task_id`, `generation` и `base_head` даёт общий `успех`; `finished_clean` даёт безопасный отказ только после host-доказательства окончательной остановки и карточочного recovery со значением `released` либо `unclaimed`. Занятая очередь, иной `last_completion`, несвязанный исполнитель или отсутствие recovery закрывают терминализацию. `освободить` разрешено только до возможного эффекта, а прежний общий `завершить --исход неопределённый` остаётся закрытым терминальным запретом автоматического повтора. Сам общий слой не меняет курсор реестра: его согласованное продвижение вместе с результатом относится к адаптеру и терминальному протоколу эффекта.

## Команды

Проверить реестр без сети, host, секретов, Git-индекса и записи:

```bash
python3 Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/диспетчер-автоматизаций.py \
  проверить \
  --корень-рабочей-копии . \
  --реестр Планирование/реестры-заданий-автоматизаций/master.json \
  --схема Инструменты/fum-dispetcher-avtomatizacij-fum/схемы/реестр-заданий-v1.schema.json \
  --json
```

Чисто смоделировать тик на явно переданных наблюдениях:

```bash
python3 Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/диспетчер-автоматизаций.py \
  симулировать \
  --корень-рабочей-копии . \
  --реестр Инструменты/fum-dispetcher-avtomatizacij-fum/tests/фикстуры/корректный-реестр.json \
  --схема Инструменты/fum-dispetcher-avtomatizacij-fum/схемы/реестр-заданий-v1.schema.json \
  --наблюдения Инструменты/fum-dispetcher-avtomatizacij-fum/примеры/наблюдения-пробника.json \
  --json
```

Чисто выбрать не более одного запуска на тех же наблюдениях:

```bash
python3 Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/диспетчер-автоматизаций.py \
  выбрать \
  --корень-рабочей-копии . \
  --реестр Инструменты/fum-dispetcher-avtomatizacij-fum/tests/фикстуры/корректный-реестр.json \
  --схема Инструменты/fum-dispetcher-avtomatizacij-fum/схемы/реестр-заданий-v1.schema.json \
  --наблюдения Инструменты/fum-dispetcher-avtomatizacij-fum/примеры/наблюдения-пробника.json \
  --json
```

Резервация требует авторитетный реестр из текущей вершины активной ветки и exact-поля результата `выбрать`:

```bash
python3 Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/диспетчер-автоматизаций.py \
  зарезервировать \
  --корень-рабочей-копии . \
  --реестр Планирование/реестры-заданий-автоматизаций/master.json \
  --схема Инструменты/fum-dispetcher-avtomatizacij-fum/схемы/реестр-заданий-v1.schema.json \
  --наблюдения <проверенный-файл-наблюдений> \
  --expected-job-id <job_id> \
  --expected-spec-generation <поколение-задания> \
  --expected-registry-generation <поколение-реестра> \
  --expected-run-key <run_key> \
  --идентификатор-попытки <канонический-UUID> \
  --json
```

`начать-вызов-среды` повторяет полную проверку выбора, условий, реестра и вершины. `подтвердить-создание`, `освободить`, `состояние-резервации` и legacy-команда `завершить` адресуют точную резервацию по `--expected-branch-ref`, `--expected-job-id`, `--expected-run-key` и идентификатору попытки; post-host-команды также принимают exact `selection_head`, поколения задания и реестра. Для живого адаптера next-step терминальный успех не записывается legacy-командой `завершить`, а выводится из очереди.

Дочерний исполнитель после admission выполняет общий `bind-run`, затем `verify-run` с теми же expected-полями, собственным `task_id` и поколением FIFO:

```bash
python3 Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/диспетчер-автоматизаций.py \
  bind-run \
  --корень-рабочей-копии . \
  --expected-branch-ref refs/heads/<ветка> \
  --expected-selection-head <selection_head> \
  --expected-job-id <job_id> \
  --expected-spec-generation <поколение-задания> \
  --expected-registry-generation <поколение-реестра> \
  --expected-run-key <run_key> \
  --идентификатор-попытки <канонический-UUID> \
  --task-id "$CODEX_THREAD_ID" \
  --json
```

`verify-run` добавляет к тем же аргументам `--generation <поколение-FIFO>`. Следующий плановый тик читает `состояние-резервации` и при связанном запуске вызывает `подтвердить-завершение-исполнителя` с теми же expected-полями. Для `finished_clean` обязательный итог восстановления адаптера передаётся как `--adapter-recovery-state released` либо `unclaimed`; для `committed` этот флаг не нужен.

Один локальный пробник выполняет второй вызов из корня рабочей копии:

```bash
Инструменты/fum-dispetcher-avtomatizacij-fum/scripts/пробник.sh
```

Автономные тесты используют положительный реестр, таблицу отрицательных мутаций, отдельную фикстуру повторного JSON-ключа и явные наблюдения. Они также проверяют строгую безопасную подмножину Git-ref, Git-маркер рабочей копии, промежуточные символические ссылки, сетку расписания и связь исполнителя с эффектом. Отдельный набор на временных Git-репозиториях закрепляет детерминированный выбор, параллельное сравнение и замену, рестарты, чужую попытку, смену поколения, независимые ссылки разных заданий и все терминальные исходы:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover \
  -s Инструменты/fum-dispetcher-avtomatizacij-fum/tests \
  -p 'test_*.py'
```

## Граница реализации

Реализованы схема, fail-closed-валидатор, автономный симулятор, чистый выбор, защищённый жизненный цикл Git-резервации схемы `2`, общий run-fence фактического FIFO-исполнителя и терминализация по точному завершению очереди. Канонический реестр `master` содержит один активный маршрут `master.next-step`; действующий карточочный selector, `show`, `claim`, `bind-run`, `verify-run`, `rearm` и recovery-`release` остаются внутри специализированного навыка следующего шага.

Пятиминутный плановый тик общего диспетчера остаётся без FIFO-билета и не меняет checkout. Он дважды проверяет штатный host-снимок задач, механически формирует шесть наблюдений, резервирует общий запуск, вызывает специализированный адаптер и ограждает единственный `create_thread`. Созданная обычная задача первым инструментальным действием входит в FIFO и после admission подтверждает сначала общий, затем карточочный fence. Пользовательский ход в той же прикреплённой задаче не является heartbeat-тиком и перед мутацией проходит обычный корневой контракт очереди.

Существующие прикреплённая задача и heartbeat мигрируются только на месте: меняются их отображаемые имена и канонический prompt, а target, пятиминутное расписание, status и история сохраняются. `Stop`/`Start` меняет только статус и не освобождает общую резервацию или claim уже созданного запуска. Сам локальный сценарий по-прежнему не вызывает host, не создаёт задачу, не меняет рабочую копию, не продвигает курсор и не выполняет внешний эффект; host-оркестрация задаётся каноническим heartbeat-промптом и проверяется отдельным read-only аудитом.

## Источники требований

- [исходный запрос 2026-08-05 12:02:53 MSK — Перенести автозапуск шагов в универсальный диспетчер](../../Журнал/2026-08-05_12-02-53_MSK_перенести-автозапуск-шагов-в-универсальный-диспетчер/запрос.md)
- [исходный запрос текущей рабочей сессии](../../Журнал/2026-08-05_09-07-08_MSK_добавить-универсальный-выбор-и-защищённую-резервацию-запуска/запрос.md)
- [исходный запрос 2026-08-05 05:48:39 MSK — Закрепить контракт универсального диспетчера автоматизаций FUM](../../Журнал/2026-08-05_05-48-39_MSK_закрепить-контракт-универсального-диспетчера-автоматизаций-FUM/запрос.md)
- [исходный запрос 2026-07-27 15:21:35 MSK — Сделать диспетчер автоматизаций ветки универсальным](../../Журнал/2026-07-27_15-21-35_MSK_сделать-диспетчер-автоматизаций-ветки-универсальным/запрос.md)
- [архитектура диспетчера автоматизаций FUM](../../Документация/45-диспетчер-автоматизаций-FUM.md)
- [требование универсальной диспетчеризации](../../Требования/🚧-универсальная-диспетчеризация-периодических-автоматизаций.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-05 13:55:12 MSK -->
<!-- content-sha256: sha256:b52f5ec51c0d6f9e9cd568b1314c00b866fe3fbced4a0f41b1c9c5f2279a5857 -->
<!-- FUM-MD-RECENCY:END -->
