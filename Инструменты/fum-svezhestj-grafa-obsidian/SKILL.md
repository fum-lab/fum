---
name: fum-svezhestj-grafa-obsidian
description: Обновлять группы цвета графа Obsidian как тепловую карту Markdown-узлов по времени последнего содержательного редактирования.
---

# FUM Obsidian Graph Recency

Этот навык описывает локальную [автоматизацию FUM](../../Глоссарий/автоматизация-FUM.md), которая пересобирает цветовые группы графа Obsidian в игнорируемом `.obsidian/graph.json` как тепловую карту Markdown-узлов по служебным меткам `FUM-MD-RECENCY`. Git хранит генератор и его контракт, но не локальные выходы Obsidian.

Автоматизация не меняет содержательные документы и не вычисляет новое время редактирования. Она читает уже сохранённые recency-метки, группирует `.md`-файлы по возрасту и записывает в `colorGroups` поисковые запросы Obsidian вида `path:"..."`, чтобы цвет применялся к конкретным узлам, а не к случайным упоминаниям дат в тексте.

Множество входов задаёт общая автоматизация [fum-proyektnyiye-fajlyi](../fum-proyektnyiye-fajlyi/SKILL.md). Весь корневой `.obsidian/`, как и `.build`, `.swiftpm` и каталоги кэшей, не является входом тепловой карты, даже если локальный файл принудительно добавлен в индекс.

## Когда использовать

Используй автоматизацию после `fum-svezhestj-markdown`, чтобы локальная цветовая карта Obsidian отражала актуальные recency-метки. Выходы никогда не добавляются в индекс, коммит или публикационный diff.

## Команда запуска

```bash
python3 Инструменты/fum-svezhestj-grafa-obsidian/scripts/build-obsidian-graph-recency.py
```

Команда обновляет:

- `collapse-color-groups` в `.obsidian/graph.json`, чтобы блок групп цвета был раскрыт;
- `colorGroups` в `.obsidian/graph.json`, заменяя прежние группы на корзины тепловой карты;
- `.obsidian/fum-recency-reference-date` с опорной датой календарного представления.

Если свежий checkout ещё не содержит `.obsidian/`, обычное обновление создаёт каталог и оба локальных выхода. Они сразу остаются игнорируемыми Git.

Для проверки без записи используется:

```bash
python3 Инструменты/fum-svezhestj-grafa-obsidian/scripts/build-obsidian-graph-recency.py --check
```

Полное отсутствие обоих локальных выходов в свежем checkout считается применимым пропуском без записи. Если существует хотя бы один из двух файлов, проверка становится строгой: требует оба и отклоняет неверный JSON, неверную дату или устаревшие группы.

В Git-checkout и обновление, и проверка дополнительно требуют ровно одну точную корне-якорную строку для `.obsidian/` в корневом `.gitignore`, её эффективное действие без последующего повторного включения выходов или контрольных потомков и пустой Git-инвентарь для `.obsidian/**`.

Обновление принимает явную опорную дату через `--today YYYY-MM-DD` либо использует текущую дату MSK и сохраняет её в проектном sidecar-файле `.obsidian/fum-recency-reference-date`. Проверка без `--today` повторно использует сохранённую дату, поэтому неизменный Git-снимок не становится устаревшим только из-за наступления следующего дня. Явный `--today` переопределяет сохранённую дату и позволяет проверить или пересобрать представление для другого календарного среза.

Опорная дата хранится отдельно от `graph.json`, потому что Obsidian удаляет незнакомые поля при сохранении собственного состояния. Обычная перезапись настроек приложением не уничтожает календарный якорь автоматизации.

## Цветовые корзины

Тепловая карта строится от горячих свежих узлов к холодным старым:

- `0 days` - файлы, изменённые сегодня;
- `1 day` - файлы, изменённые вчера;
- `2 days` - файлы, изменённые два дня назад;
- `3-4 days` - файлы, изменённые три-четыре дня назад;
- `5 days` - файлы, изменённые пять дней назад;
- `6 days` - файлы, изменённые шесть дней назад;
- `7 days` - файлы, изменённые семь дней назад;
- `8 days` - файлы, изменённые восемь дней назад;
- `9 days` - файлы, изменённые девять дней назад;
- `10+ days` - более старые файлы.

Палитра идёт десятью явными ступенями от красного через оранжевый, жёлтый, зелёно-бирюзовый и сине-бирюзовый к синему, чтобы переходы между свежими и старыми узлами были визуально плавнее. Дневная детализация первого десятидневного окна выбрана потому, что текущая память FUM обновляется часто и должна показывать различия между соседними рабочими сессиями.

## Проверки

Локальные тесты автоматизации запускаются без сети и секретов:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s Инструменты/fum-svezhestj-grafa-obsidian/tests -p 'test_*.py'
```

Общий smoke-check репозитория также запускает режим `--check`. Он всегда проверяет Git-политику `.obsidian/`, пропускает отсутствующую локальную пару и обнаруживает устаревшую тепловую карту, если она материализована.

## Граница автоматизации

Скрипт опирается на поисковый синтаксис Obsidian и текущий JSON-формат `.obsidian/graph.json`. Воспроизводимы управляемые поля `collapse-color-groups` и `colorGroups` для выбранной даты; прочие локальные настройки графа сохраняются без заявления о побайтной воспроизводимости всего файла.

Оба точных локальных выхода проверяются до чтения и записи: символическая ссылка, выход за пределы репозитория или разрешение в структурно исключённое хранилище останавливают автоматизацию. Сам корневой `.obsidian/` исключён только из проектных входов; два явных выхода сохраняют отдельную защиту путей.

Если Obsidian изменит формат `graph.json` или поведение поисковых групп, нужно обновить тесты и этот контракт перед изменением скрипта.

## Источники требований

- [исходный запрос 2026-07-21 05:39:00 MSK - Сделать служебные генераторы воспроизводимыми](../../Журнал/2026-07-21_05-39-00_MSK_сделать-служебные-генераторы-воспроизводимыми/запрос.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-14 21:26:41 MSK -->
<!-- content-sha256: sha256:069a3482585dceb2cc4954fb2619092e19e7ef68d126161433841458afa3c142 -->
<!-- FUM-MD-RECENCY:END -->
