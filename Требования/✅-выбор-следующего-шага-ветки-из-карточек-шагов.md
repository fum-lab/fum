# Выбор следующего шага ветки из карточек шагов

<!-- FUM-REQUIREMENT-ID: FUM-REQ-0016 -->

Эксплуатационный статус: отложено. Карточка сохраняет веточный selector и его FIFO-допуск как архитектурную наработку; её императивный текст не действует для ручной последовательной записи и не разрешает автоматический выбор работы.

Если у развиваемой именованной [ветки работы](../Глоссарий/ветка-работы.md) остаются возможные [следующие шаги](../Глоссарий/следующий-шаг-ветки.md), её веточная запись должна хранить открытый версионный whitelist существующих канонических [карточек шагов](../Глоссарий/карточка-шага.md). Набор не материализует `ready` заранее: он аттестует независимо допустимые карточки как `dispatch=automatic`, сохраняет точные ALL-of-зависимости от завершённых карточек и отдельно удерживает явные `paused` или `blocked` с условиями возобновления. После допуска сессия-продолжение напрямую перечитывает точный `HEAD`, заново вычисляет готовность всего набора и выбирает конкретный готовый шаг. Корректный неготовый кандидат не участвует в ранжировании и не останавливает исполнение независимого готового.

Веточная запись хранит полный ref ветки, верхнеуровневое состояние `open` или `done`, проект и массив кандидатов. Каждая кандидатная запись хранит устойчивый `step_id` конкретной версии, `dispatch`, идентификатор карточки, хэш её содержания и массив `requires_completed_card_ids`; для явных `paused` и `blocked` обязательно условие возобновления. Идентификатор карточки не подменяет `step_id`: изменение карточки, её пути, хэша, `dispatch`, спецификации зависимостей или условия возобновления должно давать новую версию кандидатной записи. Изменение только наблюдаемого статуса обязательной карточки сохраняет `step_id`, но меняет вычисленный статус и `selection.id`. Задача, критерии завершения и источники читаются из runtime-готовой карточки, а не из независимой копии в веточной записи.

## Семантические связи

- **зависит от:** [атомарных карточек плановых шагов](✅-атомарные-карточки-плановых-шагов.md) — веточный выбор может ссылаться только на индексированную каноническую карточку с проверяемой идентичностью.
- **дополняется:** [пользовательским перенаправлением непрерывного агентского цикла](🟡-пользовательское-перенаправление-непрерывного-агентского-цикла.md) — дискретная смена продолжения между задачами становится прототипом для работающего продуктового цикла.
- **требуется для:** [контекстно посильных исполняемых шагов](🚧-контекстно-посильные-исполняемые-шаги.md) — вычисленный runtime-пул `ready` является точкой принятия контекстно ограниченной карточки допущенной сессией-продолжением.
- **требуется для:** [изолированного параллельного исполнения и проверяемой интеграции](✅-изолированное-параллельное-исполнение-и-проверяемая-интеграция.md) — каждый изолированный запуск должен происходить из точной версии одной канонической карточки шага.
- **усиливается:** [веточными цепочками шагов и завершением smoke-check коммитом](🚧-веточные-цепочки-шагов-и-завершение-smoke-check-коммитом.md) — кандидат получает проверяемую принадлежность конечной цепочке и её точной ветке фиксации.

## Критерии проверки

- для каждой автоматически развиваемой именованной ветки валидатор находит ровно одну веточную запись;
- верхнеуровневая запись схемы `5` и каждый кандидат содержат только поля своей формы; открытая запись содержит хотя бы одного кандидата, а состояние `done` — обязательный явный `candidates = []`;
- все `card_id` и `step_id` внутри набора уникальны, каждая запись ссылается на актуальную карточку по устойчивому идентификатору, закрепляет точный хэш её содержания, допустимый `dispatch` и массив `requires_completed_card_ids`;
- каждый `automatic` предварительно проходит проверки безопасности, полномочий и контекстной посильности; пустой массив зависимостей вычисляется как runtime-`ready`, а непустой — только когда все точные карточки имеют literal-статус `completed`;
- `active`, `absorbed` и `withdrawn` не удовлетворяют зависимости `requires_completed_card_ids`; свободный `resume_condition`, ссылки, время, сеть, среда, секреты и модельный вывод не интерпретируются как готовность;
- неизвестная обязательная карточка, повтор, самозависимость или цикл закрывают набор; явные `paused` и `blocked` требуют непустого условия возобновления, автоматически не открываются и при корректной форме не мешают независимому runtime-`ready`;
- задача, критерии завершения и источники в машинном ответе и задаче Codex получаются из выбранной runtime-готовой карточки, а не из независимой копии;
- только после FIFO-допуска сессия вызывает селектор, который заново вычисляет готовность на точном `HEAD` и по политике `dynamic-readiness-source-history-first-parent-v2` рассматривает не более 16 коммитов от нового к старому по первой родительской линии;
- для сопоставления каждого кандидата используются только дедуплицированные нормализованные локальные ссылки из его раздела `Источники`; собственный путь кандидатной карточки, служебные и управляющие пути исключаются;
- первым сигналом связанности служит изменённая завершённая или поглощённая карточка среди источников кандидата, следующим — точное изменение иного источника; внутри класса учитываются меньшая дистанция, большее число уникальных совпавших путей и затем пара `card_id`, `step_id`, а при отсутствии различающего сигнала применяется сразу эта устойчивая пара;
- связанность истории является мягким правилом порядка только внутри вычисленного множества допустимых `ready` и не отменяет зависимости, безопасность, полномочия, явный выбор пользователя или контекстную посильность;
- машинный ответ закрепляет объект `selection` с `id`, `policy`, точным `head`, `ready_count` и свидетельством `reason`, `commit`, `distance`, `matched_paths`, а допущенная сессия получает те же значения вместе с выбранными карточкой и `step_id`;
- запуск закрывается при отсутствующей, неиндексированной, повреждённой или недопустимой по жизненному статусу карточке, неверном хэше, повторе идентификаторов, недопустимых полях либо несовпадении ожидаемых `branch_ref`, `selection.id`, `selection.head` или `step_id`;
- изменение карточки, её пути, содержательного хэша, `dispatch`, массива зависимостей или условия возобновления требует нового `step_id`; изменение наблюдаемого результата зависимости меняет `selection.id` без перевыпуска `step_id`; сессия принимает выбор только при совпадении полного ref, точного `selection.head`, `selection.id` и текущего FIFO-допуска;
- жизненный статус карточки, объявленный `dispatch` и вычисленный runtime-статус проверяются раздельно: `active` сама по себе не разрешает исполнение;
- `selection.id` хэширует объявления и результаты готовности всех кандидатов, пути и хэши их собственных карточек, условия явного возобновления, незавершённые зависимости и точные `card_id`, пути, статусы и хэши обязательных карточек вместе с готовым пулом, победителем и свидетельством ранжирования;
- завершённая сессия удаляет выполненное поколение и обновляет whitelist допустимости, но не вычисляет и не предвыбирает победителя следующего запуска; открытый набор без runtime-`ready` явно означает отсутствие исполняемого шага сейчас, а `done` не подменяется ссылкой на выполненную или снятую карточку.

## Статус и границы

[Статус требования FUM](../Глоссарий/статус-требования-FUM.md) — `✅`: требование реализовано в документационном прототипе: веточная запись схемы `5` хранит проверяемый whitelist канонических карточек и точные зависимости, а прямой селектор допущенной сессии вычисляет runtime-готовность, проверяет форму и индивидуальную допустимость кандидатов, выбирает по ограниченной истории источников и закрепляет полное доказательство выбора в `selection.id`.

Требование задаёт только ограниченный источник-связный порядок и не реализует общую оптимизацию полезности, стоимости или риска. Оно не создаёт новую задачу, не доказывает транзакционность Codex-host с Git и не координирует отдельные клоны. Создание продолжения, FIFO и восстановление сохраняются отдельными контрактами.

## Источники требований

- [исходный запрос 2026-08-23 11:33:38 MSK — Вернуть ручную последовательную схему сессий](../Журнал/2026-08-23_11-33-38_MSK_вернуть-ручную-последовательную-схему-сессий/запрос.md)
- [исходный запрос 2026-08-11 23:30:57 MSK — Заменить автозапуск обязательным продолжением ветки](../Журнал/2026-08-11_23-30-57_MSK_заменить-автозапуск-обязательным-продолжением-ветки/запрос.md)
- [исходный запрос 2026-07-29 09:04:03 MSK — Расширить динамический выбор следующего шага](../Журнал/2026-07-29_09-04-03_MSK_расширить-динамический-выбор-следующего-шага/запрос.md)
- [исходный запрос 2026-07-27 18:28:42 MSK — Выбирать следующий шаг при запуске с учётом истории коммитов](../Журнал/2026-07-27_18-28-42_MSK_выбирать-следующий-шаг-при-запуске-с-учётом-истории-коммитов/запрос.md)
- [исходный запрос 2026-07-27 15:21:35 MSK — Сделать диспетчер автоматизаций ветки универсальным](../Журнал/2026-07-27_15-21-35_MSK_сделать-диспетчер-автоматизаций-ветки-универсальным/запрос.md)
- [исходный запрос 2026-07-25 11:56:07 MSK — Закрепить контекстно ограниченную многоагентную реализацию FUM](../Журнал/2026-07-25_11-56-07_MSK_закрепить-контекстно-ограниченную-многоагентную-реализацию-FUM/запрос.md)
- [исходный запрос 2026-07-22 02:59:22 MSK — Декомпозировать предложения на карточки шагов](../Журнал/2026-07-22_02-59-22_MSK_декомпозировать-предложения-на-карточки-шагов/запрос.md)
- [исходный запрос 2026-07-03 11:49:25 MSK — Зафиксировать пошаговый отбор реализации](../Журнал/2026-07-03_11-49-25_MSK_зафиксировать-пошаговый-отбор-реализации/запрос.md)
- [исходный запрос 2026-07-20 20:06:04 MSK — Запускать следующие шаги веток](../Журнал/2026-07-20_20-06-04_MSK_запускать-следующие-шаги-веток/запрос.md)
- [исходный запрос 2026-07-20 21:22:17 MSK — Включить карточки требований в машинный плановый реестр](../Журнал/2026-07-20_21-22-17_MSK_включить-карточки-требований-в-машинный-плановый-реестр/запрос.md)
- [исходный запрос 2026-07-22 03:38:35 MSK — Разрешить выполнение доступных карточек шагов](../Журнал/2026-07-22_03-38-35_MSK_разрешить-выполнение-доступных-карточек-шагов/запрос.md)
- [исходный запрос 2026-07-24 10:01:26 MSK — Уточнить событийную непрерывность документационного прототипа FUM](../Журнал/2026-07-24_10-01-26_MSK_уточнить-событийную-непрерывность-документационного-прототипа-FUM/запрос.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-23 16:10:09 MSK -->
<!-- content-sha256: sha256:f959ed4681317c44e8a73a0ad0c03f63359898daf155f0fb33cfa026449a854e -->
<!-- FUM-MD-RECENCY:END -->
