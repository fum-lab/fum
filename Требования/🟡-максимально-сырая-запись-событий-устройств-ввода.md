# Максимально сырая запись событий устройств ввода

[Коробочная реализация FUM](../Глоссарий/коробочная-реализация-FUM.md) должна с максимально доступной на каждой поддерживаемой платформе точностью получать и долговременно сохранять события клавиатуры, трекпада, мыши, стилуса, графического планшета и других графических устройств ввода. Сохраняется наиболее сырой доступный представлению FUM поток до необратимого сведения к жестам или командам интерфейса: исходный порядок, монотонное время, тип и идентичность устройства, элемент управления, физическая фаза элемента, координаты, относительные смещения, нажатия, давление, наклон, состояние модификаторов, логическое состояние режимов фиксации, повтор и все иные предоставленные API поля.

Сырые события хранятся отдельно от производных жестов, команд и смысловых интерпретаций. Запись должна сохранять происхождение, версию схемы, сведения об использованном API и явно обозначенные потери, объединение событий, недоступные поля, разрывы и изменения конфигурации устройств. «Максимально сырая» означает наиболее ранний публично поддерживаемый и публикационно допустимый слой конкретной платформы, а не обход системной защиты, скрытый сбор или обещание аппаратных отчётов там, где операционная система их не предоставляет.

Для клавиатуры физическая фаза элемента управления хранится отдельно от состояния модификаторов и логического состояния режимов фиксации. В частности, целевая трасса должна различать нажатие и отпускание клавиши Caps Lock, а не подменять их включением и выключением одноимённого режима. Снимок или изменение режима Caps Lock может сохраняться рядом как самостоятельное поле. Если публичный API сообщает только переключение режима и не позволяет наблюдать физический переход без догадки, адаптер фиксирует недоступность фазы как потерю, не синтезирует вымышленное отпускание и по возможности дополняется более низкоуровневым источником.

## Семантические связи

- **дополняет:** [полноэкранное приложение без системной оболочки](🟡-полноэкранное-приложение-без-системной-оболочки.md) — предоставляет его основному пользовательскому контуру наблюдаемый и воспроизводимый поток действий человека, оставаясь самостоятельным межплатформенным требованием.

## Кандидаты реализации

| Кандидат                                                | Сильная сторона                                                                                   | Ограничение, которое нужно измерить                                                                                  |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Game Controller: `GCKeyboard`, `GCMouse`, другие `GCDevice` | Единая модель устройств во фреймворке Apple и наибольший кандидат на переносимость между платформами Apple | Это не один класс `GCController`; покрытие трекпада, стилуса, редких HID-полей, частота и объединение событий требуют проверки |
| AppKit `NSEvent`                                        | Богатые прикладные события клавиатуры, мыши, трекпада и планшета в macOS                          | Слой macOS и оконной системы уже интерпретирует ввод; глобальное наблюдение имеет ограничения и разрешения           |
| Core Graphics `CGEventTap`                              | Более низкий системный поток Quartz-событий клавиатуры и мыши в macOS с временными метками и полями | Не является переносимым API всех платформ Apple и не равен исходным HID-отчётам                                      |
| IOKit `IOHIDManager`                                    | Значения HID-элементов и входные отчёты ближе всего к устройству в macOS                          | Наиболее сложный и платформенно ограниченный путь; права, sandbox, встроенный трекпад и семантика отчётов требуют проверки |
| UIKit, SwiftUI и платформенные gesture/pointer API      | Поддерживаемый прикладной путь для касаний, указателя, пера и жестов на соответствующих платформах | Обычно даёт уже нормализованные или распознанные события и потому служит дополнением, а не эталоном сырости           |

Предпочтение нельзя отдавать одному кандидату только по широте API или по сырости. Архитектура может использовать переносимое общее ядро Game Controller и платформенные адаптеры более низкого уровня, если измерение подтвердит, что один источник не сохраняет требуемые данные.

## Критерии проверки

- одинаковые физические сценарии воспроизводятся через каждый доступный кандидат на macOS, iPadOS, iOS, tvOS и visionOS, где соответствующее устройство и публичный API поддерживаются;
- для каждого источника измеряются полнота полей, различимость устройств, временное разрешение, задержка, частота, порядок, пропуски, повтор, объединение событий и поведение под нагрузкой;
- для Caps Lock выполняются циклы из обоих начальных состояний режима; отчёт раздельно показывает наблюдаемые физические переходы «нажато» и «отпущено» и логическое состояние режима, не засчитывая его переключение как физическое отпускание;
- источник, который сворачивает физические переходы Caps Lock в изменение режима, получает явную отметку потери; способность фактически доступного и публикационно допустимого источника или сочетания источников сохранить обе фазы учитывается как отдельное преимущество при выборе наряду с разрешениями, ограничениями sandbox, задержкой, устойчивостью и стоимостью;
- на macOS отдельно сравниваются `GCKeyboard`/`GCMouse`, `NSEvent`, `CGEventTap` и `IOHIDManager`; для трекпада, стилуса и планшета проверяются доступные платформенные источники;
- сохранённая трасса допускает побитово устойчивое чтение по версии схемы и повторное построение производных жестов без изменения исходных записей;
- подключение, отключение, сон, пробуждение, смена раскладки, несколько одновременных устройств и переполнение очереди оставляют явные события или диагностируемые разрывы;
- сбор включается с явным согласием владельца, минимальными правами и документированным сроком хранения; чувствительный клавиатурный ввод защищён и не покидает локальный контур без отдельного разрешения;
- выбранный стек и отклонённые варианты сопровождаются воспроизводимым сравнительным отчётом, а не только архитектурным предположением.

## Статус и границы

[Статус требования FUM](../Глоссарий/статус-требования-FUM.md) — `🟡`: требование принято и запланировано; конкретный стек не выбран. Первичная гипотеза — переносимое ядро на Game Controller с платформенными адаптерами, но решение должен подтвердить Swift-прототип и измерения на реальных устройствах. Запись экрана, аудио, сетевых событий и синтез ввода находятся вне этой карточки.

## Источники требований

- [исходный запрос 2026-07-17 09:18:01 MSK](../Запросы/2026-07-17_09-18-01_MSK_добавить-карточку-сырой-записи-событий-ввода.md)
- [исходный запрос 2026-07-17 09:41:27 MSK](../Запросы/2026-07-17_09-41-27_MSK_уточнить-различение-нажатия-и-отпускания-Caps-Lock.md)
- [Game Controller](https://developer.apple.com/documentation/gamecontroller)
- [`GCKeyboard`](https://developer.apple.com/documentation/gamecontroller/gckeyboard)
- [`NSEvent`](https://developer.apple.com/documentation/appkit/nsevent)
- [`CGEvent`](https://developer.apple.com/documentation/coregraphics/cgevent)
- [`IOHIDManager`](https://developer.apple.com/documentation/iokit/iohidmanager_h)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-07-17 09:53:18 MSK -->
<!-- content-sha256: sha256:ce2bbb91b149d24f0e827d0051ec3d944553b2a7bb35084a2226aed29e4e0e75 -->
<!-- FUM-MD-RECENCY:END -->
