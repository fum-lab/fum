# Исходный запрос 2026-09-11 02:06:54 MSK - Поддержать форматы приложения в проекции

## Навигация по запросам

- Предыдущий запрос: [2026-09-11 01:28:44 MSK - Перенести исходники FUMA](../2026-09-11_01-28-44_MSK_перенести-исходники-FUMA/запрос.md)
- Следующий запрос: нет

## Текст запроса

````text
**Проверенная локальная наработка не всегда равна публично воспроизводимой поставке.** Например, журнал первого сегмента Swift-контейнера сохраняет результаты тестов и измерений, но указывает, что сам код находится в отдельном локальном репозитории без `origin`.

Eto dejstviteljno tak? Nuzhno togda sleduyusjhim shagom budet zanesti vsyo v yedinyij repozitorij, krome sabmoduljnyikh zavisimostej. 

````

````text
Nuzhno predotvratitj povtoreniye takoj situacii — po umolchaniyu vsyo kladyom v monorepu poka, krome vneshnikh zavisimostej, tipa LinguisticKit.

````

## Идентификатор сеанса Codex

Codex-Thread-ID: 01a08d6d-e706-7e70-9f70-fdfa5a6826c2

## Использованные инструменты

- [Реестр инструментов](../../Инструменты/реестр-системных-приложений-и-инструментов.md): Git 2.54.0 (Apple Git-157), Python 3.14.7 и стандартная библиотека; версии наблюдены локально. Swift и GUI не запускались.
- Контракты среды `exec_command`, `apply_patch` и дочерней координации; самостоятельные версии контрактов не раскрыты. Поверхность — Codex Desktop; приложение и встроенный runtime отдельно не опрашивались, внешний CLI Codex не использовался. Активные модель и режим в этой дочерней границе отдельно не проверялись.
- `fum-moskovskoye-vremya-rabochej-sessii` выдал согласованную пару 2026-09-11 02:06:54 MSK. `fum-struktura-papok-zaprosov` создал текущий каркас и навигацию; `fum-otchyotyi-o-zapuskakh-proverok` учитывает каждый прямой проверочный запуск; `fum-svezhestj-markdown` и `fum-svyaznostj-rabochej-sessii` обслуживают контрольную точку.
- [Братиславская проекция памяти](../../Инструменты/fum-bratislavskaya-proyekciya-pamyati/SKILL.md): прочитаны точный контракт, реализация и тесты. Проверки используют синтетические временные Git-деревья и подставной преобразователь; LinguisticKit не запускается.

## Происхождение и граница этапа

Это дочерний этап ранее начатой задачи FUM-STEP-0176, а не новое сообщение человека. Два первичных сообщения выше перенесены дословно из [первоначального запроса](../2026-09-11_01-28-44_MSK_перенести-исходники-FUMA/запрос.md). Предыдущая контрольная точка корня и база этого дерева — `aeae18cb146a34563ff39c84d9bc5ef59fffab91`. Корневой UUID передан координатором явно; собственный дочерний идентификатор не используется.

Координатор назначил одного писателя в отдельном worktree на `refs/heads/codex/форматы-приложения-FUMA-0176`. До первой записи физический корень, HEAD, symbolic ref и чистота дерева проверены. Интеграция ограничена четырьмя файлами автоматизации и текущим Журналом; общие индексы корень пересобирает у себя. Другие деревья, refs и общая Git-конфигурация доступны только для чтения.

Поручение: поддержать ровно шесть реально присутствующих расширений `.c`, `.h`, `.modulemap`, `.pbxproj`, `.plist`, `.entitlements` с действием `сохранить_байты` и один точный вложенный путь `Приложения/FUMA/macOS/.gitignore`. Технические суффиксы должны сохраняться при преобразовании имён. Неизвестные текстовые файлы по-прежнему закрывают план; `.xcscheme` и `.js` не добавляются. Правила агентов, файлы версии 1 и LinguisticKit не изменяются. Общий smoke-check, Swift и живое поколение проекции остаются корню. Разрешена проверенная контрольная точка с автором `FUM Писатель` и отправкой exact OID в собственную ветку.

По позднему уточнению координатора измерение 5 × 14000 вызовов достаточно: повтор профиля без последующего изменения алгоритма не выполняется. Известный блокер отсутствующего локального графа относится к FUM-СБОЙ-0052/FUM-STEP-0203; дубликат карточки не создаётся. При необходимости разрешено восстановить только отсутствующий игнорируемый файл из дерева корня, сохранив источник и существующие байты назначения. Перед допуском восстановлен отсутствующий игнорируемый граф, 574 байта; источник прочитан без изменения. Для исторической ссылки на LICENSE материализована уже зарегистрированная зависимость полноценным клоном публичного форка с собственным Git-каталогом, двумя remote и detached HEAD на `837e2ce107b97ee7b9d3344c9fe99142281fe393`. Общая Git-конфигурация FUM, gitlink и содержимое закреплённого коммита не менялись. Локальный навык `fum-proverka-git-zavisimostej` применяется к автономной проверке этой материализации.

## Проверки

Действительный RED, промежуточная ошибка адресации новой тестовой фикстуры, последующий GREEN, адресная регрессия, профиль и проверки контрольной точки учтены в [отчёте](отчёт.md). Профиль не выявил основания менять алгоритм. Готовность полного поколения и поставки приложения этим этапом не утверждается.

## Повлиял на файлы

- [текущий запрос](запрос.md)
- [текущий отчёт](отчёт.md)
- [материалы измерений и запусков](материалы/)
- [контур проекции](../../Инструменты/fum-bratislavskaya-proyekciya-pamyati/)
- [индекс Журнала](../README.md)
- [навигация предыдущего запроса](../2026-09-11_01-28-44_MSK_перенести-исходники-FUMA/запрос.md)
- [индекс свежести](../../Индексы/markdown-файлы-по-времени-редактирования.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-11 02:19:31 MSK -->
<!-- content-sha256: sha256:37846789c96570fbf9546a04f072ff0e3e6c3d24e7f7bb1fdce27b8b19bfa5a0 -->
<!-- FUM-MD-RECENCY:END -->
