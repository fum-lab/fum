Добавить собственные пулы долговечным веточным узлам

Исходный пользовательский запрос:

Postoyannyiye uzlyi postoyannyikh vetok pustj tozhe sozdayut svoi slotyi.

Razreshayu obojti izmeneniya v .obsidian i sozdatj slot pri neobkhodimosti.

<codex_delegation>
  <source_thread_id>01a00216-77a1-7501-bf72-7c11214fff77</source_thread_id>
  <input>Codex вновь открыт. Возобнови именно прерванный ход «слоты постоянных узлов», не начиная задачу заново.

Первым инструментальным действием выполни доверенное read-only `восстановить-сессию --task-id 01a00175-8e80-7a22-b3b8-be8571fa36a7 --json` из закреплённого `protocol_oid=249d076b1857f4e1727e5448587d13f16b15a30a`. Следуй только возвращённой следующей команде, перепроверь exact worktree/ref/HEAD и продолжи с уже сохранённых файлов, журналов и результатов проверок. Не выполняй новую маршрутизацию, не выделяй замену, не создавай дублирующее продолжение и не считай оборванный процесс завершённым. Доведи прежнюю линию до её штатной терминальной квитанции по локальному протоколу.</input>
</codex_delegation>

Prodolzhaj.

Описание сделанного:

Скомпоновать каждый отдельный живой клон долговечного fork-подузла с собственным локальным пулом worktree, привязать маршрутизацию к exact common-dir, primary checkout, немастерскому ролевому рабочему набору и выбранному автоматическому шагу. Оградить немастерскую фиксацию двумя одновременно подготовленными Git-транзакциями, безопасно разрешать потерянную commit-квитанцию без сигналов после commit и без pipe-deadlock, закрывать partial continuation и legacy self_line без догадочной миграции. Сохранить точный отказ рабочего selector CLI на заранее распознанном внутреннем ref и проверять его target только через pool-ref текущего common-dir, exact назначение, слот, FIFO, runtime-route, закреплённые путь рабочего набора, автоматический шаг и blob OID. Перевести новые собственные объявления кода одной проверяемой картой без изменения baseline-снимка. Сохранить один слот для точного продолжения, отделить runtime Подузлы/ от отслеживаемого пространства Исполнители/ и закрепить контракт в требованиях, документации, глоссарии и планировании как завершённую FUM-STEP-0149.

Codex-Thread-ID: 01a00175-8e80-7a22-b3b8-be8571fa36a7
