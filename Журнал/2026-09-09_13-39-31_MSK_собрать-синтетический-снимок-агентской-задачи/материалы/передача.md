# Точная передача синтетического сегмента 0159

Корневая задача: `01a07d3d-d376-7ad2-aafc-67e4c25a67eb`. Основание и граница: [запрос](../запрос.md), [отчёт](../отчёт.md). Это дочерний адресный результат для последующей корневой приёмки, не интеграция в master.

## Git и исходники

- Внешний локальный Swift-коммит: `dd172958b0128cba73361eeac136e8bc66190230`, полный ref `refs/heads/codex/observation-container-01a07d3d`. Remote отсутствует, публикации нет.
- Первый checkpoint нового пакета: `9e754878d20ebd5f02df24df2c44f69c4c532870`.
- Принятый контейнер: `dfdd1b65afd59666a696c29a024e0ca059497157`; его пакет не имеет diff. После итогового коммита независимое read-only-ревью подтвердило чистое дерево.
- [Итоговый манифест](исходники-итога.json): 17 файлов, включая 15 Swift-файлов, SHA-256 каждого. Добавлен только `Packages/СнимокАгентскойЗадачи`; существующее приложение не менялось.
- FUM: полный ref `refs/heads/codex/контейнер-наблюдений-01a07d3d`; исходный HEAD этого этапа `6dfe1870a9aa400dd595efabd7699c0b179d233a`; checkpoint `39a273896b3dd508153d8ef29a0b121b5a41dc35` точно доставлен в проверенный origin. Итоговый OID журнального коммита определяется содержащим этот файл коммитом и отдельно передаётся вместе с проверенным удалённым OID после push.

## Воспроизведение и свидетельства

- 40 терминальных записей [v4](запуски-проверок/), без активного хвоста. №39: 35 тестов/6 наборов GREEN, ноль отказов, XCTest 0,565 с; №40: точный staged diff внешнего пакета.
- №34 — итоговая release-сборка; №35 — [подтверждающий профиль](профиль-итога.json). Apple Swift 6.4, режим Swift 6, native SwiftPM, macOS arm64. Предупреждение о native build-system не скрывается; concurrency-предупреждений итоговый набор не дал.
- №36–38 — реальная запись 18 синтетических наблюдений и два независимых процесса replay. [JSON](пример-снимка.json) при записи и восстановлении совпал побайтово. [Markdown](пример-снимка.md) получен из той же модели; служебный FUM-recency не является частью CLI-вывода.
- Синтетический демонстрационный контейнер оставлен вне Git, в собственном пакете `.build/пример-снимка.c0V7Ix/сегмент.fumobs`; SHA-256 `c0449df6d9c143416f02f620296e3e72e350e2184cf88b994f4828da6ccf0999`.
- Профиль 256 событий: 597922 байта хранения; чистая сборка 21,933 мс, запись 640,598 мс, replay 576,585 мс, 32 повтора 3,715 мс/0 новых байт. SHA контейнера `596db8e0a06e4826ce8b4bbe28a9fb1a1bc8f58d493f01682b7d87b99f824632`; SHA снимка `083bae11a7571f6c9c436bae40cc5d92c8944f1c491bc86bcb1573dbb67ee448`.
- [Решение об оптимизации](профиль-и-решение.md) содержит baseline, исключённый пересечённый прогон, сравнение и границы причинного вывода.

CLI пакета: `пример`, `собрать <абсолютный-вход> <абсолютный-корень> <json|markdown>`, `восстановить <абсолютный-корень> <поставщик> <хост> <ID> <json|markdown>`, `профиль <новый-существующий-корень> <1..256>`. Полный контракт и команды воспроизведения находятся в README пакета; канонический записанный вход — `Примеры/пять-сценариев.json`.

## Не закрыто этим сегментом

Полные FUM-STEP-0159, FUM-STEP-0156 и FUM-REQ-0044 остаются открыты. Живые API/JSONL, Accessibility, ScreenCapture, датчики, разрешения и приложение FUMA не подключены. Свежесть реальных источников, подтверждение фактического состояния Codex, переносимость сериализации на все версии Foundation, устойчивость к потере питания и произвольные Unicode-ID не заявлены.

Общая приёмка FUM и актуальная проекция выполняются родительской задачей. Этот Журнал остаётся открытой терминальной контрольной точкой без полного smoke-check; 282 исторические ссылки на отсутствующий локальный graph.json — известная исходная граница, не устранённая созданием пользовательского состояния. Дословные запросы и старые сырые v4-записи сохранены. Разрешённый дочерний содержательный объём исчерпан; интеграция требует корневой проверки точных OID/байтов.

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-09 14:43:06 MSK -->
<!-- content-sha256: sha256:3082f200a1f2e030cc4a95820b7af0419dc6c2c01349fa2e505deda136b8a81c -->
<!-- FUM-MD-RECENCY:END -->
