# Карта переноса совместимости

Исходный M — `406c6ba1d0b3373403fefd14d5f7faf8e0665b7d`. Неизменный L — `a728283474931eda71cd581ca5429121124ba3f6`, дерево `bc258a41133107198602c60d003555898d4cfad2`. Итоговый diff вычисляется относительно M; данный этап не является кандидатом слияния.

1. `6599fe4837ef54efc7f871d2bfe6f8d9d07b4d95`: хунки форматов проектора, контракт и схема плана, прежний контракт, 13 регрессий и фикстура перехода. Сохранённый прежний контракт побайтово равен M; исходные blobs проектора, контракта, схемы и теста совпали с родителем источника.
2. `32cac61b3e90023ccfb854b5311fc0e4c37865d4`: конечная грамматика объявлений, 17 тестов и профиль. `8609003af7fdb6ef5dddf21c51cd6607ddb34088`: единственный путь адаптера в контракте и проекторе, зависимость от проверки объявлений, восемь тестов. Общий мост старой политики включён один раз вместе с форматами. Сам исполняемый `адаптер-codex.js` из L не переносится: принимающий контур проверяет его как данные кандидата. Произвольные JS, MJS и CJS не допускаются.
3. `0246844fe15ba51e48327005b33bc78b668f813a`: два изменения парсера точного маркера отсутствия связей и шесть регрессий. Исходный blob парсера M совпал с родителем источника.
4. `c14b2dee156a5a07d22addf187f06980c1f501bc`: вызов принимающей проверки и зависимость `расширение_шаблонов.py`, 19 исходных тестов и профиль. Описание результата `acab107170a4a1243b76cba4f25b0b408e735603` — происхождение. Дополнительно исправлен подтверждённый [пробел предков пути](../../../Сбои/FUM-СБОЙ-0076-пропуск-проверки-предков-каталога-типов.md), добавлены восемь адресных тестов. Установка новых типов, генератор начала сессии и транзакционный приём направлений не переносились.
5. `28f51c58fa8df4d20d33ef2f05dab758cb7a6f83`: предикат и применение в связности, четыре теста и профиль; исходный blob совпал с M. `7acc2de8ca1dcbefd82c16faecd1c31bdfa6e648`: только пять зависимых хунков графа в проекторе, семь тестов и профиль. Полный проектор L поверх M не копировался. Точный ignored `.obsidian/graph.json` в своём дереве отсутствует; пользовательское состояние не создавалось и не менялось.
6. Точная обычная политика L служит кандидатной: 419 записей, SHA-256 `4a73ad60ca3c8eee704aecff2a33481c642a4633ea3a9622adb23a5fb4ef8cb0`. Прежние 350 сохранены дословно; 69 добавлений сопоставляются с L по полям и фактическим находкам scanner. Промежуточные лишние две записи не переносились. В обычной политике M прежние 350 дополнены только `0176-projection-foreign-fixture`, пересчитанной штатным `obnovitj-policy.py`.

Высокий контур слияния, свидетельство исполнения, закрытый читатель, продвижение, стандартный профиль, строгий scanner и gitlink LinguisticKit оставлены исходными. Новые исполняемые зависимости остаются внутри рекурсивно сверяемой области `Инструменты`; проверяющий источник и данные кандидата различаются. Ни одна прежняя приёмка L или отдельного пакета не подменяет проверку этого diff.

Собственные измерения и ограничения перечислены в [отчёте](../отчёт.md), исходные команды — в [запросе](../запрос.md).

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-11 15:19:56 MSK -->
<!-- content-sha256: sha256:701eb0027364d124d42d706f45be28e9114f92a39ccb5cfa889c1b223e784391 -->
<!-- FUM-MD-RECENCY:END -->
