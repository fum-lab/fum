# Отчёт 2026-09-15 15:57:32 MSK - Уточнить границы Swift оболочки

Swift-оболочка FUMA должна охватывать оба потока обращений Codex CLI: запросы к API модели и инициируемые CLI вызовы инструментов и ОС. Это уточнение функциональной границы, а не уже подключённый перехват. Исходный вопрос, ответ «Оба потока» и вопрос о сборке сохранены дословно в запросе.

## Профиль времени выполнения

| Стадия | Длительность | Границы и способ измерения |
| --- | --- | --- |
| Адресное восстановление команд | не измерено | Прочитаны три точных диапазона JSONL |
| Проверка наличия сборки | не измерено | Прочитаны метаданные bundle и наличие исполняемого файла, без запуска |
| Подготовка постановки | не измерено | Уточнён контракт и передано продолжение единственному писателю планирования |

Граница профиля: документационный этап после контрольного коммита `7388ca9cb06fd2a6b5143b32640056a4145368cd`. Сборка приложения и время его работы сюда не включены.

### Прямые запуски проверок

<!-- FUM-CHECK-RUNS:BEGIN состояние=открыт; каталог=материалы/запуски-проверок -->

| Вызов                                              | Длительность | Результат |
| -------------------------------------------------- | ------------ | --------- |
| [корень] Проверить формат уточнения Swift оболочки | 0,07 с       | успешно   |

Общее время прямых запусков проверок: 0,07 с.

Приёмочные раунды: не готов.
Контекст Git-снимка: sha256:cffa7573075e14fceb5870bb0d82cc6f1a06b58bccde9c061a2ad37e1a1c19cc.
Контекст содержимого: sha256:b5e04e162542a3106964af498bea526c0f2102938b5a06bda277c50c1d603496.
Полных попыток: 0; успешных: 0.
Условие «переход не заменяет изменение содержимого»: выполнено.
Условие «нет активных»: выполнено.
Условие «финальная полная последняя»: не выполнено.
Условие «финальная полная успешна»: не выполнено.
Условие «снимок совпадает»: не выполнено.
Условие «содержимое совпадает»: не выполнено.
Условие «нет повторных полных попыток»: выполнено.
Условие «локализации связаны с предшествующим отказом»: выполнено.
Условие «нет запрещённых перекрытий»: выполнено.
Условие «непокрытые диагностики успешны»: выполнено.
Условие «исторические нарушения отсутствуют»: выполнено.

<!-- FUM-CHECK-RUNS:END -->

## Проверки

Контрольная точка `7388ca9cb06fd2a6b5143b32640056a4145368cd` опубликована обычным push; удалённый OID подтверждён. Она сохраняет предыдущие 11 команд и не включает последующие уточнения этого этапа. После коммита guard вернул код 3: работа продолжается, приоритет остаётся за обработкой контекста.

## Ответ о сборке FUMA

В стандартном системном каталоге приложений обнаружен установленный `FUM.app`: идентификатор `fum.app`, версия `0.1`, номер сборки `1`, исполняемый файл `FUM` размером 3 121 536 байт. Его время изменения — `2026-06-16T07:59:42.538101+00:00`; это файловое наблюдение, не доказанная дата компиляции. Приложение не запускалось и его соответствие нынешнему Git-снимку не проверялось.

Исходники доступны в [каталоге FUMA](../../Приложения/FUMA/README.md), приложение — в [каталоге macOS](../../Приложения/FUMA/macOS/README.md), где предусмотрены SwiftPM и Xcode. Четыре отдельных SwiftPM-пакета пока не подключены к приложению. Единая проверенная сборка с описанной Swift-оболочкой Codex, обоими потоками вызовов и операторным диагностическим GUI этим этапом не создана. Сохранённые руководства уже подтверждают проверки прежних исходников из чистого клона: приложение через SwiftPM и Xcode, четыре Release-сборки пакетов и 133 теста. Корень не повторял эти сборки сейчас. Остаётся собрать и проверить именно новую объединённую версию с подключёнными пакетами и оболочкой Codex, затем отдельно подготовить её установку.

## Решения и ограничения

- Внешнее управление Codex и наблюдение его stdout не доказывают охват инициируемых им API-вызовов. Нужны точки подключения модельного транспорта и исполнителей инструментов с явным перечнем покрытия.
- Уточнение передано отдельной задаче `01a08d77-2060-7701-9f44-ff04769d8a6e` как второй малый этап в `planirovaniye`. Текущий проверяемый индекс не заменяется каждым новым сообщением.
- Общий захват stdout/stderr развивается независимо в задаче контекста; новая Swift-оболочка не приписывается этому локальному CLI.
- В старой ветке планирования обнаружена проверка исторических ссылок на необязательный локальный граф. По действующему правилу 000178 согласован узкий временный обход только этой диагностики с сохранением реального отказа; код проверки, граф и правила не меняются. Остальные проверки сохраняются, полная приёмка не заявляется.
- Первый вызов создания этой папки отказал до записи из-за несовпадения заголовка с производным каноническим именем. Исправлен только аргумент заголовка, повтор успешно создал папку.

## Постановка переименования приложения

Пользователь согласовал оба имени: SwiftPM-пакет `FUMMacOSOrgans` переименовать в `FUMA`, продукт приложения `FUM.app` — в `FUMA.app`. Исполнение будет отдельным ограниченным изменением настроек и соответствующих команд сборки. Прежняя установленная копия этим этапом не переименовывается и не запускается. Задача, переносившая приложение в монорепозиторий, получила пока read-only подготовку; пишущий этап начинается от коммита этой постановки. Приоритет обработки контекста сохраняется.

Происхождение: команда — `[752194319, 752194763)`, SHA-256 `69be86b4c137db6140c74653ad7df2fb276a326ea683636297c98a2cd6f8a4f2`; подтверждение обоих имён — `[752237175, 752237992)`, SHA-256 `633dbdfae3a63b98b25e2090739e3162ceca26a7a8cb037c5887c4b8cb27c1bd` корневой JSONL.

## Происхождение

Корневая JSONL: сообщение об оболочке — байты `[751476653, 751477131)`, SHA-256 `18b890edbfe1749fdfdf0c20335cf453794762181c7c0b4d52b1c73f5efc87f8`; ответ на уточнение — `[751511866, 751512672)`, SHA-256 `9c705db0169f1d4a337a4ae7d80d7c833b6d95c26d0bb2eca04675a333b7c170`; вопрос о сборке — `[751635669, 751636087)`, SHA-256 `216eb45aed8b7af002aadda88ef119bef2d07422820eb2f3ff045990bf9b957f`. Приватные локальные пути не включены в публичные материалы.

## Источники

- [Исходный запрос](запрос.md).

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-09-15 16:11:39 MSK -->
<!-- content-sha256: sha256:12df3992f8966458c225b80ee873b29a91f02ea56c7d0da924e81c75b8cea2c3 -->
<!-- FUM-MD-RECENCY:END -->
