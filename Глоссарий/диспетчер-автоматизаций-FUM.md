# Диспетчер автоматизаций FUM

Диспетчер автоматизаций FUM — постоянный управляющий контур одной прикреплённой задачи Codex, одной рабочей копии и одной именованной Git-ветки. Его [локальный контракт](../Инструменты/fum-dispetcher-avtomatizacij-fum/SKILL.md) описывает закрытый веточный реестр схемы `1`, расписания, пороги подтверждённых событий, условия, состояния, классы эффектов, исполнителей, fence и курсоры. Канонический реестр `master` содержит ровно одно активное задание `master.next-step`: оно закрепляет текущий checkout и `refs/heads/master` и вызывает специализированный адаптер следующего шага, не копируя задачу и критерии карточки.

Диспетчер разделяет управление и исполнение. Сам плановый тик без FIFO-билета наблюдает, выбирает и выполняет только fenced-операции общего слоя и адаптера; изменение репозитория или внешнего состояния поручается обычной корневой задаче через FIFO-очередь. Отдельные пользовательские исключения — подтверждённый штатный сброс из постоянной задачи диспетчера и отдельный корневой человеческий break-glass `./sbrositj.sh`; heartbeat не вызывает ни один из них. После допуска обычный исполнитель подтверждает общий `bind-run`/`verify-run` и собственный адаптерный fence по фактическому `CODEX_THREAD_ID`, поколению очереди и базовой вершине. Создание задачи не является успехом: следующий тик завершает запуск по точному текущему `last_completion` либо по самодостаточной квитанции штатного сброса, доказавшей неизменившиеся reservation-ref и OID.

До `heartbeat-status`, выхода по занятой FIFO и выбора нового запуска диспетчер проверяет, не требует ли уже связанный запуск ограждённого возобновления. Допускается только та же exact-задача со статусом `idle` либо `notLoaded`, точным `read_thread` схемы `1`, ровно одним новейшим терминальным ходом `failed` и exact-ошибкой разрыва потока ответа. Терминальность этого host-хода не терминализирует логический запуск: его нетерминальность отдельно доказывают резервация, FIFO-владение, run-fence и отсутствие точного завершения.

Одна Git-CAS-транзакция до `send_message_to_thread` проверяет `HEAD`, общую резервацию, очередь, claim и management-fence, переводит резервацию в схему `4` и записывает ровно одну попытку в фазе `вызов_мог_состояться`. Только победитель этой транзакции адресует одно детерминированное сообщение прежней задаче; любой ответ, ошибка или тайм-аут запрещают повтор и создание замены в том же и следующих тиках. Точный прежний исполнитель первым инструментальным действием идемпотентно входит в тот же FIFO-билет, перечитывает актуальные fence и контрольную точку, затем подтверждает ключ возобновления. Пока подтверждения нет, pending-состояние блокирует терминализацию запуска и штатный сброс. Этот профиль доказывает разрыв потока и текущую доступность host-пути чтения, но не гибернацию, пробуждение машины или доступность всей сети.

Сообщения в прикреплённую задачу служат человекочитаемым интерфейсом закрытых намерений просмотра и настройки. Просмотр возвращает канонический обезличенный список поколений, триггеров, условий, состояний, подтверждённых курсоров и блокировок. Обычная мутация проходит две фазы: свободный текст преобразуется в структурное предложение с ожидаемыми поколениями, точным diff, классом эффекта и подтверждениями, а затем только это предложение применяется по CAS после обычного FIFO-допуска. Возобновить задание можно только из `paused`; выход из `blocked` является отдельным намерением со специальным safety-подтверждением. Отмена создаёт лишь no-op-конверт рабочего процесса, не откатывает изменение и не отзывает исходное предложение. Любое изменение подтверждённого курсора и расширение внешнего эффекта требуют отдельных подтверждений. Смена remote или полного ref не поддерживается схемой `1`: для неё нужны будущая версионированная миграция и отдельное подтверждение.

Штатный сброс использует собственный подтверждённый план и фазовый `reset-record` в `queue-ref`: он закрепляет физический checkout, полный ref локальной именованной ветки, точный `HEAD`, объект очереди, процессную и host-идентичность диспетчера, участников, изменённые пути, preimage/target каждого изменённого tracked-пути, служебные ссылки и общую эпоху резерваций и блокирует обычные переходы до исхода. Специальный untracked-тип, скрытые index-флаги, изменённая checkout-политика, внешний filter, ignored-target-коллизия и переход gitlink закрывают операцию до потери данных. Резервация схемы `3` различает exact `threadId`/`hostId` и предварительный `clientThreadId`, а exact-свидетельство обязано совпасть с будущим `task_id`. Текущий host не даёт чистый stop произвольной живой задачи, поэтому `active`, неизвестный, неполный или неоднозначный preflight блокирует подготовку до первой мутации, а `handoff` не заменяет остановку. `Apply` отказывает на грязном tracked submodule или любой неотслеживаемой границе вложенного обычного либо bare Git-репозитория и повторяет частично применённый `read-tree` только при целом индексе в preimage либо target и каждом tracked-пути в собственном preimage либо target; позднее tracked-, index- или untracked-расхождение блокирует дальнейшее удаление. Каждый подтверждённый обычный файл или символическая ссылка, включая такие пути `.obsidian/`, проверяется непосредственно перед собственным удалением; target-путь не обходится через символическую ссылку, игнорируемые и новые объекты сохраняются. До финала продолжение хранит reset-record; финал атомарно создаёт самодостаточную квитанцию для последующей терминализации точной сохранённой резервации с совпавшими reservation-ref/OID и recovery `released` либо `unclaimed` даже после Git GC, повторного сброса или смены `last_completion`. Recovery сначала доказывает текущий reset-id и может сохранить exact более раннее `committed`-свидетельство той же резервации, но не принимает только повторно использованный идентификатор или время. Живая приёмка остановки остаётся открытой. После успешного `apply` вызвавший ход больше ничего не пишет и не запускает.

Корневой `./sbrositj.sh` — иной, намеренно более сильный break-glass. Он работает только при настоящих TTY одновременно на stdin и stdout, показывает план для exact текущих именованной ветки и `HEAD` и требует ввести напечатанную динамическую фразу. После подтверждения он архивирует и аннулирует сырой branch- и checkout-scoped runtime-снимок, безопасно восстанавливает отслеживаемое дерево и индекс к `HEAD`, создаёт свежую пустую очередь, новую эпоху и границу перезапуска с checkout-scoped tombstone-свидетельствами. Валидные official reset receipts в `refs/fum/квитанции-сброса-состояния-FIFO/` он строго парсит и сохраняет; повреждённую официальную квитанцию архивирует и удаляет, а собственную квитанцию пишет отдельно в `refs/fum/квитанции-простого-сброса/`, откуда точный terminal replay восстанавливает уже состоявшийся исход без новой мутации. Игнорируемые объекты, вложенные Git-репозитории и чужие runtime-области он не удаляет. Сброс не останавливает host-задачи и не запускает карточку сам: следующий обычный heartbeat при `ACTIVE` и наблюдаемом host-простое заново вычисляет selector из того же `HEAD`. Старые задачи могут физически продолжаться, но их добросовестные поздние записи отклоняются новой границей даже после смены ветки в том же checkout.

Начало возобновления переводит exact-связанную резервацию из схемы `3` в схему `4`, не меняя `threadId`, `hostId` и `task_id`. Пока её состояние `вызов_мог_состояться` не подтверждено точным исполнителем, штатный сброс отклоняет такую резервацию как неразрешимую host-неоднозначность.

Management-fence хранится во внутренней служебной Git-ссылке. Перед clean-восстановлением heartbeat получает снимок этой ссылки и передаёт FIFO точные ссылку и ожидаемый объект; одна Git-транзакция проверяет management-ref и queue ref, поэтому встречная перенастройка не может быть самоисключена тиком. Повторная проверка перед общей резервацией не позволяет создать задание из устаревшего снимка.

Диспетчер не является Git-[веткой работы](ветка-работы.md), хотя обслуживает её полный ref. Он также не совпадает со [следующим шагом ветки](следующий-шаг-ветки.md): следующий шаг остаётся одним источником работы и первым специализированным адаптером. Прежние прикреплённая задача и heartbeat мигрированы на месте с сохранением цели, расписания и истории; второй экземпляр не создан. Предложение `Stop` или `Start` закрепляет наблюдённый исходный host-статус; helper сверяет его с полным снимком, меняет только runtime-статус, а результат подтверждается readback и чистой передачей FIFO. Переключение не освобождает уже созданный запуск или claim. Ручной `push` публикует накопленный проверенный локальный префикс `master`, но не подтверждает каждую карточку и не расширяет полномочия диспетчера. Инфраструктурный диспетчер документационного прототипа не равен и продуктовому [фоновому заданию FUM](фоновое-задание-FUM.md), для которого нужны собственный runtime, бюджеты, приоритеты и вытеснение пользовательским вводом. Канонический реестр сохраняет одно активное `master.next-step`; подключение только первого адаптера не завершает универсальный контур для аналитики и других типов заданий.

## Связанные документы

- [Диспетчер автоматизаций FUM](../Документация/45-диспетчер-автоматизаций-FUM.md)
- [Воспроизводимые автоматизации FUM](../Документация/17-воспроизводимые-автоматизации.md)
- [Требование универсальной диспетчеризации](../Требования/🚧-универсальная-диспетчеризация-периодических-автоматизаций.md)
- [Требование штатного сброса FIFO-очереди и рабочей копии](../Требования/🚧-штатный-сброс-FIFO-очереди-и-рабочей-копии.md)
- [Требование подтверждаемого ручного сброса FIFO и автозапуска](../Требования/✅-подтверждаемый-ручной-сброс-FIFO-и-автозапуска-к-текущему-HEAD.md)
- [FUM-STEP-0142 — ограждённое возобновление задач](../Планирование/карточки-шагов/🟡-FUM-STEP-0142-добавить-ограждённое-возобновление-задач-после-потери-связи.md)

## Источники требований

- [исходный запрос 2026-08-10 10:19:59 MSK — Добавить простой сброс FIFO к текущему HEAD](../Журнал/2026-08-10_10-19-59_MSK_добавить-простой-сброс-FIFO-к-текущему-HEAD/запрос.md)
- [исходный запрос 2026-08-08 18:57:20 MSK — Добавить ограждённое возобновление после разрыва связи](../Журнал/2026-08-08_18-57-20_MSK_добавить-ограждённое-возобновление-после-разрыва-связи/запрос.md)
- [исходный запрос 2026-08-07 20:34:22 MSK — Добавить штатный сброс очереди](../Журнал/2026-08-07_20-34-22_MSK_добавить-штатный-сброс-очереди/запрос.md)
- [исходный запрос 2026-08-06 06:59:01 MSK — Добавить управление диспетчером через сообщения](../Журнал/2026-08-06_06-59-01_MSK_добавить-управление-диспетчером-через-сообщения/запрос.md)
- [исходный запрос 2026-08-05 12:02:53 MSK — Перенести автозапуск шагов в универсальный диспетчер](../Журнал/2026-08-05_12-02-53_MSK_перенести-автозапуск-шагов-в-универсальный-диспетчер/запрос.md)
- [исходный запрос 2026-08-05 09:07:08 MSK — Добавить универсальный выбор и защищённую резервацию запуска](../Журнал/2026-08-05_09-07-08_MSK_добавить-универсальный-выбор-и-защищённую-резервацию-запуска/запрос.md)
- [исходный запрос 2026-08-05 05:48:39 MSK — Закрепить контракт универсального диспетчера автоматизаций FUM](../Журнал/2026-08-05_05-48-39_MSK_закрепить-контракт-универсального-диспетчера-автоматизаций-FUM/запрос.md)
- [исходный запрос 2026-07-27 15:21:35 MSK — Сделать диспетчер автоматизаций ветки универсальным](../Журнал/2026-07-27_15-21-35_MSK_сделать-диспетчер-автоматизаций-ветки-универсальным/запрос.md)


<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-10 13:02:52 MSK -->
<!-- content-sha256: sha256:44015a2576b428440a89b5e99d9845554b1a0153f42829459a5f6bf08287cde4 -->
<!-- FUM-MD-RECENCY:END -->
