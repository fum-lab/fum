# Следующий шаг ветки

Следующий шаг ветки — версионный набор кандидатов на продолжение конкретной именованной [ветки работы](ветка-работы.md). Веточная запись содержит полный ref ветки, состояние `open` или `done`, проект и отдельные защищённые хэшем записи актуальных [карточек шагов](карточка-шага.md); задача, критерии завершения и источники читаются из самих карточек.

Следующий шаг ветки отличается от [предложения о следующем шаге](предложение-о-следующем-шаге.md): общий пул может содержать много нераспределённых карточек, а веточный набор явно удерживает только конечный whitelist относящихся к этой ветке кандидатов. Открытый набор схемы `5` хранит `dispatch = automatic | paused | blocked` и точный ALL-of-массив `requires_completed_card_ids` каждого кандидата. `automatic` не является заранее записанным `ready`: при каждом автозапуске он вычисляется как готовый, только если все обязательные карточки имеют literal-статус `completed`; иначе его runtime-статус равен `paused`. Явные `paused` и `blocked` автоматически не открываются. Корректные неготовые записи не участвуют в ранжировании и не мешают независимому `ready`; состояние `done` означает пустой набор кандидатов.

Каждая кандидатная запись имеет собственный `step_id`, режим диспетчеризации, идентификатор карточки, хэш её содержания и массив машинных зависимостей. Смена карточки, пути, хэша, `dispatch`, зависимостей или условия возобновления создаёт новое поколение `step_id`. Изменение только наблюдаемого жизненного статуса обязательной карточки сохраняет `step_id`, но меняет вычисленный пул и `selection.id`. Свободный `resume_condition` не интерпретируется: выполнение немашинного условия явной паузы или блокировки требует нового поколения. Завершённая сессия обновляет whitelist допустимости, но не вычисляет и не предвыбирает следующего исполнителя.

Конкретный шаг выбирается только при запуске после второй host-инвентаризации. Политика `dynamic-readiness-source-history-first-parent-v2` сначала вычисляет готовность всего whitelist, затем читает из точного `HEAD` окно не более 16 first-parent-коммитов и сопоставляет их только с дедуплицированными нормализованными локальными ссылками раздела `Источники` каждого runtime-`ready`: сначала с изменённой завершённой или поглощённой карточкой, затем с точно изменённым иным источником. Управляющие пути и собственная карточка кандидата исключаются; внутри класса учитываются меньшая дистанция, большее число уникальных совпавших путей и затем `card_id`, `step_id`, а отсутствие различающего сигнала сразу даёт этот устойчивый порядок. Связанность истории остаётся мягкой: она не отменяет вычисленную неготовность, явную паузу или блокировку, безопасность, полномочия, выбор пользователя и контекстную посильность.

Результат выбора закрепляет объект `selection`: идентичность `id`, политику `policy`, точный `head`, `ready_count` и свидетельство `reason`, `commit`, `distance`, `matched_paths`. Идентичность хэширует объявления и вычисленные статусы всех кандидатов, незавершённые зависимости и точные пути, жизненные статусы и хэши обязательных карточек вместе с готовым пулом и ранжированием. Claim получает `selection.id` через `--expected-selection-id`, атомарно проверяет вершину полного ref вместе с compare-and-swap и сохраняет `selection_id`, `selection_head` и lease логической попытки в записи схемы `2`. После создания задачи точный `bind-run` связывает claim с её неизменяемым `task_id` в схеме `3`, а первый `verify-run` атомарно закрепляет подтверждённые FIFO-поколение и базовую вершину в схеме `4`. Каждый допуск требует повторной проверки той же задачи, очереди, ветки, выбора и claim до содержательной работы. Непрозрачные значения lease и внутренних поколений этого fence не публикуются.

Полный чистый откат внутри того же допуска может перевооружить прежний выбор для нового запуска только привязанной к запуску операцией `rearm`: она до `finish-clean` атомарно сверяет владельца очереди, ветку и claim схемы `4` и идемпотентна лишь для собственной попытки. После успешного `rearm` разрешён только немедленный чистый handoff, а после handoff любые `rearm` и `release` запрещены. Успешно созданная живая задача не вызывает `release`. Эта операция остаётся отдельным внешним восстановлением любой читаемой схемы явно не созданной или неоднозначно созданной задачи только после host-подтверждения окончательной остановки возможного исполнителя. Любое структурное, содержательное, зависимостное, вершинное или поколенческое расхождение закрывает диспетчеризацию по принципу fail-closed.

Записи хранятся в [Планирование/следующие-шаги-веток](../Планирование/следующие-шаги-веток/README.md) и проверяются локальной автоматизацией [fum-sleduyusjhij-shag-vetki](../Инструменты/fum-sleduyusjhij-shag-vetki/SKILL.md).

В целевой архитектуре этот контракт остаётся специализированным источником работы для [диспетчера автоматизаций FUM](диспетчер-автоматизаций-FUM.md). Общий диспетчер не переносит в свой реестр задачу или критерии карточки и не заменяет карточочный `step_id` собственным поколением задания: оба уровня идентичности проверяются раздельно.

## Связанные документы

- [Диспетчер автоматизаций FUM](../Документация/45-диспетчер-автоматизаций-FUM.md)
- [Проекты](../Проекты/README.md)
- [Параллельная работа и слияние](../Документация/04-параллельная-работа-и-слияние.md)
- [Воспроизводимые автоматизации FUM](../Документация/17-воспроизводимые-автоматизации.md)

## Источники требований

- [исходный запрос 2026-08-01 09:16:33 MSK — Исправить повторный автозапуск после отката](../Запросы/2026-08-01_09-16-33_MSK_исправить-повторный-автозапуск-после-отката.md)
- [исходный запрос 2026-07-29 09:04:03 MSK — Расширить динамический выбор следующего шага](../Запросы/2026-07-29_09-04-03_MSK_расширить-динамический-выбор-следующего-шага.md)
- [исходный запрос 2026-07-27 18:28:42 MSK — Выбирать следующий шаг при запуске с учётом истории коммитов](../Запросы/2026-07-27_18-28-42_MSK_выбирать-следующий-шаг-при-запуске-с-учётом-истории-коммитов.md)
- [исходный запрос 2026-07-27 15:21:35 MSK — Сделать диспетчер автоматизаций ветки универсальным](../Запросы/2026-07-27_15-21-35_MSK_сделать-диспетчер-автоматизаций-ветки-универсальным.md)
- [исходный запрос 2026-07-20 20:06:04 MSK - Запускать следующие шаги веток](../Запросы/2026-07-20_20-06-04_MSK_запускать-следующие-шаги-веток.md)
- [исходный запрос 2026-07-22 02:59:22 MSK - Декомпозировать предложения на карточки шагов](../Запросы/2026-07-22_02-59-22_MSK_декомпозировать-предложения-на-карточки-шагов.md)
- [исходный запрос 2026-07-22 03:38:35 MSK - Разрешить выполнение доступных карточек шагов](../Запросы/2026-07-22_03-38-35_MSK_разрешить-выполнение-доступных-карточек-шагов.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-01 11:10:43 MSK -->
<!-- content-sha256: sha256:c5351628a46be34f244792738034362d2e827bcc1b8eeb516709cc4caf7c69be -->
<!-- FUM-MD-RECENCY:END -->
