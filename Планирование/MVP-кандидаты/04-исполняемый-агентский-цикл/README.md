# MVP-кандидат: исполняемый агентский цикл

Эксплуатационный статус репозиторного прототипа: действующая запись выполняется вручную запускаемыми последовательными сессиями. Упомянутые ниже автоматически продолжаемый цикл, FIFO и handoff сохранены как прежний экспериментальный слой и не являются текущим способом запуска; продуктовая идея MVP остаётся целевой.

## Паспорт

- Статус: [MVP-кандидат](../../../Глоссарий/MVP-кандидат.md).
- Горизонты дорожной карты: [исполняемый агентский цикл](../../дорожная-карта.md) и [эволюционные цепочки](../../дорожная-карта.md).
- Пользователь: разработчик или исследователь FUM, которому нужен не только ответ агента, но и проверяемая трасса работы.
- Минимальный результат: локальный runtime одного ограниченного [агентского цикла](../../../Глоссарий/агентский-цикл.md), где цель, наблюдения, действия, проверки, состояние, итог и признаки продуктивности цепочки сохраняются как структурированный артефакт памяти.

## Продуктовая идея для запуска

Продукт: **Трассировщик агентского прогона FUM** - локальный исполнитель одной ограниченной задачи с наблюдаемой трассой действий и проверок.

Первый пользователь - разработчик FUM, который хочет увидеть не только финальный ответ агента, но и пригодный для анализа след: какая цель была поставлена, какие действия разрешены, что реально выполнено, какие проверки прошли и где цикл остановился.

До появления собственного runtime текущая связка Git + Codex уже проверяет поведение автоматически продолжаемого цикла на более крупной гранулярности. Заранее созданная задача-продолжение точной ветки, прямой выбор следующего шага, FIFO-допуск и атомарная передача связывают дискретные задачи и коммиты, а пользовательская задача может изменить дальнейшую наблюдаемую траекторию. Этот действующий контур является поведенческим прототипом MVP, но не реализацией команды `fum run`: допущенная задача не получает от FIFO немедленного вытеснения, а ввод внутри неё ещё не наблюдается как событийный поток.

[Безоконный Swift-прототип воспроизводимого пополнения памяти](../../../Прототипы/воспроизводимое-пополнение-памяти/README.md) проверяет более узкий внутренний слой будущего `fum run`: версионированные события проходят через ограниченные операции к каноническим снимку и трассе. Он не ставит цель агенту, не выбирает действия и не реализует цикл наблюдения, поэтому подтверждает воспроизводимую память исполнения, но не готовность продуктового runtime.

Поверх этих слоёв завершён [узкий инженерный эксперимент одного собственного runtime-сценария](../../../Прототипы/живой-одноагентный-эпизод/Отчёты/2026-08-01_19-37-43_MSK_живой-прогон-одноагентного-эпизода.md). Один версионный паспорт связывает синтетическую цель и контекст с model-only-провайдером, бюджетом, раскрытием данных, единственным разрешённым Git-действием, проверками и терминальными исходами. Тот же runtime провёл автономную recorded-фикстуру и один opt-in живой локальный прогон: сравнил две модельные ветви от общего предка, отказал третьему вызову по бюджету без обращения к провайдеру, дождался отдельного внешнего подтверждения, создал кандидатный коммит в изолированной ветке, получил независимую приёмку и после двух фактических `SIGKILL` продолжил работу новыми процессами только из подтверждённого `CURRENT`.

Этот результат проверяет один заранее закреплённый сценарий, а не готовность команды `fum run`, общего или продуктового runtime. Он не разрешает произвольные actions, распределённое исполнение, фоновые задания, непрерывный приём пользовательского ввода и не доказывает преимущество FUM над контрольным агентом.

Первый сценарий запуска: пользователь задаёт цель и локальный сценарий с разрешёнными действиями. Трассировщик создаёт структурированный файл прогона, выполняет маленькую задачу на локальных данных, сохраняет наблюдения после каждого действия, фиксирует ошибки инструментов и завершает прогон итогом с проверками.

Первый сценарий собственного runtime не должен скрыто зависеть от внешнего агентского цикла. Если в прогоне появляется модельный шаг, он должен быть описан как отдельный провайдер: локальная LLM, проверяемая заглушка или доказанный режим `Codex CLI`, где инструмент используется как простой LLM-провайдер, а не как самостоятельный цикл наблюдения, действий и проверок. Эта граница не отменяет ценность внешнего Git + Codex-контура как поведенческого прототипа на масштабе дискретных задач.

Состав первого релиза:

- команда или сценарий `fum run <scenario>`;
- безоконные интерфейсы осмотра, статуса, возобновления, воспроизведения и приёмки эпизода без обязательной привязки к этим точным именам команд;
- простой формат сценария с целью, входными файлами, разрешёнными действиями и ожидаемым результатом;
- allowlist первых действий: чтение файлов, создание отчёта и запуск локальной проверки;
- машинно читаемая трасса и человекочитаемое резюме;
- кандидатный коммит в изолированной ветке с отдельной проверкой и явной приёмкой;
- фикстура, где воспроизведение принятого эпизода даёт то же состояние, а повторное живое исполнение оформляется как новый сравниваемый эпизод.

Критерий готовности продуктового запуска шире завершённого эксперимента: демонстрационный прогон должен использовать будущий пользовательский формат сценария и поддержанный набор действий, оставлять полную трассу наблюдаемых действий и показывать, какие шаги дали проверяемую пользу, а какие были остановлены или завершились ошибкой.

## Почему это может быть первым MVP

Этот кандидат ближе всего к целевому образу FUM как агента, а не только репозитория. Он отвечает на вопрос: какой минимальный цикл уже можно исполнять так, чтобы он оставлял след, был проверяем и мог становиться материалом для следующего цикла.

Однако это более сложный кандидат, чем память сессии или глоссарный контур. Его лучше строить на уже проверенных механизмах фиксации запросов, журналов, ссылок и локальных автоматизаций.

## Проверяемый MVP

Минимальный вариант должен уметь:

- принять цель и стартовый контекст из локальной памяти;
- зафиксировать начальное состояние цикла в структурированном файле;
- явно записать контракт модельного шага или отсутствие реального LLM-провайдера в первом прогоне;
- выполнить ограниченный набор разрешённых действий, например чтение файлов, создание отчёта или запуск локальной проверки;
- сохранить наблюдение после каждого действия;
- явно записать решение о продолжении, остановке или запросе подтверждения;
- при ожидании подтверждения закрыть только точный ожидающий переход и, если модельная среда и её бюджет независимо разрешены, продолжить безопасную модельную работу от зафиксированного общего предка;
- сохранить итоговый результат, проверки и связь с изменёнными файлами;
- воспроизводить принятое состояние из версионированных входов, а после появления долговременных поколений получать одинаковый канонический результат из полного replay и продолжения от подтверждённого поколения.
- после принудительного прерывания продолжить работу новым процессом из последнего подтверждённого поколения без скрытого контекста прежней сессии.

## Критерии приёмки

- Каждый шаг цикла имеет тип: наблюдение, намерение, действие, результат проверки или остановка.
- Цикл не выполняет внешние действия без явного разрешённого адаптера.
- Ошибка инструмента сохраняется как наблюдение, а не теряется в терминальном выводе.
- Итог можно прочитать как человекочитаемый отчёт и как машинно обрабатываемую трассу.
- Кандидатный коммит не становится принятой истиной без отдельного протокола проверки и приёмки.
- Модельное продолжение не выполняет ожидающий внешний эффект, не увеличивает разрешения из-за молчания пользователя и отделяет модельные статусы `selected_in_model` и `recommended` от свидетельств `transition_user_confirmed`, `authorized`, `preflight_passed`, `executed` и `observed`.
- Идентичность провайдера, локальность или удалённость, раскрытие данных и пределы вызовов, токенов, времени, вычислений и денег явно заданы до эпизода; ожидание не расширяет эти пределы.
- При достаточном бюджете минимум две содержательно различимые ветви имеют один точный общий предок, явные дельты и одинаково применимые проверки; при бюджете только на одну ветвь трасса сохраняет непроверенные альтернативы и не объявляет неоднозначность устранённой.
- Принудительное прерывание в заранее заданной контрольной точке не теряет подтверждённое состояние и не требует прежнего чата для возобновления.
- Трасса позволяет оценить, насколько цепочка была длинной, полезной и продуктивной: какие шаги создали проверяемую пользу, какие породили следующий материал, а какие оказались холостыми.
- Минимальная фикстура подтверждает детерминированное [воспроизведение принятого эпизода FUM](../../../Глоссарий/воспроизведение-принятого-эпизода-FUM.md); новый вызов реальной модели относится к [повторному живому исполнению FUM](../../../Глоссарий/повторное-живое-исполнение-FUM.md) и оценивается отдельно.

## Не входит в первый вариант

- Самостоятельная долговременная автономия без пользовательского запроса.
- Физическое действие, внешние сервисные операции и управление приватными данными.
- Полный автоматический дарвиновский отбор нескольких агентов или вычисление весов, хотя поля трассы должны готовить данные для такого отбора.
- Неограниченное ветвление, расходование неразрешённого провайдера или бюджета и перенос модельного победителя в каноническую память без отдельной политики принятия.
- Работающий канал непрерывного событийного наблюдения пользовательского ввода и перенаправление незавершённого runtime; версия `2` уже проверяет статический формат этого стыка отдельной локальной фикстурой, но не реализует канал.

Ограниченные [фоновые задания FUM](../../../Глоссарий/фоновое-задание-FUM.md) относятся не к первому варианту, а к следующему слою коробочного runtime. Формат [пользовательского перенаправления цикла](../../../Требования/🟡-пользовательское-перенаправление-непрерывного-агентского-цикла.md) уже проверен на синтетической безопасной контрольной точке; до фонового планировщика собственный runtime должен отдельно доказать корректность состояния, остановки, живого приёма входа и применения той же границы на явно заданной пользовательской цели.

## Зависимости

- [Память рабочей сессии](../01-память-рабочей-сессии/README.md) как предварительный контур фиксации результата.
- Локальные правила [воспроизводимых автоматизаций](../../../Глоссарий/автоматизация-FUM.md).
- Явная граница между модельным планом и реальным действием.
- Версионированный контракт неблокирующего модельного продолжения из [FUM-STEP-0106](../../карточки-шагов/✅-FUM-STEP-0106-закрепить-неблокирующее-модельное-ветвление-при-ожидании-подтверждения.md) до включения этого поведения в исполняемый runtime.
- [Безоконный Swift-контур](../../../Требования/✅-безоконный-Swift-контур-первого-коробочного-прототипа.md) и [воспроизводимое штатное пополнение памяти](../../../Требования/🚧-воспроизводимое-штатное-пополнение-памяти.md) как нижний слой состояния до добавления агентского выбора.
- Прояснение [развилки гиперсети и агентского цикла FUM](../../../Вопросы/2026-07-03_15-36-48_MSK_развилка-гиперсети-и-агентского-цикла-FUM.md) перед утверждением runtime, который должен вкладывать циклы друг в друга.

## Риски

- Можно преждевременно усложнить runtime, вместо того чтобы проверить маленький понятный цикл.
- Трасса не должна раскрывать скрытые рассуждения или приватные данные; ей достаточно фиксировать наблюдаемые действия, решения и результаты.
- Нельзя смешивать планирование будущего с утверждениями о фактическом состоянии проекта.
- Нельзя смешивать непрерывность входного и управляющего контура с непрерывным inference: [событийное наблюдение](../../../Требования/🟡-непрерывное-событийное-наблюдение-пользовательского-ввода.md) должно допускать явную фильтрацию и агрегацию без вызова LLM на каждое событие.

## Первый эксперимент

Первый узкий инженерный эксперимент завершён на синтетической локальной задаче. Собственный runtime чередует модельный шаг, строгий разбор намерения, проверку, разрешённое действие, наблюдение и решение о продолжении; сохраняет единственную трассу и терминальный исход; создаёт точный изолированный кандидат с изменением `artifact.txt`; передаёт его отдельному процессу приёмки. Внешний harness после подтверждённого внутреннего выбора и после подтверждённого наблюдения кандидата дважды завершает worker через `SIGKILL`, а новые PID продолжают эпизод из канонического `CURRENT` без прежнего чата, stdin и скрытого состояния процесса.

Автономная recorded-фикстура проверяет два варианта от общего предка, no-call-отказ третьему варианту по конечному бюджету, детерминированный replay и отсутствие model-, tool-, Git- и workspace-эффектов при воспроизведении. Один opt-in живой прогон тем же runtime использует уже доступный локальный model-only-провайдер и закрепляет его identity, usage, PID, checkpoint, candidate object и отдельную приёмку в [отчёте](../../../Прототипы/живой-одноагентный-эпизод/Отчёты/2026-08-01_19-37-43_MSK_живой-прогон-одноагентного-эпизода.md).

Этот инженерный эксперимент не меняет единственный активный продуктовый MVP архивирования прикрепляемых материалов, не назначает релиз FUM и не превращает точный синтетический Git-адаптер в общий allowlist действий.

Документационная форма эксперимента закреплена в [минимальном формате трассы исполняемого агентского цикла](../../../Документация/37-минимальный-формат-трассы-исполняемого-агентского-цикла.md). Прежняя локальная JSONL-фикстура проверяет семь типов событий, восстановимую ошибку, allowlist действия, отсутствие реального модельного шага и завершение по наблюдаемому результату. Завершённый Swift-эксперимент добавляет наблюдаемое исполнение только одного сценария; оба результата остаются входами для будущего `run`, а не утверждением о готовом универсальном исполнителе.

[FUM-STEP-0073](../../карточки-шагов/✅-FUM-STEP-0073-начать-безоконный-Swift-прототип-воспроизводимого-пополнения-памяти.md) завершил первый исполняемый Swift-bootstrap: два чистых replay дают одинаковый канонический артефакт, а недопустимый вход завершается типизированным отказом. [FUM-STEP-0074](../../карточки-шагов/✅-FUM-STEP-0074-добавить-восстанавливаемые-поколения-памяти-и-декларативную-GUI-проекцию.md) добавил восстанавливаемые поколения и доказал сходимость полного и инкрементального пути; постановка цели, модельный шаг и агентский выбор остаются отдельными критериями runtime.

[Контракт чистого модельного шага](../../../Документация/41-контракт-чистого-модельного-шага.md) версии `1` и его [Swift-прототип с детерминированной заглушкой](../../../Прототипы/чистый-модельный-шаг/README.md) теперь проверяют следующий изолированный слой: полный явный контекст, точную идентичность провайдера, пределы, запрет инструментов, файлов и сети, канонический ответ, связанный с входом, и структурированные ошибки. Заглушка не является LLM и не реализует runtime цикла; реальный локальный провайдер и неагентский режим `Codex CLI` требуют отдельных conformance-проверок.

[Завершённая карточка FUM-STEP-0072](../../карточки-шагов/✅-FUM-STEP-0072-описать-перенаправление-агентского-цикла-пользовательским-вводом.md) закрепила детерминированный эксперимент без внешних эффектов: разрешённый пользовательский вход поступает до завершения исходного плана, проходит через синтетическую безопасную контрольную точку и наблюдаемо меняет цель, ветку и ещё не начатое действие. Фикстура версии `2` различает сообщение-задачу, два первичных события и их агрегированный сигнал, сохраняя прежнее и новое продолжения; живой событийный runtime из этого результата не следует.

[Карточка FUM-STEP-0106](../../карточки-шагов/✅-FUM-STEP-0106-закрепить-неблокирующее-модельное-ветвление-при-ожидании-подтверждения.md) добавляет к этому контуру другой детерминированный сценарий: подтверждение точного перехода отсутствует, внешний эффект остаётся закрыт, а две бюджетированные модельные ветви от общего предка проверяются и сравниваются без выдачи модельного выбора за пользовательскую волю или за принятую каноническую память.

Последующий коробочный эксперимент добавляет две очереди: при отсутствии пользовательского ввода и готовой более приоритетной задачи запускается одно бюджетированное фоновое описание модели мира и языкового пространства тестовой LLM-заглушки; поступление пользовательского ввода сохраняет трассу и перенаправляет фон на объявленной безопасной контрольной точке.

## Источники требований

- [исходный запрос 2026-08-23 11:33:38 MSK — Вернуть ручную последовательную схему сессий](../../../Журнал/2026-08-23_11-33-38_MSK_вернуть-ручную-последовательную-схему-сессий/запрос.md)
- [исходный запрос 2026-08-11 23:30:57 MSK — Заменить автозапуск обязательным продолжением ветки](../../../Журнал/2026-08-11_23-30-57_MSK_заменить-автозапуск-обязательным-продолжением-ветки/запрос.md)
- [исходный запрос 2026-08-01 19:37:43 MSK — Замкнуть возобновление и живую приёмку одноагентного эпизода](../../../Журнал/2026-08-01_19-37-43_MSK_замкнуть-возобновление-и-живую-приёмку-одноагентного-эпизода/запрос.md)
- [исходный запрос 2026-07-29 10:25:10 MSK — Продолжать мышление при ожидании подтверждения](../../../Журнал/2026-07-29_10-25-10_MSK_продолжать-мышление-при-ожидании-подтверждения/запрос.md)
- [исходный запрос 2026-07-27 20:45:59 MSK — Интегрировать критический анализ и приоритеты развития FUM](../../../Журнал/2026-07-27_20-45-59_MSK_интегрировать-критический-анализ-и-приоритеты-развития-FUM/запрос.md)
- [исходный запрос 2026-07-25 09:09:06 MSK — Добавить восстанавливаемые поколения памяти и декларативную GUI-проекцию](../../../Журнал/2026-07-25_09-09-06_MSK_добавить-восстанавливаемые-поколения-памяти-и-декларативную-GUI-проекцию/запрос.md)
- [исходный запрос 2026-07-23 18:12:05 MSK - Проверить контракт чистого модельного шага для исполняемого агентского цикла](../../../Журнал/2026-07-23_18-12-05_MSK_проверить-контракт-чистого-модельного-шага-для-исполняемого-агентского-цикла/запрос.md)
- [исходный запрос 2026-06-24 14:08:09 MSK](../../../Журнал/2026-06-24_14-08-09_MSK/запрос.md)
- [исходный запрос 2026-06-24 16:22:00 MSK](../../../Журнал/2026-06-24_16-22-00_MSK/запрос.md)
- [исходный запрос 2026-06-25 18:30:09 MSK](../../../Журнал/2026-06-25_18-30-09_MSK/запрос.md)
- [исходный запрос 2026-07-03 15:36:48 MSK - Уточнить развилку гиперсети и агентского цикла](../../../Журнал/2026-07-03_15-36-48_MSK_уточнить-развилку-гиперсети-и-агентского-цикла/запрос.md)
- [исходный запрос 2026-07-14 03:18:36 MSK - Закрепить фоновые задания для простоя LLM](../../../Журнал/2026-07-14_03-18-36_MSK_закрепить-фоновые-задания-для-простоя-LLM/запрос.md)
- [исходный запрос 2026-07-22 13:07:48 MSK — Сформулировать минимальный формат трассы исполняемого агентского цикла](../../../Журнал/2026-07-22_13-07-48_MSK_сформулировать-минимальный-формат-трассы-исполняемого-агентского-цикла/запрос.md)
- [исходный запрос 2026-07-24 10:01:26 MSK — Уточнить событийную непрерывность документационного прототипа FUM](../../../Журнал/2026-07-24_10-01-26_MSK_уточнить-событийную-непрерывность-документационного-прототипа-FUM/запрос.md)
- [исходный запрос 2026-07-24 10:44:28 MSK — Начать безоконный Swift-прототип воспроизводимого пополнения памяти FUM](../../../Журнал/2026-07-24_10-44-28_MSK_начать-безоконный-Swift-прототип-воспроизводимого-пополнения-памяти-FUM/запрос.md)

## Опорные материалы

- [Отчёт о живом прогоне одноагентного эпизода](../../../Прототипы/живой-одноагентный-эпизод/Отчёты/2026-08-01_19-37-43_MSK_живой-прогон-одноагентного-эпизода.md)
- [Обзор актуальных реализаций агентских циклов](../../../Документация/06-обзор-агентских-циклов.md)
- [Воспроизводимые автоматизации FUM](../../../Документация/17-воспроизводимые-автоматизации.md)
- [Git-инфраструктура эволюционных цепочек FUM](../../../Документация/20-Git-инфраструктура-эволюционных-цепочек-FUM.md)
- [Агентский цикл](../../../Глоссарий/агентский-цикл.md)
- [Развилка гиперсети и агентского цикла FUM](../../../Вопросы/2026-07-03_15-36-48_MSK_развилка-гиперсети-и-агентского-цикла-FUM.md)
- [Паспорт начального коробочного прототипа FUM](../../../Документация/43-паспорт-начального-коробочного-прототипа-FUM.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-08-24 09:37:36 MSK -->
<!-- content-sha256: sha256:90759cbf0e68d276732a06ac1133e3c95cec3dd46e6017ca127b12421d285dda -->
<!-- FUM-MD-RECENCY:END -->
