# Следующие шаги веток

Этот каталог хранит по одному версионному селектору [следующего шага ветки](../../Глоссарий/следующий-шаг-ветки.md) для каждой автоматически развиваемой именованной [ветки работы](../../Глоссарий/ветка-работы.md). Селектор не копирует задачу и критерии: он выбирает одну актуальную [карточку шага](../карточки-шагов/README.md) по устойчивому `card_id` и закрепляет точный хэш её содержания.

Идентичность ветки задаётся полным `branch_ref` внутри записи, а не именем файла. Для активной ветки должно находиться ровно одно совпадение. Файл `README.md` описывает формат и не участвует в выборе.

## Формат селектора

Запись начинается с TOML-блока схемы `2`:

```toml
+++
schema_version = 2
branch_ref = "refs/heads/project/пример"
step_id = "project-example-fum-step-0042-v1"
status = "ready"
project_path = "Проекты/пример/README.md"
card_id = "FUM-STEP-0042"
card_content_sha256 = "sha256:<64 шестнадцатеричных символа>"
+++
```

После блока располагается ровно один заголовок первого уровня. Разделы `Задача` и `Критерии завершения` запрещены: они принадлежат карточке. Другие поясняющие разделы разрешены. HTML-комментарии вне fenced-блоков запрещены, чтобы скрытый для человека текст не попадал в агентский prompt.

`project_path` является нормализованным путём к существующему паспорту проекта относительно корня репозитория: для `master` он равен `README.md`, а для `project/<имя>` — `Проекты/<имя>/README.md`. Поле `card_content_sha256` вычисляется по полному UTF-8-содержанию карточки после удаления только конечного служебного блока `FUM-MD-RECENCY`, удаления конечных пробельных символов и добавления одного перевода строки.

Допустимые статусы выбора:

- `ready` — закреплённая актуальная карточка разрешена к однократному автоматическому запуску;
- `paused` — запуск актуальной карточки временно приостановлен осознанным решением;
- `blocked` — продолжение по актуальной карточке требует нового входа, полномочия или внешнего изменения;
- `done` — у ветки нет следующей карточки; поля `card_id` и `card_content_sha256` отсутствуют.

Для `ready`, `paused` и `blocked` карточка обязательна и должна иметь статус `active`. `step_id` — устойчивый технический идентификатор конкретной версии выбора и claim-fence диспетчера. Смена карточки, её содержательного хэша или статуса выбора требует нового `step_id`.

## Жизненный цикл

Пятиминутный диспетчер Codex сначала проверяет recent-снимок до 50 задач. Если он видит другую активную задачу, неизвестное состояние или недоступный host, то не резервирует шаг, не ставит плановую работу в локальную очередь и ничего не создаёт. Только при наблюдаемом простое локальная автоматизация [fum-branch-next-step](../../Инструменты/fum-branch-next-step/SKILL.md) проверяет активный ref, `step_id`, карточку и её хэш, атомарно резервирует пару `branch_ref` и `step_id` в общем Git-каталоге и разрешает создать обычную задачу Codex в локальном проекте.

Резервация хранится как JSON blob под служебной Git-ссылкой физического checkout и меняется compare-and-swap. Она не требует POSIX-блокировок или ручного переноса файлов прежней реализации; проигравший CAS-вызов не может заменить конкурентно опубликованный другой `step_id` без новой проверки.

Созданная задача первым действием регистрируется в общей [FIFO-очереди checkout](../../Инструменты/fum-ocheredj-zadach-git-vetki/SKILL.md) и до собственного допуска только ждёт. После `admitted` она повторно проверяет ожидаемые `branch_ref` и `step_id`, полностью читает возвращённые `record_path`, `card_path` и `project_path` и соблюдает границы действий, доступа, публикации и проверки паспорта.

После успешной работы задача в том же атомарном commit+handoff переводит выполненную карточку в исторический статус с разделом `Результат`, а затем:

- выбирает другую актуальную карточку со свежими `card_content_sha256` и `step_id` в состоянии `ready`, `paused` или `blocked`; либо
- устанавливает для ветки `done` без карточных полей.

Прежний `step_id` автоматически повторно не запускается. Claim не имеет TTL: после аварии он снимается только по предварительно наблюдённому `lease_id` и после внешнего подтверждения, что прежняя задача больше не исполняется.

Проверка списка задач и создание новой задачи не образуют единой транзакции. Сам список не сообщает общее число или полноту и потому не доказывает состояние более старых задач за пределами recent-снимка. Повторная инвентаризация после claim, FIFO-очередь checkout и проверка идентичности внутри созданной задачи сужают окно гонки, но не обещают абсолютную глобальную блокировку между разными host или клонами.

## Создание проектной ветки

Для новой ветки `project/<имя>`:

1. Создать `Проекты/<имя>/README.md` с паспортом проекта.
2. Создать актуальную карточку шага и включить её в [индекс карточек](../карточки-шагов/README.md).
3. Создать в этом каталоге селектор с точным `refs/heads/project/<имя>`, `card_id`, хэшем карточки и новым `step_id`.
4. Запустить `python3 Инструменты/fum-branch-next-step/scripts/branch-next-step.py validate --repo-root . --json`.
5. Закоммитить паспорт, карточку, индекс и селектор до разрешения фонового запуска.

## Источники требований

- [исходный запрос 2026-07-22 02:59:22 MSK - Декомпозировать предложения на карточки шагов](../../Запросы/2026-07-22_02-59-22_MSK_декомпозировать-предложения-на-карточки-шагов.md)
- [исходный запрос 2026-07-20 20:06:04 MSK - Запускать следующие шаги веток](../../Запросы/2026-07-20_20-06-04_MSK_запускать-следующие-шаги-веток.md)
- [исходный запрос 2026-07-21 18:31:35 MSK — Ввести последовательную очередь сессий без hooks](../../Запросы/2026-07-21_18-31-35_MSK_ввести-последовательную-очередь-сессий-без-hooks.md)

<!-- FUM-MD-RECENCY:BEGIN -->
<!-- last-content-edit: 2026-07-22 03:17:01 MSK -->
<!-- content-sha256: sha256:d1b746441fce61fb0278dda38586fed81511301144d7d736300041cdc333c158 -->
<!-- FUM-MD-RECENCY:END -->
