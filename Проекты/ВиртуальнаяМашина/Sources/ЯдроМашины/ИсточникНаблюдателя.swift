import Foundation

/// Выбранные и связанные байты исполнения; проверка Python принадлежит вызывающему мосту.
struct ИсточникНаблюдателя {
    let питон: Data
    let наблюдатель: Data
    let происхождение: String

    private init(питон: Data, наблюдатель: Data, происхождение: String) {
        self.питон = питон
        self.наблюдатель = наблюдатель
        self.происхождение = происхождение
    }

    /// Только отсутствие соседней поставки допускает явный снимок разработки.
    /// Обязательный проверяющий получает JSON пяти полей Python после проверки источника.
    /// Результат возвращает те же проверенные Data без повторного открытия для их получения.
    /// Происхождение связывает Python, наблюдатель и фактический исполняемый файл.
    static func прочитать(исполняемыйФайл: URL, ресурс: URL, явныйСнимок: String?,
                          проверитьПитон: (Data) throws -> Void) throws -> ИсточникНаблюдателя {
        throw ОшибкаМашины("Выбор источника наблюдателя ожидает первого RED.")
    }
}
